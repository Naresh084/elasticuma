Design a copy-on-write B+ tree in C++20 using only the standard library. It must support concurrent snapshots, bounded page-cache memory, crash-safe root publication, range scans, and background compaction without invalidating readers. Explain page and journal formats, ownership and synchronization invariants, then provide a compact implementation skeleton and tests for splits, snapshot isolation, torn root writes, eviction, and compaction races.
