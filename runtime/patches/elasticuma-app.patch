diff --git a/Sources/TurboFieldfare/Tokenization/StructuredAssistantDecoder.swift b/Sources/TurboFieldfare/Tokenization/StructuredAssistantDecoder.swift
index 4b9676e..dc40ecf 100644
--- a/Sources/TurboFieldfare/Tokenization/StructuredAssistantDecoder.swift
+++ b/Sources/TurboFieldfare/Tokenization/StructuredAssistantDecoder.swift
@@ -2,6 +2,9 @@ import Foundation
 
 public enum StructuredAssistantEvent: Equatable, Sendable {
     case content(String)
+    case reasoningStarted
+    case reasoning(String)
+    case reasoningEnded
     case toolCall(ParsedToolCall)
 }
 
@@ -122,7 +125,7 @@ public final class StructuredAssistantDecoder: @unchecked Sendable {
         case .thought:
             reasoning += delta
             reasoningTokenCount += 1
-            return []
+            return delta.isEmpty ? [] : [.reasoning(delta)]
         case .visible:
             return delta.isEmpty ? [] : [.content(delta)]
         case .label:
@@ -180,7 +183,7 @@ public final class StructuredAssistantDecoder: @unchecked Sendable {
         }
         if tokenID == tokenizer.thinkStartID {
             channel = .thought
-            return []
+            return [.reasoningStarted]
         }
         if tokenID == tokenizer.thinkEndID {
             channel = .visible
@@ -189,12 +192,12 @@ public final class StructuredAssistantDecoder: @unchecked Sendable {
             // drops it (`content.split('</think>')[-1].lstrip('\n')`), so a
             // reply would otherwise start with two blank lines.
             trimmingLeadingNewlines = true
-            return []
+            return [.reasoningEnded]
         }
         guard channel != .thought else {
             reasoning += delta
             reasoningTokenCount += 1
-            return []
+            return delta.isEmpty ? [] : [.reasoning(delta)]
         }
         var visible = delta
         if trimmingLeadingNewlines {
@@ -210,5 +213,29 @@ public final class StructuredAssistantDecoder: @unchecked Sendable {
         }
     }
 
+    /// Flush text from the streaming detokenizer without inventing a token ID.
+    /// This preserves the active thought/visible channel at a stop boundary.
+    public func consumeTrailingText(_ delta: String) throws -> [StructuredAssistantEvent] {
+        guard !failed, toolTokens == nil else {
+            throw ToolCallParserError.malformed
+        }
+        guard !delta.isEmpty else { return [] }
+        switch channel {
+        case .thought:
+            reasoning += delta
+            return [.reasoning(delta)]
+        case .label:
+            label += delta
+            return []
+        case .visible:
+            var visible = delta
+            if trimmingLeadingNewlines {
+                visible = String(visible.drop(while: { $0 == "\n" }))
+                if !visible.isEmpty { trimmingLeadingNewlines = false }
+            }
+            return visible.isEmpty ? [] : [.content(visible)]
+        }
+    }
+
     public var hasToolCalls: Bool { emittedCalls > 0 }
 }
diff --git a/Sources/TurboFieldfare/Tokenization/Tokenizer.swift b/Sources/TurboFieldfare/Tokenization/Tokenizer.swift
index 3e37c28..f78d42b 100644
--- a/Sources/TurboFieldfare/Tokenization/Tokenizer.swift
+++ b/Sources/TurboFieldfare/Tokenization/Tokenizer.swift
@@ -348,11 +348,23 @@ public struct GFTokenizer: @unchecked Sendable {
     /// token IDs — which is what makes thinking affordable here.
     private static let chatMLGenerationSuffix =
         "<|im_start|>assistant\n<think>\n"
+    private static let chatMLNonThinkingGenerationSuffix =
+        "<|im_start|>assistant\n<think>\n\n</think>\n\n"
 
     public func applyChatTemplate(_ messages: [Message]) throws -> String {
         switch dialect {
         case .gemma: return try gemmaChatTemplate(messages)
-        case .chatml: return try chatMLChatTemplate(messages)
+        case .chatml: return try chatMLChatTemplate(messages, enableThinking: true)
+        }
+    }
+
+    /// User-facing chat path for clients that do not expose a reasoning UI.
+    /// Qwen's bundled template accepts this exact closed-think generation
+    /// suffix when `enable_thinking=false`.
+    public func applyChatTemplateWithoutThinking(_ messages: [Message]) throws -> String {
+        switch dialect {
+        case .gemma: return try gemmaChatTemplate(messages)
+        case .chatml: return try chatMLChatTemplate(messages, enableThinking: false)
         }
     }
 
@@ -373,7 +385,10 @@ public struct GFTokenizer: @unchecked Sendable {
         return s
     }
 
-    private func chatMLChatTemplate(_ messages: [Message]) throws -> String {
+    private func chatMLChatTemplate(
+        _ messages: [Message],
+        enableThinking: Bool
+    ) throws -> String {
         var s = ""
         for (index, message) in messages.enumerated() {
             guard let rawContent = message.content else {
@@ -385,7 +400,9 @@ public struct GFTokenizer: @unchecked Sendable {
             }
             s += Self.imStartMark + message.role.rawValue + "\n" + content + Self.imEndMark + "\n"
         }
-        s += Self.chatMLGenerationSuffix
+        s += enableThinking
+            ? Self.chatMLGenerationSuffix
+            : Self.chatMLNonThinkingGenerationSuffix
         return s
     }
 
diff --git a/Sources/TurboFieldfareApp/Core/Configuration/AppRuntimeOptions.swift b/Sources/TurboFieldfareApp/Core/Configuration/AppRuntimeOptions.swift
index cef9c13..9ca35b4 100644
--- a/Sources/TurboFieldfareApp/Core/Configuration/AppRuntimeOptions.swift
+++ b/Sources/TurboFieldfareApp/Core/Configuration/AppRuntimeOptions.swift
@@ -49,6 +49,20 @@ public enum AppModelVerification: String, CaseIterable, Sendable, Identifiable {
     }
 }
 
+public enum AppExpertCacheResidency: String, CaseIterable, Codable, Sendable, Identifiable {
+    case fixed
+    case osManaged = "os-managed"
+
+    public var id: String { rawValue }
+
+    public var label: String {
+        switch self {
+        case .fixed: return "Fixed"
+        case .osManaged: return "OS-managed"
+        }
+    }
+}
+
 public struct AppRuntimeOptions: Equatable, Sendable {
     public static let allowedSlotCounts = RuntimeConfiguration.allowedExpertCacheSlots
     public static let allowedPrefillChunkTokens = RuntimeConfiguration.allowedPrefillChunkTokens
@@ -59,19 +73,25 @@ public struct AppRuntimeOptions: Equatable, Sendable {
     public var prefillChunkTokens: Int
     public var rdadvisePolicy: AppRDAdvicePolicy
     public var modelVerification: AppModelVerification
+    public var expertCacheResidency: AppExpertCacheResidency
+    public var expertCacheHotSlots: Int
 
-    public init(expertCacheSlots: Int = RuntimeDefaults.expertCacheSlots,
+    public init(expertCacheSlots: Int = 96,
                 expertCachePolicy: AppExpertCachePolicy = .lfu,
                 prefillEnabled: Bool = RuntimeDefaults.prefillEnabled,
                 prefillChunkTokens: Int = RuntimeDefaults.prefillChunkTokens,
                 rdadvisePolicy: AppRDAdvicePolicy = .off,
-                modelVerification: AppModelVerification = .fullSha256) {
+                modelVerification: AppModelVerification = .fullSha256,
+                expertCacheResidency: AppExpertCacheResidency = .osManaged,
+                expertCacheHotSlots: Int? = nil) {
         self.expertCacheSlots = expertCacheSlots
         self.expertCachePolicy = expertCachePolicy
         self.prefillEnabled = prefillEnabled
         self.prefillChunkTokens = prefillChunkTokens
         self.rdadvisePolicy = rdadvisePolicy
         self.modelVerification = modelVerification
+        self.expertCacheResidency = expertCacheResidency
+        self.expertCacheHotSlots = expertCacheHotSlots ?? min(16, expertCacheSlots)
     }
 
     public func validate() throws {
@@ -83,6 +103,10 @@ public struct AppRuntimeOptions: Equatable, Sendable {
             throw AppInferenceError.invalidRequest(
                 "prefill chunk size must be one of \(Self.allowedPrefillChunkTokens)")
         }
+        guard (0...expertCacheSlots).contains(expertCacheHotSlots) else {
+            throw AppInferenceError.invalidRequest(
+                "hot expert slots must be between zero and the logical cache size")
+        }
     }
 
     public var prefillConfig: PrefillRuntimeConfig {
@@ -92,7 +116,10 @@ public struct AppRuntimeOptions: Equatable, Sendable {
     public var resultSummary: String {
         let prefill = prefillEnabled ? "prefill \(prefillChunkTokens)" : "prefill off"
         let verification = modelVerification == .fullSha256 ? "full SHA-256" : "trusted receipt"
-        return "Cache \(expertCacheSlots) \(expertCachePolicy.label), \(prefill), FP16 KV, RDADVISE \(rdadvisePolicy.label.lowercased()), \(verification)"
+        let residency = expertCacheResidency == .osManaged
+            ? "OS-managed / hot \(expertCacheHotSlots)"
+            : "fixed"
+        return "Cache \(expertCacheSlots) \(expertCachePolicy.label), \(residency), \(prefill), FP16 KV, RDADVISE \(rdadvisePolicy.label.lowercased()), \(verification)"
     }
 
     public static func slotsLabel(for slots: Int) -> String {
@@ -124,6 +151,8 @@ public struct AppLoadedRuntimeKey: Equatable, Sendable {
     public var expertCachePolicy: AppExpertCachePolicy
     public var rdadvisePolicy: AppRDAdvicePolicy
     public var modelVerification: AppModelVerification
+    public var expertCacheResidency: AppExpertCacheResidency
+    public var expertCacheHotSlots: Int
     public var forceLogitsHead: Bool
 
     public init(modelDirectory: URL,
@@ -136,6 +165,8 @@ public struct AppLoadedRuntimeKey: Equatable, Sendable {
         self.expertCachePolicy = options.expertCachePolicy
         self.rdadvisePolicy = options.rdadvisePolicy
         self.modelVerification = options.modelVerification
+        self.expertCacheResidency = options.expertCacheResidency
+        self.expertCacheHotSlots = options.expertCacheHotSlots
         self.forceLogitsHead = forceLogitsHead
     }
 }
diff --git a/Sources/TurboFieldfareApp/Core/Configuration/MacAppSettings.swift b/Sources/TurboFieldfareApp/Core/Configuration/MacAppSettings.swift
index c81121b..8bba04c 100644
--- a/Sources/TurboFieldfareApp/Core/Configuration/MacAppSettings.swift
+++ b/Sources/TurboFieldfareApp/Core/Configuration/MacAppSettings.swift
@@ -3,23 +3,21 @@ import TurboFieldfare
 
 struct MacAppSettings: Codable, Equatable, Sendable {
     static let fileName = "mac-app-settings.json"
-    /// 2: the expert-cache default moved 16 -> 64 on measured evidence
-    /// (+10.7% on a real agent workload). A stored settings file pins its own
-    /// value, so without this bump every existing install would keep running
-    /// 16 and never see the change. Bumping invalidates the file and
-    /// re-defaults it, which is why the tuning fields below take their values
-    /// from RuntimeDefaults rather than repeating literals.
-    static let currentVersion = 2
+    /// Version 4 adds the per-model thinking preference.
+    static let currentVersion = 4
 
     var version: Int = currentVersion
     var contextTokens: Int = AppContextLengthOption.fourK.tokens
-    var expertCacheSlots: Int = RuntimeDefaults.expertCacheSlots
+    var expertCacheSlots: Int = 96
     var temperature: Double = 0.2
     var topKEnabled: Bool = true
     var topK: Int = 64
     var topPEnabled: Bool = true
     var topP: Double = 0.95
+    var thinkingEnabled: Bool = false
     var prefillEnabled: Bool = RuntimeDefaults.prefillEnabled
+    var expertCacheResidency: AppExpertCacheResidency = .osManaged
+    var expertCacheHotSlots: Int = 16
 
     func isValid() -> Bool {
         version == Self.currentVersion
@@ -28,6 +26,7 @@ struct MacAppSettings: Codable, Equatable, Sendable {
             && temperature.isFinite && (0...2).contains(temperature)
             && (1...256).contains(topK)
             && topP.isFinite && (0.01...1).contains(topP)
+            && (0...expertCacheSlots).contains(expertCacheHotSlots)
     }
 }
 
diff --git a/Sources/TurboFieldfareApp/Core/Diagnostics/AppDiagnostics.swift b/Sources/TurboFieldfareApp/Core/Diagnostics/AppDiagnostics.swift
index b604027..bbf5813 100644
--- a/Sources/TurboFieldfareApp/Core/Diagnostics/AppDiagnostics.swift
+++ b/Sources/TurboFieldfareApp/Core/Diagnostics/AppDiagnostics.swift
@@ -21,6 +21,10 @@ public struct AppRunnerDiagnostics: Equatable, Sendable {
     public var rdadviseMegabytesPerToken: Double
     public var rdadviseSkippedPerToken: Double
     public var rdadviseFailures: UInt64
+    public var expertHotSlots: Int?
+    public var expertVolatileTransitions: UInt64
+    public var expertEmptyRecoveries: UInt64
+    public var expertReclaimedBytes: UInt64
 
     public init(cb1MillisecondsPerToken: Double = 0,
                 ioMillisecondsPerToken: Double = 0,
@@ -30,7 +34,11 @@ public struct AppRunnerDiagnostics: Equatable, Sendable {
                 rdadviseCallsPerToken: Double = 0,
                 rdadviseMegabytesPerToken: Double = 0,
                 rdadviseSkippedPerToken: Double = 0,
-                rdadviseFailures: UInt64 = 0) {
+                rdadviseFailures: UInt64 = 0,
+                expertHotSlots: Int? = nil,
+                expertVolatileTransitions: UInt64 = 0,
+                expertEmptyRecoveries: UInt64 = 0,
+                expertReclaimedBytes: UInt64 = 0) {
         self.cb1MillisecondsPerToken = cb1MillisecondsPerToken
         self.ioMillisecondsPerToken = ioMillisecondsPerToken
         self.cb2MillisecondsPerToken = cb2MillisecondsPerToken
@@ -40,6 +48,10 @@ public struct AppRunnerDiagnostics: Equatable, Sendable {
         self.rdadviseMegabytesPerToken = rdadviseMegabytesPerToken
         self.rdadviseSkippedPerToken = rdadviseSkippedPerToken
         self.rdadviseFailures = rdadviseFailures
+        self.expertHotSlots = expertHotSlots
+        self.expertVolatileTransitions = expertVolatileTransitions
+        self.expertEmptyRecoveries = expertEmptyRecoveries
+        self.expertReclaimedBytes = expertReclaimedBytes
     }
 }
 
@@ -86,10 +98,30 @@ public struct AppDiagnostics: Equatable, Sendable {
     }
 }
 
+public enum AppTokenChannel: String, Codable, Equatable, Sendable {
+    case content
+    case reasoningStarted = "reasoning-started"
+    case reasoning
+    case reasoningEnded = "reasoning-ended"
+}
+
 public struct AppTokenEvent: Equatable, Sendable {
     public var index: Int
     public var textDelta: String
     public var elapsedDecodeSeconds: Double
+    public var channel: AppTokenChannel
+
+    public init(
+        index: Int,
+        textDelta: String,
+        elapsedDecodeSeconds: Double,
+        channel: AppTokenChannel = .content
+    ) {
+        self.index = index
+        self.textDelta = textDelta
+        self.elapsedDecodeSeconds = elapsedDecodeSeconds
+        self.channel = channel
+    }
 }
 
 public enum AppInferenceEvent: Equatable, Sendable {
diff --git a/Sources/TurboFieldfareApp/Core/Inference/AppGenerationRequest.swift b/Sources/TurboFieldfareApp/Core/Inference/AppGenerationRequest.swift
index 28f833c..db2a9ca 100644
--- a/Sources/TurboFieldfareApp/Core/Inference/AppGenerationRequest.swift
+++ b/Sources/TurboFieldfareApp/Core/Inference/AppGenerationRequest.swift
@@ -9,6 +9,7 @@ public struct AppGenerationRequest: Equatable, Sendable {
     public var topK: Int?
     public var topP: Float?
     public var repetitionPenalty: Float
+    public var thinkingEnabled: Bool
     public var runtimeOptions: AppRuntimeOptions
 
     public init(modelDirectory: URL,
@@ -19,6 +20,7 @@ public struct AppGenerationRequest: Equatable, Sendable {
                 topK: Int? = 64,
                 topP: Float? = 0.95,
                 repetitionPenalty: Float = 1.0,
+                thinkingEnabled: Bool = false,
                 runtimeOptions: AppRuntimeOptions = AppRuntimeOptions()) {
         self.modelDirectory = modelDirectory
         self.prompt = prompt
@@ -28,6 +30,7 @@ public struct AppGenerationRequest: Equatable, Sendable {
         self.topK = topK
         self.topP = topP
         self.repetitionPenalty = repetitionPenalty
+        self.thinkingEnabled = thinkingEnabled
         self.runtimeOptions = runtimeOptions
     }
 
diff --git a/Sources/TurboFieldfareApp/Core/Inference/DecodeServiceInferenceClient.swift b/Sources/TurboFieldfareApp/Core/Inference/DecodeServiceInferenceClient.swift
index a4e9e28..24adc86 100644
--- a/Sources/TurboFieldfareApp/Core/Inference/DecodeServiceInferenceClient.swift
+++ b/Sources/TurboFieldfareApp/Core/Inference/DecodeServiceInferenceClient.swift
@@ -76,6 +76,7 @@ public final class DecodeServiceInferenceClient: AppModelLifecycleClient,
                         maxContextTokens: request.maxContextTokens,
                         temperature: request.temperature,
                         repetitionPenalty: request.repetitionPenalty,
+                        thinkingEnabled: request.thinkingEnabled,
                         runtimeOptions: Self.decodeRuntimeOptions(request.runtimeOptions),
                         generationID: generationID)
                     try handles.input.write(contentsOf: DecodeFrameCodec.encode(
@@ -104,6 +105,17 @@ public final class DecodeServiceInferenceClient: AppModelLifecycleClient,
                             continue
                         }
                         if event.kind == .snapshot {
+                            let channel = event.tokenChannel.flatMap(AppTokenChannel.init(rawValue:))
+                                ?? .content
+                            if channel != .content {
+                                continuation.yield(.token(AppTokenEvent(
+                                    index: max(0, event.tokenCount - 1),
+                                    textDelta: event.textDelta,
+                                    elapsedDecodeSeconds: event.decodeSeconds,
+                                    channel: channel)))
+                                continue
+                            }
+
                             generationTranscriptMailbox.append(event.textDelta)
                             let now = Date()
                             let beginsVisibleText = !hasYieldedVisibleText
@@ -115,7 +127,8 @@ public final class DecodeServiceInferenceClient: AppModelLifecycleClient,
                                 continuation.yield(.token(AppTokenEvent(
                                     index: max(0, event.tokenCount - 1),
                                     textDelta: beginsVisibleText ? event.textDelta : "",
-                                    elapsedDecodeSeconds: event.decodeSeconds)))
+                                    elapsedDecodeSeconds: event.decodeSeconds,
+                                    channel: .content)))
                             }
                             continue
                         }
@@ -306,7 +319,11 @@ public final class DecodeServiceInferenceClient: AppModelLifecycleClient,
             rdadviseCallsPerToken: value.rdadviseCallsPerToken,
             rdadviseMegabytesPerToken: value.rdadviseMegabytesPerToken,
             rdadviseSkippedPerToken: value.rdadviseSkippedPerToken,
-            rdadviseFailures: value.rdadviseFailures)
+            rdadviseFailures: value.rdadviseFailures,
+            expertHotSlots: value.expertHotSlots,
+            expertVolatileTransitions: value.expertVolatileTransitions,
+            expertEmptyRecoveries: value.expertEmptyRecoveries,
+            expertReclaimedBytes: value.expertReclaimedBytes)
     }
 
     private static func decodeRuntimeOptions(_ options: AppRuntimeOptions)
@@ -317,7 +334,9 @@ public final class DecodeServiceInferenceClient: AppModelLifecycleClient,
             prefillEnabled: options.prefillEnabled,
             prefillChunkTokens: options.prefillChunkTokens,
             rdadvisePolicy: options.rdadvisePolicy.rawValue,
-            modelVerification: options.modelVerification.rawValue)
+            modelVerification: options.modelVerification.rawValue,
+            expertCacheResidency: options.expertCacheResidency.rawValue,
+            expertCacheHotSlots: options.expertCacheHotSlots)
     }
 
     private static func removeLaunchJob(label: String) {
@@ -333,6 +352,6 @@ public final class DecodeServiceInferenceClient: AppModelLifecycleClient,
     private static func defaultServiceURL() -> URL {
         return Bundle.main.executableURL!
             .deletingLastPathComponent()
-            .appendingPathComponent("TurboFieldfareDecodeService")
+            .appendingPathComponent("slipstream-decode-service")
     }
 }
diff --git a/Sources/TurboFieldfareApp/Core/Inference/RealInferenceClient.swift b/Sources/TurboFieldfareApp/Core/Inference/RealInferenceClient.swift
index 2406f93..27c5e44 100644
--- a/Sources/TurboFieldfareApp/Core/Inference/RealInferenceClient.swift
+++ b/Sources/TurboFieldfareApp/Core/Inference/RealInferenceClient.swift
@@ -154,6 +154,7 @@ actor RealInferenceSession {
     private var ctx: MetalContext?
     private var tokenizer: GFTokenizer?
     private var tokenizerDirectoryCache = TokenizerDirectoryCache()
+    private var model: Model?
     private var runner: RealForwardRunner?
     private var scratch: RawCompletionScratch?
 
@@ -162,6 +163,7 @@ actor RealInferenceSession {
         if loadedKey == key, runner != nil { return }
 
         runner = nil
+        model = nil
         scratch = nil
         loadedKey = nil
 
@@ -208,11 +210,15 @@ actor RealInferenceSession {
                 context: context,
                 maxContext: key.maxContext,
                 runtimeConfiguration: runtimeConfiguration)
+            if key.options.expertCacheResidency == .osManaged {
+                loadedRunner.expertResidencyHotSlots = key.options.expertCacheHotSlots
+            }
             let loadedScratch = try RawCompletionScratch(context: context,
                                                          vocab: loadedModel.config.vocabSize,
                                                          logitSoftcap: Float(loadedModel.config.finalLogitSoftcap))
             try Task.checkCancellation()
 
+            model = loadedModel
             runner = loadedRunner
             scratch = loadedScratch
             loadedKey = key
@@ -254,6 +260,7 @@ actor RealInferenceSession {
     }
 
     func unload() {
+        model = nil
         runner = nil
         scratch = nil
         tokenizer = nil
@@ -284,9 +291,10 @@ actor RealInferenceSession {
                 throw AppInferenceError.modelLoadFailed("session lost its loaded state")
             }
 
-            let renderedPrompt = try tokenizer.applyChatTemplate([
-                GFTokenizer.Message(role: .user, content: request.prompt)
-            ])
+            let messages = [GFTokenizer.Message(role: .user, content: request.prompt)]
+            let renderedPrompt = try request.thinkingEnabled
+                ? tokenizer.applyChatTemplate(messages)
+                : tokenizer.applyChatTemplateWithoutThinking(messages)
             let promptIds = tokenizer.encode(renderedPrompt, addBOS: false)
             progress.promptTokenCount = promptIds.count
             guard promptIds.count < runner.maxContext else {
@@ -305,10 +313,21 @@ actor RealInferenceSession {
             runner.reset()
             progress.prefillStart = Date()
 
+            let decoder = tokenizer.dialect == .chatml
+                ? StructuredAssistantDecoder(
+                    tokenizer: tokenizer,
+                    allowedTools: [],
+                    thinkingPreopened: request.thinkingEnabled)
+                : nil
+            var decodingError: Error?
+            var shouldStop = false
+
             let result = try await runRawCompletion(
                 producer: runner, tokenizer: tokenizer, promptIds: promptIds,
                 config: config, context: ctx, scratch: scratch,
-                prefillConfig: prefillConfig) { event in
+                prefillConfig: prefillConfig,
+                shouldStop: { shouldStop }) { event in
+                guard decodingError == nil else { return }
                 switch event {
                 case .prefill(let done, let total):
                     if done == total {
@@ -316,21 +335,55 @@ actor RealInferenceSession {
                         progress.countersAtDecodeStart = RunnerCounterSnapshot(runner)
                     }
                     continuation.yield(.prefillProgress(done: done, total: total))
-                case .token(let index, _, let delta):
-                    if progress.firstTokenDate == nil { progress.firstTokenDate = Date() }
-                    progress.generated = index + 1
-                    if index % 8 == 0 { _ = memorySampler.sample() }
-                    continuation.yield(.token(AppTokenEvent(
-                        index: index,
-                        textDelta: delta,
-                        elapsedDecodeSeconds: progress.elapsedDecodeSeconds)))
+                case .token(let index, let tokenID, let delta):
+                    do {
+                        let events = if let decoder {
+                            try decoder.consume(tokenID: tokenID, delta: delta)
+                        } else {
+                            delta.isEmpty ? [] : [StructuredAssistantEvent.content(delta)]
+                        }
+                        if progress.firstTokenDate == nil { progress.firstTokenDate = Date() }
+                        progress.generated = index + 1
+                        if index % 8 == 0 { _ = memorySampler.sample() }
+                        let appEvents = try Self.appTokenEvents(
+                            events,
+                            index: index,
+                            elapsedDecodeSeconds: progress.elapsedDecodeSeconds)
+                        if appEvents.isEmpty, decoder == nil {
+                            continuation.yield(.token(AppTokenEvent(
+                                index: index,
+                                textDelta: "",
+                                elapsedDecodeSeconds: progress.elapsedDecodeSeconds)))
+                        } else {
+                            for appEvent in appEvents {
+                                continuation.yield(.token(appEvent))
+                            }
+                        }
+                    } catch {
+                        decodingError = error
+                        shouldStop = true
+                    }
                 case .tail(let text):
-                    continuation.yield(.token(AppTokenEvent(
-                        index: max(progress.generated - 1, 0),
-                        textDelta: text,
-                        elapsedDecodeSeconds: progress.elapsedDecodeSeconds)))
+                    do {
+                        let events = if let decoder {
+                            try decoder.consumeTrailingText(text)
+                        } else {
+                            text.isEmpty ? [] : [StructuredAssistantEvent.content(text)]
+                        }
+                        for appEvent in try Self.appTokenEvents(
+                            events,
+                            index: max(progress.generated - 1, 0),
+                            elapsedDecodeSeconds: progress.elapsedDecodeSeconds) {
+                            continuation.yield(.token(appEvent))
+                        }
+                    } catch {
+                        decodingError = error
+                        shouldStop = true
+                    }
                 }
             }
+            if let decodingError { throw decodingError }
+            try decoder?.finish()
 
             let diagnostics = makeDiagnostics(request: request,
                                               memorySampler: memorySampler,
@@ -435,6 +488,7 @@ actor RealInferenceSession {
         func ms(_ end: UInt64, _ start: UInt64) -> Double {
             Double(end &- start) / 1_000_000 / forwards
         }
+        let residency = model?.routedExpertCacheResidencyStats()
         return AppRunnerDiagnostics(
             cb1MillisecondsPerToken: ms(now.cb1, base.cb1),
             ioMillisecondsPerToken: ms(now.io, base.io),
@@ -444,7 +498,11 @@ actor RealInferenceSession {
             rdadviseCallsPerToken: Double(now.rdadviseCalls &- base.rdadviseCalls) / forwards,
             rdadviseMegabytesPerToken: Double(now.rdadviseBytes &- base.rdadviseBytes) / 1_048_576.0 / forwards,
             rdadviseSkippedPerToken: Double(now.rdadviseSkipped &- base.rdadviseSkipped) / forwards,
-            rdadviseFailures: now.rdadviseFailures &- base.rdadviseFailures)
+            rdadviseFailures: now.rdadviseFailures &- base.rdadviseFailures,
+            expertHotSlots: runner.expertResidencyHotSlots,
+            expertVolatileTransitions: residency?.volatileTransitions ?? 0,
+            expertEmptyRecoveries: residency?.emptyRecoveries ?? 0,
+            expertReclaimedBytes: residency?.reclaimedBytes ?? 0)
     }
 
     private static func stopReason(_ reason: StopReason) -> AppStopReason {
@@ -457,6 +515,46 @@ actor RealInferenceSession {
         }
     }
 
+    private static func appTokenEvents(
+        _ events: [StructuredAssistantEvent],
+        index: Int,
+        elapsedDecodeSeconds: Double
+    ) throws -> [AppTokenEvent] {
+        var result: [AppTokenEvent] = []
+        for event in events {
+            switch event {
+            case .content(let delta):
+                result.append(AppTokenEvent(
+                    index: index,
+                    textDelta: delta,
+                    elapsedDecodeSeconds: elapsedDecodeSeconds,
+                    channel: .content))
+            case .reasoningStarted:
+                result.append(AppTokenEvent(
+                    index: index,
+                    textDelta: "",
+                    elapsedDecodeSeconds: elapsedDecodeSeconds,
+                    channel: .reasoningStarted))
+            case .reasoning(let delta):
+                result.append(AppTokenEvent(
+                    index: index,
+                    textDelta: delta,
+                    elapsedDecodeSeconds: elapsedDecodeSeconds,
+                    channel: .reasoning))
+            case .reasoningEnded:
+                result.append(AppTokenEvent(
+                    index: index,
+                    textDelta: "",
+                    elapsedDecodeSeconds: elapsedDecodeSeconds,
+                    channel: .reasoningEnded))
+            case .toolCall:
+                throw AppInferenceError.invalidRequest(
+                    "tool calls are not supported by the Mac chat interface")
+            }
+        }
+        return result
+    }
+
     internal static func prefillFailureDiagnostics(config: PrefillRuntimeConfig,
                                                    kvStorageMode: PrefillKVStorageMode,
                                                    reason: String) -> PrefillExecutionDiagnostics {
diff --git a/Sources/TurboFieldfareApp/Core/Installation/AppModelCatalog.swift b/Sources/TurboFieldfareApp/Core/Installation/AppModelCatalog.swift
new file mode 100644
index 0000000..961aac8
--- /dev/null
+++ b/Sources/TurboFieldfareApp/Core/Installation/AppModelCatalog.swift
@@ -0,0 +1,658 @@
+import CryptoKit
+import Foundation
+import TurboFieldfare
+
+public enum AppHubCompatibility: String, Sendable, Equatable, CaseIterable {
+    case verified
+    case compatible
+    case needsValidation
+
+    public var label: String {
+        switch self {
+        case .verified: return "Verified"
+        case .compatible: return "Compatible"
+        case .needsValidation: return "Needs validation"
+        }
+    }
+}
+
+public struct AppInstalledModel: Identifiable, Sendable, Equatable {
+    public let id: String
+    public let displayName: String
+    public let family: ModelFamily
+    public let directory: URL
+    public let allocatedBytes: UInt64
+    public let descriptor: AppModelInstallDescriptor
+}
+
+public struct AppHubModel: Identifiable, Sendable, Equatable {
+    public let id: String
+    public let displayName: String
+    public let family: ModelFamily
+    public let revision: String
+    public let downloads: Int
+    public let lastModified: String?
+    public let hubStorageBytes: UInt64?
+    public let compatibility: AppHubCompatibility
+    public let compatibilityDetail: String
+    public let installDescriptor: AppModelInstallDescriptor?
+
+    public var canInstall: Bool { installDescriptor != nil }
+}
+
+public struct AppModelCatalogSnapshot: Sendable, Equatable {
+    public let installed: [AppInstalledModel]
+    public let available: [AppHubModel]
+    public let refreshedAt: Date
+    public let hubQuery: String?
+    public let hubWarning: String?
+}
+
+public struct AppModelCatalogClient: Sendable {
+    private let session: URLSession
+    private let token: String?
+
+    public init(
+        session: URLSession = .shared,
+        token: String? = ProcessInfo.processInfo.environment["HF_TOKEN"]
+    ) {
+        self.session = session
+        self.token = token
+    }
+
+    public func refresh(search: String? = nil) async -> AppModelCatalogSnapshot {
+        let query = search?.trimmingCharacters(in: .whitespacesAndNewlines)
+        let normalizedQuery = query?.isEmpty == false ? query : nil
+        async let installed = scanInstalled()
+        do {
+            let available = try await discoverAvailable(search: normalizedQuery)
+            let installedRows = await installed
+            return AppModelCatalogSnapshot(
+                installed: installedRows,
+                available: available,
+                refreshedAt: Date(),
+                hubQuery: normalizedQuery,
+                hubWarning: nil)
+        } catch {
+            let installedRows = await installed
+            let fallbacks = Self.verifiedFallbacks.filter {
+                Self.matchesSearch($0, search: normalizedQuery)
+            }
+            return AppModelCatalogSnapshot(
+                installed: installedRows,
+                available: fallbacks,
+                refreshedAt: Date(),
+                hubQuery: normalizedQuery,
+                hubWarning: "Hugging Face could not be refreshed: \(error.localizedDescription)")
+        }
+    }
+
+    private func scanInstalled() -> [AppInstalledModel] {
+        let root = AppModelLocation.defaultURL().deletingLastPathComponent()
+        let keys: Set<URLResourceKey> = [.isDirectoryKey]
+        guard let entries = try? FileManager.default.contentsOfDirectory(
+            at: root,
+            includingPropertiesForKeys: Array(keys),
+            options: [.skipsHiddenFiles]) else { return [] }
+
+        return entries.compactMap { directory in
+            guard directory.pathExtension == "gturbo",
+                  AppModelInstallationProbe.status(at: directory) == .complete,
+                  let family = try? ManifestReader.peekFamily(directoryURL: directory)
+            else { return nil }
+            guard let baseline = AppModelInstallDescriptor.descriptor(for: family) else {
+                return nil
+            }
+            let receipt = Self.readReceipt(directory)
+            let receiptRevision = receipt?.sourceRevision.flatMap {
+                $0.range(of: "^[0-9a-f]{40}$", options: .regularExpression) == nil ? nil : $0
+            }
+            let repoID = receipt?.sourceRepoID ?? baseline.repoID
+            guard repoID == baseline.repoID || receiptRevision != nil else { return nil }
+            let revision = receiptRevision ?? baseline.revision
+            let displayName = repoID == baseline.repoID
+                ? baseline.displayName
+                : Self.displayName(repoID)
+            let descriptor = AppModelInstallDescriptor(
+                family: family,
+                displayName: displayName,
+                repoID: repoID,
+                revision: revision,
+                sourceIndexSHA256: baseline.sourceIndexSHA256,
+                approximateDownloadBytes: baseline.approximateDownloadBytes,
+                installedBytes: baseline.installedBytes,
+                rangeStagingBytes: baseline.rangeStagingBytes,
+                reserveBytes: baseline.reserveBytes)
+            return AppInstalledModel(
+                id: directory.standardizedFileURL.path,
+                displayName: descriptor.displayName,
+                family: family,
+                directory: directory.standardizedFileURL,
+                allocatedBytes: Self.directoryAllocatedBytes(directory),
+                descriptor: descriptor)
+        }
+        .sorted { $0.displayName.localizedCaseInsensitiveCompare($1.displayName) == .orderedAscending }
+    }
+
+    private func discoverAvailable(search hubSearch: String?) async throws -> [AppHubModel] {
+        var records: [String: HubModelRecord] = [:]
+        if let hubSearch, let repoID = Self.exactRepoID(from: hubSearch) {
+            let record = try await modelInfo(repoID: repoID)
+            if NativeCapability.couldMatch(record) { records[record.id] = record }
+        } else {
+            let queries = hubSearch.map { [$0] }
+                ?? NativeCapability.allCases.flatMap(\.searchTerms)
+            for query in queries {
+                for record in try await search(query, limit: hubSearch == nil ? 24 : 50)
+                where NativeCapability.couldMatch(record) {
+                    records[record.id] = record
+                }
+            }
+        }
+
+        var rows: [AppHubModel] = []
+        await withTaskGroup(of: AppHubModel?.self) { group in
+            for record in records.values {
+                group.addTask { await classify(record) }
+            }
+            for await row in group {
+                if let row { rows.append(row) }
+            }
+        }
+
+        for fallback in Self.verifiedFallbacks
+        where !rows.contains(where: { $0.id == fallback.id })
+            && Self.matchesSearch(fallback, search: hubSearch) {
+            rows.append(fallback)
+        }
+        return rows.sorted {
+            if $0.compatibility != $1.compatibility {
+                return Self.rank($0.compatibility) < Self.rank($1.compatibility)
+            }
+            if $0.downloads != $1.downloads { return $0.downloads > $1.downloads }
+            return $0.displayName.localizedCaseInsensitiveCompare($1.displayName) == .orderedAscending
+        }
+    }
+
+    private func search(_ query: String, limit: Int) async throws -> [HubModelRecord] {
+        var components = URLComponents(string: "https://huggingface.co/api/models")!
+        components.queryItems = [
+            URLQueryItem(name: "search", value: query),
+            URLQueryItem(name: "sort", value: "downloads"),
+            URLQueryItem(name: "direction", value: "-1"),
+            URLQueryItem(name: "limit", value: String(limit)),
+            URLQueryItem(name: "full", value: "true"),
+            URLQueryItem(name: "config", value: "true"),
+        ]
+        let (data, response) = try await data(from: components.url!)
+        try Self.requireSuccess(response)
+        return try JSONDecoder().decode([HubModelRecord].self, from: data)
+    }
+
+    private func modelInfo(repoID: String) async throws -> HubModelRecord {
+        var components = URLComponents(
+            string: "https://huggingface.co/api/models/\(Self.encodedRepoID(repoID))")!
+        components.queryItems = [
+            URLQueryItem(name: "full", value: "true"),
+            URLQueryItem(name: "config", value: "true"),
+        ]
+        let (payload, response) = try await data(from: components.url!)
+        try Self.requireSuccess(response)
+        return try JSONDecoder().decode(HubModelRecord.self, from: payload)
+    }
+
+    private func classify(_ record: HubModelRecord) async -> AppHubModel? {
+        let exactDescriptor = AppModelInstallDescriptor.supported.first {
+            $0.repoID == record.id && $0.revision == record.sha
+        }
+        let capability: NativeCapability
+        if let exactDescriptor,
+           let exactCapability = NativeCapability.forFamily(exactDescriptor.family) {
+            capability = exactCapability
+        } else if let inlineCapability = NativeCapability.match(record.config) {
+            capability = inlineCapability
+        } else {
+            do {
+                let config = try await fetchRawConfig(record)
+                guard let rawCapability = NativeCapability.match(config) else { return nil }
+                capability = rawCapability
+            } catch {
+                return nil
+            }
+        }
+        let base = exactDescriptor ?? capability.descriptor
+        let fingerprint: String?
+        let exactVerified = exactDescriptor != nil
+        if exactVerified {
+            fingerprint = base.sourceIndexSHA256
+        } else {
+            fingerprint = try? await fetchIndexFingerprint(record)
+        }
+        let knownLayout = exactVerified || fingerprint == base.sourceIndexSHA256
+        let compatibility: AppHubCompatibility = exactVerified
+            ? .verified
+            : knownLayout ? .compatible : .needsValidation
+        let descriptor = knownLayout
+            ? AppModelInstallDescriptor(
+                family: base.family,
+                displayName: Self.displayName(record.id),
+                repoID: record.id,
+                revision: record.sha,
+                sourceIndexSHA256: base.sourceIndexSHA256,
+                approximateDownloadBytes: base.approximateDownloadBytes,
+                installedBytes: base.installedBytes,
+                rangeStagingBytes: base.rangeStagingBytes,
+                reserveBytes: base.reserveBytes)
+            : nil
+        let detail: String
+        switch compatibility {
+        case .verified:
+            detail = "Exact checkpoint admitted by ElasticUMA"
+        case .compatible:
+            detail = "Implemented architecture and admitted tensor fingerprint"
+        case .needsValidation:
+            detail = "Architecture fits, but this tensor fingerprint is not admitted"
+        }
+        return AppHubModel(
+            id: record.id,
+            displayName: Self.displayName(record.id),
+            family: capability.family,
+            revision: record.sha,
+            downloads: record.downloads ?? 0,
+            lastModified: record.lastModified,
+            hubStorageBytes: knownLayout ? await fetchHubStorage(record) : nil,
+            compatibility: compatibility,
+            compatibilityDetail: detail,
+            installDescriptor: descriptor)
+    }
+
+    private func fetchRawConfig(_ record: HubModelRecord) async throws -> HubModelConfig {
+        let url = Self.resolveURL(record, file: "config.json")
+        let (data, response) = try await data(from: url)
+        try Self.requireSuccess(response)
+        return try JSONDecoder().decode(HubModelConfig.self, from: data)
+    }
+
+    private func fetchIndexFingerprint(_ record: HubModelRecord) async throws -> String {
+        let url = Self.resolveURL(record, file: "model.safetensors.index.json")
+        let (data, response) = try await data(from: url)
+        try Self.requireSuccess(response)
+        return SHA256.hash(data: data).map { String(format: "%02x", $0) }.joined()
+    }
+
+    private func fetchHubStorage(_ record: HubModelRecord) async -> UInt64? {
+        if let usedStorage = record.usedStorage { return usedStorage }
+        var components = URLComponents(
+            string: "https://huggingface.co/api/models/\(Self.encodedRepoID(record.id))")!
+        components.queryItems = [URLQueryItem(name: "expand[]", value: "usedStorage")]
+        guard let url = components.url,
+              let (data, response) = try? await data(from: url),
+              (try? Self.requireSuccess(response)) != nil,
+              let info = try? JSONDecoder().decode(HubStorageRecord.self, from: data)
+        else { return nil }
+        return info.usedStorage
+    }
+
+    private func data(from url: URL) async throws -> (Data, URLResponse) {
+        var request = URLRequest(url: url)
+        if let token, !token.isEmpty {
+            request.setValue("Bearer \(token)", forHTTPHeaderField: "Authorization")
+        }
+        return try await session.data(for: request)
+    }
+
+    private static func resolveURL(_ record: HubModelRecord, file: String) -> URL {
+        URL(
+            string: "https://huggingface.co/\(encodedRepoID(record.id))/resolve/"
+                + "\(record.sha)/\(file)")!
+    }
+
+    private static func encodedRepoID(_ repoID: String) -> String {
+        repoID
+            .split(separator: "/")
+            .map {
+                String($0).addingPercentEncoding(withAllowedCharacters: .urlPathAllowed)!
+            }
+            .joined(separator: "/")
+    }
+
+    private static func exactRepoID(from query: String) -> String? {
+        let trimmed = query.trimmingCharacters(in: .whitespacesAndNewlines)
+        if let url = URL(string: trimmed),
+           let host = url.host?.lowercased(),
+           host == "huggingface.co" || host == "www.huggingface.co" {
+            let components = url.path.split(separator: "/")
+            guard components.count >= 2 else { return nil }
+            return components.prefix(2).map(String.init).joined(separator: "/")
+        }
+        let components = trimmed.split(separator: "/", omittingEmptySubsequences: true)
+        guard components.count == 2,
+              components.allSatisfy({ !$0.contains(where: \.isWhitespace) }) else { return nil }
+        return components.map(String.init).joined(separator: "/")
+    }
+
+    private static func requireSuccess(_ response: URLResponse) throws {
+        guard let http = response as? HTTPURLResponse,
+              (200..<300).contains(http.statusCode) else {
+            throw URLError(.badServerResponse)
+        }
+    }
+
+    private static func rank(_ value: AppHubCompatibility) -> Int {
+        switch value {
+        case .verified: return 0
+        case .compatible: return 1
+        case .needsValidation: return 2
+        }
+    }
+
+    private static func displayName(_ repoID: String) -> String {
+        repoID.split(separator: "/").last.map(String.init) ?? repoID
+    }
+
+    private static func matchesSearch(_ model: AppHubModel, search: String?) -> Bool {
+        guard let search, !search.isEmpty else { return true }
+        return model.id.localizedCaseInsensitiveContains(search)
+            || model.displayName.localizedCaseInsensitiveContains(search)
+    }
+
+    private static func directoryAllocatedBytes(_ root: URL) -> UInt64 {
+        guard let enumerator = FileManager.default.enumerator(
+            at: root,
+            includingPropertiesForKeys: [.fileAllocatedSizeKey, .totalFileAllocatedSizeKey],
+            options: [.skipsHiddenFiles]) else { return 0 }
+        var total: UInt64 = 0
+        for case let file as URL in enumerator {
+            guard let values = try? file.resourceValues(forKeys: [
+                .isRegularFileKey,
+                .fileAllocatedSizeKey,
+                .totalFileAllocatedSizeKey,
+            ]), values.isRegularFile == true else { continue }
+            total += UInt64(max(values.totalFileAllocatedSize ?? values.fileAllocatedSize ?? 0, 0))
+        }
+        return total
+    }
+
+    private static func readReceipt(_ directory: URL) -> InstalledReceipt? {
+        let url = directory.appendingPathComponent("verified-install.json")
+        guard let data = try? Data(contentsOf: url) else { return nil }
+        return try? JSONDecoder().decode(InstalledReceipt.self, from: data)
+    }
+
+    private static var verifiedFallbacks: [AppHubModel] {
+        AppModelInstallDescriptor.supported.map { descriptor in
+            AppHubModel(
+                id: descriptor.repoID,
+                displayName: descriptor.displayName,
+                family: descriptor.family,
+                revision: descriptor.revision,
+                downloads: 0,
+                lastModified: nil,
+                hubStorageBytes: nil,
+                compatibility: .verified,
+                compatibilityDetail: "Exact checkpoint admitted by ElasticUMA",
+                installDescriptor: descriptor)
+        }
+    }
+}
+
+private struct InstalledReceipt: Decodable {
+    let sourceRepoID: String?
+    let sourceRevision: String?
+}
+
+private struct HubModelRecord: Decodable, Sendable {
+    let id: String
+    let sha: String
+    let downloads: Int?
+    let lastModified: String?
+    let usedStorage: UInt64?
+    let config: HubModelConfig?
+}
+
+private struct HubStorageRecord: Decodable, Sendable {
+    let usedStorage: UInt64?
+}
+
+private struct HubModelConfig: Decodable, Sendable {
+    let modelType: String?
+    let textConfig: HubTextConfig?
+    let quantization: HubQuantization?
+    let quantizationConfig: HubQuantization?
+
+    enum CodingKeys: String, CodingKey {
+        case modelType = "model_type"
+        case textConfig = "text_config"
+        case quantization
+        case quantizationConfig = "quantization_config"
+    }
+}
+
+private struct HubTextConfig: Decodable, Sendable {
+    let modelType: String?
+    let hiddenSize: Int?
+    let intermediateSize: Int?
+    let sharedExpertIntermediateSize: Int?
+    let numHiddenLayers: Int?
+    let numAttentionHeads: Int?
+    let numKeyValueHeads: Int?
+    let numGlobalKeyValueHeads: Int?
+    let headDim: Int?
+    let globalHeadDim: Int?
+    let numExperts: Int?
+    let numExpertsPerTok: Int?
+    let topKExperts: Int?
+    let moeIntermediateSize: Int?
+    let vocabSize: Int?
+    let slidingWindow: Int?
+    let finalLogitSoftcapping: Double?
+    let layerTypes: [String]?
+    let ropeParameters: HubRopeParameters?
+    let tieWordEmbeddings: Bool?
+    let attentionKEqV: Bool?
+    let hiddenActivation: String?
+    let hiddenAct: String?
+    let attentionOutputGate: Bool?
+    let linearNumKeyHeads: Int?
+    let linearNumValueHeads: Int?
+    let linearKeyHeadDim: Int?
+    let linearValueHeadDim: Int?
+    let linearConvKernelDim: Int?
+
+    enum CodingKeys: String, CodingKey {
+        case modelType = "model_type"
+        case hiddenSize = "hidden_size"
+        case intermediateSize = "intermediate_size"
+        case sharedExpertIntermediateSize = "shared_expert_intermediate_size"
+        case numHiddenLayers = "num_hidden_layers"
+        case numAttentionHeads = "num_attention_heads"
+        case numKeyValueHeads = "num_key_value_heads"
+        case numGlobalKeyValueHeads = "num_global_key_value_heads"
+        case headDim = "head_dim"
+        case globalHeadDim = "global_head_dim"
+        case numExperts = "num_experts"
+        case numExpertsPerTok = "num_experts_per_tok"
+        case topKExperts = "top_k_experts"
+        case moeIntermediateSize = "moe_intermediate_size"
+        case vocabSize = "vocab_size"
+        case slidingWindow = "sliding_window"
+        case finalLogitSoftcapping = "final_logit_softcapping"
+        case layerTypes = "layer_types"
+        case ropeParameters = "rope_parameters"
+        case tieWordEmbeddings = "tie_word_embeddings"
+        case attentionKEqV = "attention_k_eq_v"
+        case hiddenActivation = "hidden_activation"
+        case hiddenAct = "hidden_act"
+        case attentionOutputGate = "attn_output_gate"
+        case linearNumKeyHeads = "linear_num_key_heads"
+        case linearNumValueHeads = "linear_num_value_heads"
+        case linearKeyHeadDim = "linear_key_head_dim"
+        case linearValueHeadDim = "linear_value_head_dim"
+        case linearConvKernelDim = "linear_conv_kernel_dim"
+    }
+}
+
+private struct HubRopeParameters: Decodable, Sendable {
+    let ropeTheta: Double?
+    let partialRotaryFactor: Double?
+    let fullAttention: HubRopeLeaf?
+    let slidingAttention: HubRopeLeaf?
+
+    enum CodingKeys: String, CodingKey {
+        case ropeTheta = "rope_theta"
+        case partialRotaryFactor = "partial_rotary_factor"
+        case fullAttention = "full_attention"
+        case slidingAttention = "sliding_attention"
+    }
+}
+
+private struct HubRopeLeaf: Decodable, Sendable {
+    let ropeTheta: Double?
+    let partialRotaryFactor: Double?
+
+    enum CodingKeys: String, CodingKey {
+        case ropeTheta = "rope_theta"
+        case partialRotaryFactor = "partial_rotary_factor"
+    }
+}
+
+private struct HubQuantization: Decodable, Sendable {
+    let groupSize: Int?
+    let bits: Int?
+    let mode: String?
+    let quantMethod: String?
+
+    enum CodingKeys: String, CodingKey {
+        case groupSize = "group_size"
+        case bits
+        case mode
+        case quantMethod = "quant_method"
+    }
+}
+
+private enum NativeCapability: CaseIterable {
+    case qwen36
+    case gemma4
+
+    var descriptor: AppModelInstallDescriptor {
+        switch self {
+        case .qwen36: return .qwen36
+        case .gemma4: return .default
+        }
+    }
+
+    var family: ModelFamily { descriptor.family }
+
+    var searchTerms: [String] {
+        switch self {
+        case .qwen36:
+            return ["Qwen3.6-35B-A3B-4bit", "Qwen3.6-35B-A3B"]
+        case .gemma4:
+            return ["gemma-4-26b-a4b-it-4bit", "gemma-4-26b-a4b"]
+        }
+    }
+
+    static func forFamily(_ family: ModelFamily) -> NativeCapability? {
+        allCases.first { $0.family == family }
+    }
+
+    static func couldMatch(_ record: HubModelRecord) -> Bool {
+        guard record.sha.range(
+            of: "^[0-9a-f]{40}$", options: .regularExpression) != nil else { return false }
+        if AppModelInstallDescriptor.supported.contains(where: {
+            $0.repoID == record.id && $0.revision == record.sha
+        }) { return true }
+
+        let normalizedID = record.id.lowercased()
+        let looksFourBit = normalizedID.contains("4bit") || normalizedID.contains("4-bit")
+        let quantization = record.config?.quantization ?? record.config?.quantizationConfig
+        if let method = quantization?.quantMethod?.lowercased(), method.contains("fp8") {
+            return false
+        }
+        if let bits = quantization?.bits, bits != 4 { return false }
+        let modelType = record.config?.modelType
+        let knownType = modelType == "qwen3_5_moe" || modelType == "gemma4"
+        return knownType && (quantization?.bits == 4 || looksFourBit)
+    }
+
+    static func match(_ config: HubModelConfig?) -> NativeCapability? {
+        guard let config,
+              let text = config.textConfig,
+              let quantization = config.quantization ?? config.quantizationConfig,
+              quantization.bits == 4,
+              quantization.groupSize == 64,
+              quantization.mode == "affine" else { return nil }
+
+        if config.modelType == "qwen3_5_moe", matchesQwen36(text) {
+            return .qwen36
+        }
+        if config.modelType == "gemma4", matchesGemma4(text) {
+            return .gemma4
+        }
+        return nil
+    }
+
+    private static func matchesQwen36(_ text: HubTextConfig) -> Bool {
+        guard text.modelType == "qwen3_5_moe_text",
+              text.hiddenSize == 2_048,
+              text.sharedExpertIntermediateSize == 512,
+              text.moeIntermediateSize == 512,
+              text.numHiddenLayers == 40,
+              text.numAttentionHeads == 16,
+              text.numKeyValueHeads == 2,
+              text.headDim == 256,
+              text.numExperts == 256,
+              text.numExpertsPerTok == 8,
+              text.vocabSize == 248_320,
+              text.tieWordEmbeddings == false,
+              text.attentionOutputGate == true,
+              text.hiddenAct == "silu",
+              text.linearNumKeyHeads == 16,
+              text.linearNumValueHeads == 32,
+              text.linearKeyHeadDim == 128,
+              text.linearValueHeadDim == 128,
+              text.linearConvKernelDim == 4,
+              approximately(text.ropeParameters?.ropeTheta, 10_000_000),
+              approximately(text.ropeParameters?.partialRotaryFactor, 0.25),
+              let layers = text.layerTypes,
+              layers.count == 40 else { return false }
+        return layers.enumerated().allSatisfy { index, value in
+            value == (index % 4 == 3 ? "full_attention" : "linear_attention")
+        }
+    }
+
+    private static func matchesGemma4(_ text: HubTextConfig) -> Bool {
+        guard text.modelType == "gemma4_text",
+              text.hiddenSize == 2_816,
+              text.intermediateSize == 2_112,
+              text.moeIntermediateSize == 704,
+              text.numHiddenLayers == 30,
+              text.numAttentionHeads == 16,
+              text.numKeyValueHeads == 8,
+              text.numGlobalKeyValueHeads == 2,
+              text.headDim == 256,
+              text.globalHeadDim == 512,
+              text.numExperts == 128,
+              text.topKExperts == 8,
+              text.vocabSize == 262_144,
+              text.slidingWindow == 1_024,
+              approximately(text.finalLogitSoftcapping, 30),
+              text.tieWordEmbeddings == true,
+              text.attentionKEqV == true,
+              text.hiddenActivation == "gelu_pytorch_tanh",
+              approximately(text.ropeParameters?.fullAttention?.ropeTheta, 1_000_000),
+              approximately(
+                text.ropeParameters?.fullAttention?.partialRotaryFactor, 0.25),
+              approximately(text.ropeParameters?.slidingAttention?.ropeTheta, 10_000),
+              let layers = text.layerTypes,
+              layers.count == 30 else { return false }
+        return layers.enumerated().allSatisfy { index, value in
+            value == (index % 6 == 5 ? "full_attention" : "sliding_attention")
+        }
+    }
+
+    private static func approximately(_ value: Double?, _ expected: Double) -> Bool {
+        guard let value else { return false }
+        return abs(value - expected) <= max(abs(expected) * 1e-12, 1e-12)
+    }
+}
diff --git a/Sources/TurboFieldfareApp/Core/Installation/AppModelInstallDescriptor.swift b/Sources/TurboFieldfareApp/Core/Installation/AppModelInstallDescriptor.swift
index 2e533b4..51caa14 100644
--- a/Sources/TurboFieldfareApp/Core/Installation/AppModelInstallDescriptor.swift
+++ b/Sources/TurboFieldfareApp/Core/Installation/AppModelInstallDescriptor.swift
@@ -2,7 +2,39 @@ import Foundation
 import TurboFieldfare
 import TurboFieldfareRepackCore
 
+public enum AppModelInputModality: String, CaseIterable, Sendable, Hashable {
+    case text
+    case image
+    case audio
+    case video
+
+    public var label: String { rawValue.capitalized }
+}
+
+public struct AppModelRuntimeCapabilities: Equatable, Sendable {
+    public let inputModalities: Set<AppModelInputModality>
+    public let detail: String
+
+    public init(inputModalities: Set<AppModelInputModality>, detail: String) {
+        self.inputModalities = inputModalities
+        self.detail = detail
+    }
+
+    public func supports(_ modality: AppModelInputModality) -> Bool {
+        inputModalities.contains(modality)
+    }
+
+    public var inputSummary: String {
+        if inputModalities == [.text] { return "Text only" }
+        return AppModelInputModality.allCases
+            .filter(inputModalities.contains)
+            .map(\.label)
+            .joined(separator: " · ")
+    }
+}
+
 public struct AppModelInstallDescriptor: Equatable, Sendable {
+    public let family: ModelFamily
     public let displayName: String
     public let repoID: String
     public let revision: String
@@ -12,7 +44,8 @@ public struct AppModelInstallDescriptor: Equatable, Sendable {
     public let rangeStagingBytes: UInt64
     public let reserveBytes: UInt64
 
-    public init(displayName: String,
+    public init(family: ModelFamily = .gemma4,
+                displayName: String,
                 repoID: String,
                 revision: String,
                 sourceIndexSHA256: String,
@@ -20,6 +53,7 @@ public struct AppModelInstallDescriptor: Equatable, Sendable {
                 installedBytes: UInt64,
                 rangeStagingBytes: UInt64,
                 reserveBytes: UInt64) {
+        self.family = family
         self.displayName = displayName
         self.repoID = repoID
         self.revision = revision
@@ -34,8 +68,36 @@ public struct AppModelInstallDescriptor: Equatable, Sendable {
         installedBytes + rangeStagingBytes + reserveBytes
     }
 
+    public var id: String {
+        switch family {
+        case .qwen36: return "qwen36"
+        case .gemma4: return "gemma4"
+        }
+    }
+
+    /// Stable identity for one immutable Hugging Face snapshot. Unlike `id`,
+    /// this distinguishes compatible mirrors and revisions within a family.
+    public var selectionKey: String { "\(repoID)@\(revision)" }
+
+    /// End-to-end modalities implemented by ElasticUMA for this model
+    /// profile. This intentionally describes the runtime, not every modality
+    /// present in the upstream checkpoint.
+    public var runtimeCapabilities: AppModelRuntimeCapabilities {
+        switch family {
+        case .qwen36:
+            AppModelRuntimeCapabilities(
+                inputModalities: [.text],
+                detail: "Text only in ElasticUMA; the upstream vision tower is not installed.")
+        case .gemma4:
+            AppModelRuntimeCapabilities(
+                inputModalities: [.text],
+                detail: "Text only in ElasticUMA; the upstream vision tower is not installed.")
+        }
+    }
+
     public static let `default` = AppModelInstallDescriptor(
-        displayName: "Gemma 4 26B-A4B IT 4-bit",
+        family: .gemma4,
+        displayName: "Gemma 4 26B-A4B · 4-bit",
         repoID: "mlx-community/gemma-4-26b-a4b-it-4bit",
         revision: "0d77464eeb233a2da68ebf9d7dc4edaac7db956d",
         sourceIndexSHA256: "bf198c9f5ea6462addca1966e5dd669c407537a876e82cf06db9084c5c850b13",
@@ -45,15 +107,26 @@ public struct AppModelInstallDescriptor: Equatable, Sendable {
         reserveBytes: 1_073_741_824)
 
     public static let qwen36 = AppModelInstallDescriptor(
-        displayName: "Qwen3.6 35B-A3B 4-bit",
+        family: .qwen36,
+        displayName: "Qwen3.6-35B-A3B · 4-bit",
         repoID: "mlx-community/Qwen3.6-35B-A3B-4bit",
         revision: "38740b847e4cb78f352aba30aa41c76e08e6eb46",
         sourceIndexSHA256: "0b28df60e33753a14e816d3b31577ae2c93884c58430a4a6de6ae9ea483842ea",
-        approximateDownloadBytes: 19_529_025_048,
+        approximateDownloadBytes: 20_146_183_200,
         installedBytes: 19_546_491_213,
         rangeStagingBytes: UInt64(RemoteChunkPolicy.defaultBytes),
         reserveBytes: 1_073_741_824)
 
+    public static let supported: [AppModelInstallDescriptor] = [.qwen36, .default]
+
+    /// Built-in verified checkpoints plus the current dynamically discovered
+    /// checkpoint when it is a compatible mirror rather than a built-in pin.
+    public static func selectionChoices(
+        including current: AppModelInstallDescriptor
+    ) -> [AppModelInstallDescriptor] {
+        supported.contains(current) ? supported : [current] + supported
+    }
+
     /// The shipped descriptor for a model family, if one exists.
     public static func descriptor(for family: ModelFamily) -> AppModelInstallDescriptor? {
         switch family {
@@ -64,20 +137,75 @@ public struct AppModelInstallDescriptor: Equatable, Sendable {
 
     /// Basename of the installed `.gturbo` directory for this descriptor.
     public var installDirectoryName: String {
-        self == .qwen36 ? "qwen36.gturbo" : "gemma4.gturbo"
+        "\(id)-\(revision).gturbo"
     }
 
-    /// The descriptor the app products select at launch. Defaults to Gemma 4.
-    /// `TURBO_FIELDFARE_MODEL=qwen36` in the environment wins; otherwise the
-    /// persisted preference (`defaults write TurboFieldfare model qwen36`)
-    /// applies, so GUI launches without an environment also select Qwen.
+    /// The descriptor the ElasticUMA app selects at launch. Qwen is the default
+    /// because it is the primary admitted profile in the public catalog.
     public static var selected: AppModelInstallDescriptor {
-        let environmentValue = ProcessInfo.processInfo.environment["TURBO_FIELDFARE_MODEL"]
-        let preferenceValue = UserDefaults(suiteName: "TurboFieldfare")?
-            .string(forKey: "model")
+        let environment = ProcessInfo.processInfo.environment
+        let environmentValue = environment["ELASTICUMA_MODEL"]
+            ?? environment["TURBO_FIELDFARE_MODEL"]
+        let defaults = UserDefaults(suiteName: "ElasticUMA")
+        if environmentValue == nil,
+           let data = defaults?.data(forKey: "selectedModelDescriptor"),
+           let persisted = try? JSONDecoder().decode(PersistedDescriptor.self, from: data),
+           let descriptor = persisted.descriptor {
+            return descriptor
+        }
+        let preferenceValue = defaults?.string(forKey: "model")
+            ?? UserDefaults(suiteName: "TurboFieldfare")?.string(forKey: "model")
         switch environmentValue ?? preferenceValue {
-        case "qwen36": return .qwen36
-        default: return .default
+        case "gemma4": return .default
+        default: return .qwen36
+        }
+    }
+
+    public func persistSelection() {
+        let defaults = UserDefaults(suiteName: "ElasticUMA")
+        defaults?.set(id, forKey: "model")
+        if let data = try? JSONEncoder().encode(PersistedDescriptor(self)) {
+            defaults?.set(data, forKey: "selectedModelDescriptor")
+        }
+    }
+
+    private struct PersistedDescriptor: Codable {
+        let family: String
+        let displayName: String
+        let repoID: String
+        let revision: String
+        let sourceIndexSHA256: String
+        let approximateDownloadBytes: UInt64
+        let installedBytes: UInt64
+        let rangeStagingBytes: UInt64
+        let reserveBytes: UInt64
+
+        init(_ descriptor: AppModelInstallDescriptor) {
+            family = descriptor.family.rawValue
+            displayName = descriptor.displayName
+            repoID = descriptor.repoID
+            revision = descriptor.revision
+            sourceIndexSHA256 = descriptor.sourceIndexSHA256
+            approximateDownloadBytes = descriptor.approximateDownloadBytes
+            installedBytes = descriptor.installedBytes
+            rangeStagingBytes = descriptor.rangeStagingBytes
+            reserveBytes = descriptor.reserveBytes
+        }
+
+        var descriptor: AppModelInstallDescriptor? {
+            guard let family = ModelFamily(rawValue: family),
+                  AppModelInstallDescriptor.descriptor(for: family)?
+                    .sourceIndexSHA256 == sourceIndexSHA256 else { return nil }
+            return AppModelInstallDescriptor(
+                family: family,
+                displayName: displayName,
+                repoID: repoID,
+                revision: revision,
+                sourceIndexSHA256: sourceIndexSHA256,
+                approximateDownloadBytes: approximateDownloadBytes,
+                installedBytes: installedBytes,
+                rangeStagingBytes: rangeStagingBytes,
+                reserveBytes: reserveBytes)
         }
     }
 }
diff --git a/Sources/TurboFieldfareApp/Core/Installation/AppModelLocation.swift b/Sources/TurboFieldfareApp/Core/Installation/AppModelLocation.swift
index c605eac..82de3bc 100644
--- a/Sources/TurboFieldfareApp/Core/Installation/AppModelLocation.swift
+++ b/Sources/TurboFieldfareApp/Core/Installation/AppModelLocation.swift
@@ -1,46 +1,39 @@
 import Foundation
 
-enum AppModelLocation {
-    static func defaultURL(
+public enum AppModelLocation {
+    public static func defaultURL(
         descriptor: AppModelInstallDescriptor = .selected
     ) -> URL {
         let fileManager = FileManager.default
-        let applicationSupport = (try? fileManager.url(
-            for: .applicationSupportDirectory,
-            in: .userDomainMask,
-            appropriateFor: nil,
-            create: false)) ?? fileManager.homeDirectoryForCurrentUser
+        let cacheRoot: URL
+        if let configured = ProcessInfo.processInfo.environment["ELASTICUMA_CACHE_ROOT"],
+           !configured.isEmpty {
+            cacheRoot = URL(fileURLWithPath: configured, isDirectory: true)
+        } else {
+            let caches = (try? fileManager.url(
+                for: .cachesDirectory,
+                in: .userDomainMask,
+                appropriateFor: nil,
+                create: false)) ?? fileManager.homeDirectoryForCurrentUser
+            cacheRoot = caches.appendingPathComponent("elasticuma", isDirectory: true)
+        }
         return resolve(
             explicitURL: nil,
-            executableURL: Bundle.main.executableURL,
             currentDirectoryURL: URL(fileURLWithPath: fileManager.currentDirectoryPath,
                                      isDirectory: true),
-            applicationSupportURL: applicationSupport,
-            fileExists: fileManager.fileExists(atPath:),
+            cacheRootURL: cacheRoot,
             installDirectoryName: descriptor.installDirectoryName)
     }
 
     static func resolve(explicitURL: URL?,
-                        executableURL: URL?,
                         currentDirectoryURL: URL,
-                        applicationSupportURL: URL,
-                        fileExists: (String) -> Bool,
-                        installDirectoryName: String = "gemma4.gturbo") -> URL {
+                        cacheRootURL: URL,
+                        installDirectoryName: String) -> URL {
         if let explicitURL {
             return absoluteURL(explicitURL, relativeTo: currentDirectoryURL)
         }
-        if let executableURL,
-           let root = packageRoot(startingAt: executableURL.deletingLastPathComponent(),
-                                  fileExists: fileExists) {
-            return root.appendingPathComponent("scratch/\(installDirectoryName)", isDirectory: true)
-                .standardizedFileURL
-        }
-        if let root = packageRoot(startingAt: currentDirectoryURL, fileExists: fileExists) {
-            return root.appendingPathComponent("scratch/\(installDirectoryName)", isDirectory: true)
-                .standardizedFileURL
-        }
-        return applicationSupportURL
-            .appendingPathComponent("TurboFieldfare", isDirectory: true)
+        return cacheRootURL
+            .appendingPathComponent("packed", isDirectory: true)
             .appendingPathComponent(installDirectoryName, isDirectory: true)
             .standardizedFileURL
     }
@@ -52,20 +45,4 @@ enum AppModelLocation {
         return base.appendingPathComponent(url.path, isDirectory: true).standardizedFileURL
     }
 
-    private static func packageRoot(startingAt start: URL,
-                                    fileExists: (String) -> Bool) -> URL? {
-        var candidatePath = start.standardizedFileURL.path
-        while true {
-            let candidate = URL(fileURLWithPath: candidatePath, isDirectory: true)
-            let package = candidate.appendingPathComponent("Package.swift").path
-            let appSources = candidate.appendingPathComponent(
-                "Sources/TurboFieldfareApp/Mac", isDirectory: true).path
-            if fileExists(package), fileExists(appSources) {
-                return candidate
-            }
-            let parentPath = (candidatePath as NSString).deletingLastPathComponent
-            if parentPath.isEmpty || parentPath == candidatePath { return nil }
-            candidatePath = parentPath
-        }
-    }
 }
diff --git a/Sources/TurboFieldfareApp/Core/State/AppModel.swift b/Sources/TurboFieldfareApp/Core/State/AppModel.swift
index 9e3d280..c90566d 100644
--- a/Sources/TurboFieldfareApp/Core/State/AppModel.swift
+++ b/Sources/TurboFieldfareApp/Core/State/AppModel.swift
@@ -2,6 +2,18 @@ import Foundation
 import TurboFieldfareRepackCore
 import Observation
 
+public struct AppThinkingBlock: Identifiable, Equatable, Sendable {
+    public let id: Int
+    public var text: String
+    public var isComplete: Bool
+
+    public init(id: Int, text: String = "", isComplete: Bool = false) {
+        self.id = id
+        self.text = text
+        self.isComplete = isComplete
+    }
+}
+
 @MainActor
 @Observable
 public final class AppModel {
@@ -23,6 +35,7 @@ public final class AppModel {
     public var topK: Int = 64
     public var topPEnabled: Bool = true
     public var topP: Double = 0.95
+    public var thinkingEnabled: Bool = false
     public var diagnostics: AppDiagnostics?
     public var error: AppInferenceError?
     public var installState: AppModelInstallState = .idle
@@ -40,6 +53,8 @@ public final class AppModel {
     public private(set) var livePrefillTotal: Int = 0
     public private(set) var liveMemoryBytes: UInt64?
     public private(set) var isCancellationPending: Bool = false
+    public private(set) var thinkingBlocks: [AppThinkingBlock] = []
+    public private(set) var isThinking: Bool = false
 
     private let client: any AppInferenceClient
     private let installer: any AppModelInstallerClient
@@ -53,6 +68,7 @@ public final class AppModel {
     private var pendingExplicitLoadRuntimeKey: AppLoadedRuntimeKey?
     private var activeRunRuntimeKey: AppLoadedRuntimeKey?
     private var hasHandledTerminalEvent = false
+    private var nextThinkingBlockID = 0
     private let memorySampler: AppMemorySampler
     private let settingsPersistenceEnabled: Bool
     private let installETAClock: SuspendingClock
@@ -63,8 +79,10 @@ public final class AppModel {
                 client: any AppInferenceClient = RealInferenceClient(),
                 installer: any AppModelInstallerClient = RepackModelInstallerClient(descriptor: .selected),
                 memorySampler: AppMemorySampler = AppMemorySampler(),
-                settingsPersistenceEnabled: Bool = false) {
-        let directory = (modelDirectory ?? AppModelLocation.defaultURL()).standardizedFileURL
+                settingsPersistenceEnabled: Bool = false,
+                installationStatusOverride: AppModelInstallationStatus? = nil) {
+        let directory = (modelDirectory
+            ?? AppModelLocation.defaultURL(descriptor: installer.descriptor)).standardizedFileURL
         let installETAClock = SuspendingClock()
         let settings = settingsPersistenceEnabled
             ? MacAppSettingsFileStore.loadOrCreate(forModelDirectory: directory)
@@ -72,21 +90,27 @@ public final class AppModel {
         self.modelPathText = directory.path
         self.runtimeOptions = AppRuntimeOptions(
             expertCacheSlots: settings.expertCacheSlots,
-            prefillEnabled: settings.prefillEnabled)
+            prefillEnabled: settings.prefillEnabled,
+            expertCacheResidency: settings.expertCacheResidency,
+            expertCacheHotSlots: settings.expertCacheHotSlots)
         self.maxContextTokens = settings.contextTokens
         self.temperature = settings.temperature
         self.topKEnabled = settings.topKEnabled
         self.topK = settings.topK
         self.topPEnabled = settings.topPEnabled
         self.topP = settings.topP
-        self.installationStatus = AppModelInstallationProbe.status(at: directory)
+        self.thinkingEnabled = settings.thinkingEnabled
+        self.installationStatus = installationStatusOverride
+            ?? AppModelInstallationProbe.status(at: directory)
         self.client = client
         self.installer = installer
         self.memorySampler = memorySampler
         self.settingsPersistenceEnabled = settingsPersistenceEnabled
         self.installETAClock = installETAClock
         self.installETAOrigin = installETAClock.now
-        refreshInstallReadiness()
+        if installationStatusOverride == nil {
+            refreshInstallReadiness()
+        }
     }
 
     public var isRunning: Bool { runState == .running }
@@ -605,13 +629,16 @@ public final class AppModel {
             forModelDirectory: modelDirectory)
         runtimeOptions = AppRuntimeOptions(
             expertCacheSlots: settings.expertCacheSlots,
-            prefillEnabled: settings.prefillEnabled)
+            prefillEnabled: settings.prefillEnabled,
+            expertCacheResidency: settings.expertCacheResidency,
+            expertCacheHotSlots: settings.expertCacheHotSlots)
         maxContextTokens = settings.contextTokens
         temperature = settings.temperature
         topKEnabled = settings.topKEnabled
         topK = settings.topK
         topPEnabled = settings.topPEnabled
         topP = settings.topP
+        thinkingEnabled = settings.thinkingEnabled
     }
 
     private func persistSettings() {
@@ -624,7 +651,10 @@ public final class AppModel {
             topK: topK,
             topPEnabled: topPEnabled,
             topP: topP,
-            prefillEnabled: runtimeOptions.prefillEnabled)
+            thinkingEnabled: thinkingEnabled,
+            prefillEnabled: runtimeOptions.prefillEnabled,
+            expertCacheResidency: runtimeOptions.expertCacheResidency,
+            expertCacheHotSlots: runtimeOptions.expertCacheHotSlots)
         let modelDirectory = URL(fileURLWithPath: modelPathText, isDirectory: true)
         try? MacAppSettingsFileStore.save(
             settings,
@@ -684,6 +714,9 @@ public final class AppModel {
         guard !isRunning else { return }
         outputPromptText = ""
         outputText = ""
+        thinkingBlocks = []
+        isThinking = false
+        nextThinkingBlockID = 0
         generationTranscriptMailbox?.reset()
         diagnostics = nil
         error = nil
@@ -706,7 +739,11 @@ public final class AppModel {
 
         generationTranscriptMailbox?.reset()
         outputPromptText = request.prompt
+        promptText = ""
         outputText = ""
+        thinkingBlocks = []
+        isThinking = false
+        nextThinkingBlockID = 0
         diagnostics = nil
         error = nil
         hasHandledTerminalEvent = false
@@ -754,6 +791,7 @@ public final class AppModel {
             topK: topKEnabled ? topK : nil,
             topP: topKEnabled && topPEnabled ? Float(topP) : nil,
             repetitionPenalty: 1.0,
+            thinkingEnabled: thinkingEnabled,
             runtimeOptions: runtimeOptions)
         try request.validate(requireModelDirectory: true)
         return request
@@ -774,8 +812,16 @@ public final class AppModel {
             } else {
                 liveMemoryBytes = memorySampler.sample()
             }
-            if !token.textDelta.isEmpty {
-                outputText += token.textDelta
+            switch token.channel {
+            case .content:
+                if isThinking { endThinkingBlock() }
+                if !token.textDelta.isEmpty { outputText += token.textDelta }
+            case .reasoningStarted:
+                startThinkingBlock()
+            case .reasoning:
+                appendThinking(token.textDelta)
+            case .reasoningEnded:
+                endThinkingBlock()
             }
         case .finished(let diagnostics):
             finishSuccessfully(diagnostics)
@@ -791,6 +837,7 @@ public final class AppModel {
     private func finishSuccessfully(_ diagnostics: AppDiagnostics) {
         guard !hasHandledTerminalEvent else { return }
         hasHandledTerminalEvent = true
+        endThinkingBlock()
         materializeServiceTranscript()
         self.diagnostics = diagnostics
         finishTerminalRun()
@@ -799,6 +846,7 @@ public final class AppModel {
     private func finishCancelled(_ diagnostics: AppDiagnostics) {
         guard !hasHandledTerminalEvent else { return }
         hasHandledTerminalEvent = true
+        endThinkingBlock()
         materializeServiceTranscript()
         self.diagnostics = diagnostics
         error = .cancelled
@@ -813,10 +861,33 @@ public final class AppModel {
     private func finishWithError(_ appError: AppInferenceError) {
         guard !hasHandledTerminalEvent else { return }
         hasHandledTerminalEvent = true
+        endThinkingBlock()
         error = appError
         finishTerminalRun()
     }
 
+    private func startThinkingBlock() {
+        if isThinking { endThinkingBlock() }
+        thinkingBlocks.append(AppThinkingBlock(id: nextThinkingBlockID))
+        nextThinkingBlockID += 1
+        isThinking = true
+    }
+
+    private func appendThinking(_ delta: String) {
+        guard !delta.isEmpty else { return }
+        if !isThinking { startThinkingBlock() }
+        guard let index = thinkingBlocks.indices.last else { return }
+        thinkingBlocks[index].text += delta
+    }
+
+    private func endThinkingBlock() {
+        guard isThinking else { return }
+        if let index = thinkingBlocks.indices.last {
+            thinkingBlocks[index].isComplete = true
+        }
+        isThinking = false
+    }
+
     private func finishStreamFailure(_ appError: AppInferenceError) {
         materializeServiceTranscript()
         finishWithError(appError)
diff --git a/Sources/TurboFieldfareApp/Mac/App/ChatHomeView.swift b/Sources/TurboFieldfareApp/Mac/App/ChatHomeView.swift
new file mode 100644
index 0000000..0706d39
--- /dev/null
+++ b/Sources/TurboFieldfareApp/Mac/App/ChatHomeView.swift
@@ -0,0 +1,263 @@
+import TurboFieldfareAppCore
+import TurboFieldfareMacPresentation
+import SwiftUI
+
+struct ChatHomeView: View {
+    let model: AppModel
+    let onOpenModels: () -> Void
+    let onSelectModel: (AppModelInstallDescriptor) -> Void
+
+    var body: some View {
+        VStack(spacing: 0) {
+            ChatToolbar(model: model, onSelectModel: onSelectModel)
+            TurboFieldfareMacTheme.subtleDivider.frame(height: 1)
+            conversation
+            ChatPrivacyFooter(model: model)
+        }
+        .background(Color(nsColor: .textBackgroundColor))
+        .task(id: model.modelPathText) {
+            guard !model.requiresModelInstallation, model.canLoadModel else { return }
+            model.loadModel()
+        }
+        .onChange(of: model.requiresModelInstallation) { _, needsInstall in
+            if !needsInstall, model.canLoadModel { model.loadModel() }
+        }
+    }
+
+    @ViewBuilder
+    private var conversation: some View {
+        if model.requiresModelInstallation {
+            installState
+        } else if model.hasOutputTranscript {
+            activeConversation
+        } else {
+            emptyConversation
+        }
+    }
+
+    private var emptyConversation: some View {
+        VStack(spacing: 30) {
+            Spacer(minLength: 40)
+
+            Text("What would you like to work on?")
+                .font(.system(size: 32, weight: .semibold))
+                .accessibilityHeading(.h1)
+
+            PromptComposerView(
+                model: model,
+                onSelectModel: onSelectModel)
+                .frame(maxWidth: .infinity)
+                .disabled(!model.isModelAvailable || model.isInstallingModel)
+                .opacity(model.isModelAvailable ? 1 : 0.74)
+
+            Spacer(minLength: 40)
+        }
+        .frame(maxWidth: .infinity, maxHeight: .infinity)
+        .padding(.horizontal, 12)
+    }
+
+    private var activeConversation: some View {
+        ZStack {
+            OutputPaneView(model: model)
+        }
+        .safeAreaInset(edge: .bottom, spacing: 0) {
+            VStack(spacing: 8) {
+                ErrorBanner(model: model)
+                PromptComposerView(
+                    model: model,
+                    onSelectModel: onSelectModel)
+                    .frame(maxWidth: .infinity)
+            }
+            .frame(maxWidth: .infinity)
+            .padding(.horizontal, 12)
+            .padding(.top, 10)
+            .padding(.bottom, 16)
+            .background(.bar)
+        }
+        .frame(maxWidth: .infinity, maxHeight: .infinity)
+    }
+
+    private var installState: some View {
+        VStack(spacing: 14) {
+            Image(systemName: "shippingbox")
+                .font(.system(size: 30))
+                .foregroundStyle(TurboFieldfareMacTheme.accentColor)
+                .accessibilityHidden(true)
+            Text("Choose a verified model")
+                .font(.title2.weight(.semibold))
+            Text("ElasticUMA will show what fits this Mac before downloading anything.")
+                .foregroundStyle(.secondary)
+                .multilineTextAlignment(.center)
+            Button("Open Models", action: onOpenModels)
+                .buttonStyle(PrimaryMacButtonStyle())
+                .padding(.top, 6)
+        }
+        .frame(maxWidth: .infinity, maxHeight: .infinity)
+        .padding(36)
+    }
+}
+
+private struct ChatToolbar: View {
+    let model: AppModel
+    let onSelectModel: (AppModelInstallDescriptor) -> Void
+
+    var body: some View {
+        HStack(spacing: 14) {
+            Menu {
+                Button("New chat") {
+                    model.clearOutput()
+                    model.promptText = ""
+                }
+                Button("Clear chat", action: model.clearOutput)
+                    .disabled(!model.hasOutputTranscript)
+            } label: {
+                Text(model.outputPromptText.isEmpty ? "New chat" : chatTitle)
+                    .font(.body.weight(.semibold))
+                    .lineLimit(1)
+            }
+            .menuStyle(.borderlessButton)
+            .tint(Color(nsColor: .labelColor))
+
+            Spacer()
+
+            ModelReadinessControl(model: model, onSelectModel: onSelectModel)
+        }
+        .frame(height: 76)
+        .padding(.horizontal, 28)
+        .background(Color(nsColor: .windowBackgroundColor))
+        .gesture(WindowDragGesture())
+    }
+
+    private var chatTitle: String {
+        let words = model.outputPromptText.split(separator: " ").prefix(5)
+        return words.joined(separator: " ")
+    }
+}
+
+private struct ModelReadinessControl: View {
+    let model: AppModel
+    let onSelectModel: (AppModelInstallDescriptor) -> Void
+
+    var body: some View {
+        HStack(spacing: 12) {
+            Menu {
+                ForEach(
+                    AppModelInstallDescriptor.selectionChoices(
+                        including: model.installDescriptor),
+                    id: \.selectionKey
+                ) { descriptor in
+                    Button {
+                        onSelectModel(descriptor)
+                    } label: {
+                        if descriptor == model.installDescriptor {
+                            Label(descriptor.displayName, systemImage: "checkmark")
+                        } else {
+                            Text(descriptor.displayName)
+                        }
+                    }
+                    .disabled(model.isRunning || model.isInstallingModel)
+                }
+            } label: {
+                Text(model.installDescriptor.displayName)
+                    .lineLimit(1)
+            }
+            .menuStyle(.borderlessButton)
+            .tint(Color(nsColor: .labelColor))
+
+            Divider().frame(height: 18)
+
+            Label("Verified", systemImage: "checkmark.shield")
+                .foregroundStyle(TurboFieldfareMacTheme.accentColor)
+
+            HStack(spacing: 6) {
+                if model.loadState.isLoading {
+                    ProgressView().controlSize(.mini)
+                } else {
+                    Circle()
+                        .fill(statusColor)
+                        .frame(width: 7, height: 7)
+                }
+                Text(statusLabel)
+                    .foregroundStyle(.secondary)
+                    .lineLimit(1)
+            }
+
+            Menu {
+                Button("Unload model", action: model.unloadModel)
+                    .disabled(!model.canUnloadModel)
+                Button("Reload model", action: model.reloadModel)
+                    .disabled(!model.canReloadModel)
+            } label: {
+                ZStack {
+                    Circle().fill(Color.primary.opacity(0.045))
+                    Circle().stroke(
+                        Color(nsColor: .separatorColor).opacity(0.65), lineWidth: 0.8)
+                    Image(systemName: "ellipsis")
+                }
+                .frame(width: 24, height: 24)
+            }
+            .menuStyle(.borderlessButton)
+            .menuIndicator(.hidden)
+            .foregroundStyle(.secondary)
+        }
+        .font(.body)
+        .padding(.horizontal, 16)
+        .frame(minWidth: 480)
+        .frame(height: 44)
+        .background(Color(nsColor: .controlBackgroundColor), in: .rect(cornerRadius: 10))
+        .overlay {
+            RoundedRectangle(cornerRadius: 10)
+                .stroke(TurboFieldfareMacTheme.subtleDivider, lineWidth: 0.75)
+        }
+    }
+
+    private var statusColor: Color {
+        model.isModelAvailable ? TurboFieldfareMacTheme.accentColor : .secondary
+    }
+
+    private var statusLabel: String {
+        if model.requiresModelInstallation { return "Not installed" }
+        if model.loadState.isLoading { return model.presentation.label }
+        if model.isModelAvailable { return model.isRunning ? "Generating" : "Ready" }
+        if model.loadState.isFailed { return "Load failed" }
+        return "Waiting"
+    }
+}
+
+private struct ChatPrivacyFooter: View {
+    let model: AppModel
+
+    var body: some View {
+        HStack {
+            Label("Local · Private", systemImage: "lock.fill")
+            Spacer()
+            Label(
+                MetricFormat.memory(ProcessInfo.processInfo.physicalMemory) + " unified memory",
+                systemImage: "memorychip")
+        }
+        .font(.caption)
+        .foregroundStyle(.secondary)
+        .padding(.horizontal, 24)
+        .frame(height: 44)
+        .background(Color(nsColor: .windowBackgroundColor))
+        .overlay(alignment: .top) {
+            TurboFieldfareMacTheme.subtleDivider.frame(height: 1)
+        }
+    }
+}
+
+struct PrimaryMacButtonStyle: ButtonStyle {
+    @Environment(\.isEnabled) private var isEnabled
+
+    func makeBody(configuration: Configuration) -> some View {
+        configuration.label
+            .font(.body.weight(.semibold))
+            .foregroundStyle(.white)
+            .padding(.horizontal, 28)
+            .frame(minWidth: 180, minHeight: 42)
+            .background(
+                TurboFieldfareMacTheme.accentColor.opacity(
+                    isEnabled ? (configuration.isPressed ? 0.82 : 1) : 0.38),
+                in: .rect(cornerRadius: 9))
+    }
+}
diff --git a/Sources/TurboFieldfareApp/Mac/App/ElasticSidebarView.swift b/Sources/TurboFieldfareApp/Mac/App/ElasticSidebarView.swift
new file mode 100644
index 0000000..8838e53
--- /dev/null
+++ b/Sources/TurboFieldfareApp/Mac/App/ElasticSidebarView.swift
@@ -0,0 +1,233 @@
+import AppKit
+import TurboFieldfareAppCore
+import TurboFieldfareMacPresentation
+import SwiftUI
+
+struct ElasticSidebarView: View {
+    let model: AppModel
+    @Binding var selection: ElasticUMASection
+    let recentChats: [RecentChat]
+    let onNewChat: () -> Void
+
+    var body: some View {
+        VStack(alignment: .leading, spacing: 0) {
+            identity
+            navigation
+            TurboFieldfareMacTheme.subtleDivider
+                .frame(height: 1)
+                .padding(.vertical, 12)
+            chats
+        }
+        .padding(.horizontal, 12)
+        .padding(.bottom, 12)
+        .background(TurboFieldfareMacTheme.sidebarColor)
+    }
+
+    private var identity: some View {
+        HStack(spacing: 10) {
+            appIcon
+            Text("ElasticUMA")
+                .font(.title2.weight(.semibold))
+        }
+        .padding(.horizontal, 8)
+        .padding(.top, 18)
+        .padding(.bottom, 18)
+        .gesture(WindowDragGesture())
+    }
+
+    @ViewBuilder
+    private var appIcon: some View {
+        if let url = Bundle.module.url(
+            forResource: "turbofieldfare-app-icon",
+            withExtension: "png"),
+           let image = NSImage(contentsOf: url) {
+            Image(nsImage: image)
+                .resizable()
+                .scaledToFit()
+                .frame(width: 50, height: 50)
+                .accessibilityHidden(true)
+        }
+    }
+
+    private var navigation: some View {
+        VStack(spacing: 4) {
+            SidebarNavigationButton(
+                label: "New chat",
+                symbol: "bubble.left",
+                selected: selection == .chat,
+                shortcut: "⌘ N") {
+                onNewChat()
+            }
+            SidebarNavigationButton(
+                label: "Search",
+                symbol: "magnifyingglass",
+                selected: selection == .search,
+                shortcut: "⌘ K") {
+                selection = .search
+            }
+            SidebarNavigationButton(
+                label: "Models",
+                symbol: "shippingbox",
+                selected: selection == .models) {
+                selection = .models
+            }
+            SidebarNavigationButton(
+                label: "Developer",
+                symbol: "chevron.left.forwardslash.chevron.right",
+                selected: selection == .developer) {
+                selection = .developer
+            }
+            SidebarNavigationButton(
+                label: "Settings",
+                symbol: "gearshape",
+                selected: selection == .settings) {
+                selection = .settings
+            }
+        }
+    }
+
+    private var chats: some View {
+        VStack(alignment: .leading, spacing: 6) {
+            HStack {
+                Text("Chats")
+                    .font(.caption.weight(.medium))
+                    .foregroundStyle(.secondary)
+                Spacer()
+                Image(systemName: "chevron.down")
+                    .font(.caption2.weight(.semibold))
+                    .foregroundStyle(.secondary)
+            }
+            .padding(.horizontal, 8)
+
+            if recentChats.isEmpty {
+                Text("Your conversations will appear here.")
+                    .font(.caption)
+                    .foregroundStyle(.tertiary)
+                    .padding(.horizontal, 8)
+                    .padding(.top, 4)
+            } else {
+                ScrollView {
+                    LazyVStack(spacing: 2) {
+                        ForEach(recentChats) { chat in
+                            SidebarChatRow(
+                                chat: chat,
+                                selected: model.promptText == chat.title
+                                    && selection == .chat) {
+                                model.promptText = chat.title
+                                selection = .chat
+                            }
+                        }
+                    }
+                }
+                .scrollIndicators(.hidden)
+            }
+        }
+        .frame(maxHeight: .infinity, alignment: .top)
+    }
+
+}
+
+private struct SidebarNavigationButton: View {
+    let label: String
+    let symbol: String
+    let selected: Bool
+    var shortcut: String? = nil
+    let action: () -> Void
+    @State private var hovering = false
+
+    var body: some View {
+        Button(action: action) {
+            HStack(spacing: 11) {
+                Image(systemName: symbol)
+                    .frame(width: 20)
+                Text(label)
+                Spacer()
+                if let shortcut {
+                    Text(shortcut)
+                        .font(.caption)
+                        .foregroundStyle(.tertiary)
+                }
+            }
+            .frame(maxWidth: .infinity, minHeight: 42, alignment: .leading)
+            .padding(.horizontal, 10)
+            .contentShape(Rectangle())
+        }
+        .buttonStyle(.plain)
+        .font(.body)
+        .foregroundStyle(
+            selected
+                ? TurboFieldfareMacTheme.accentColor
+                : Color(nsColor: .labelColor))
+        .background(
+            selected
+                ? TurboFieldfareMacTheme.accentColor.opacity(0.08)
+                : hovering ? Color.primary.opacity(0.05) : .clear,
+            in: .rect(cornerRadius: 8))
+        .overlay {
+            if selected {
+                RoundedRectangle(cornerRadius: 8)
+                    .stroke(TurboFieldfareMacTheme.subtleDivider, lineWidth: 0.5)
+            }
+        }
+        .onHover { hovering = $0 }
+    }
+}
+
+private struct SidebarChatRow: View {
+    let chat: RecentChat
+    let selected: Bool
+    let action: () -> Void
+    @State private var hovering = false
+
+    var body: some View {
+        Button(action: action) {
+            HStack(spacing: 9) {
+                Image(systemName: "bubble.left")
+                    .font(.callout)
+                    .foregroundStyle(.secondary)
+                Text(chat.title)
+                    .lineLimit(1)
+                Spacer(minLength: 4)
+                CompactRelativeTime(date: chat.updatedAt)
+            }
+            .frame(maxWidth: .infinity, minHeight: 34, alignment: .leading)
+            .padding(.horizontal, 9)
+            .contentShape(Rectangle())
+        }
+        .buttonStyle(.plain)
+        .foregroundStyle(.primary.opacity(0.88))
+        .background(
+            selected
+                ? TurboFieldfareMacTheme.accentColor.opacity(0.08)
+                : hovering ? Color.primary.opacity(0.05) : .clear,
+            in: .rect(cornerRadius: 7))
+        .onHover { hovering = $0 }
+        .help(chat.title)
+    }
+}
+
+struct CompactRelativeTime: View {
+    let date: Date
+
+    var body: some View {
+        TimelineView(.periodic(from: .now, by: 30)) { context in
+            Text(Self.label(from: date, to: context.date))
+                .font(.caption)
+                .foregroundStyle(.tertiary)
+                .monospacedDigit()
+        }
+    }
+
+    private static func label(from date: Date, to now: Date) -> String {
+        let seconds = max(0, Int(now.timeIntervalSince(date)))
+        if seconds < 60 { return seconds < 10 ? "now" : "\(seconds)s" }
+        let minutes = seconds / 60
+        if minutes < 60 { return "\(minutes)m" }
+        let hours = minutes / 60
+        if hours < 24 { return "\(hours)h" }
+        let days = hours / 24
+        if days == 1 { return "Yesterday" }
+        if days < 7 { return "\(days)d" }
+        return date.formatted(.dateTime.month(.abbreviated).day())
+    }
+}
diff --git a/Sources/TurboFieldfareApp/Mac/App/LocalAPIServerController.swift b/Sources/TurboFieldfareApp/Mac/App/LocalAPIServerController.swift
new file mode 100644
index 0000000..82e3d04
--- /dev/null
+++ b/Sources/TurboFieldfareApp/Mac/App/LocalAPIServerController.swift
@@ -0,0 +1,166 @@
+import Foundation
+import Observation
+
+@MainActor
+@Observable
+final class LocalAPIServerController {
+    enum State: Equatable {
+        case stopped
+        case starting
+        case running
+        case stopping
+        case failed(String)
+
+        var label: String {
+            switch self {
+            case .stopped: return "Local API off"
+            case .starting: return "Starting Local API"
+            case .running: return "Local API running"
+            case .stopping: return "Stopping Local API"
+            case .failed: return "Needs attention"
+            }
+        }
+    }
+
+    private(set) var state: State = .stopped
+    private(set) var logText = ""
+    private var process: Process?
+    private var outputPipe: Pipe?
+    private var readinessTask: Task<Void, Never>?
+
+    let port = 8080
+    var endpoint: String { "http://127.0.0.1:\(port)" }
+    var isActive: Bool { state == .starting || state == .running || state == .stopping }
+
+    func start(
+        modelPath: String,
+        modelID: String,
+        maxContextTokens: Int,
+        cacheSlots: Int,
+        hotSlots: Int
+    ) {
+        guard !isActive else { return }
+        let executable = Self.serverExecutableURL()
+        guard FileManager.default.isExecutableFile(atPath: executable.path) else {
+            state = .failed("The bundled local API server is missing. Reinstall ElasticUMA.")
+            return
+        }
+
+        let process = Process()
+        let pipe = Pipe()
+        process.executableURL = executable
+        process.arguments = [
+            "--model", modelPath,
+            "--model-id", modelID,
+            "--port", String(port),
+            "--max-context", String(maxContextTokens),
+            "--queue-limit", "4",
+            "--prompt-cache-mode", "single-prefix",
+            "--expert-cache-slots", String(cacheSlots),
+            "--expert-cache-residency", "os-managed",
+            "--expert-cache-hot-slots", String(hotSlots),
+        ]
+        process.standardOutput = pipe
+        process.standardError = pipe
+        process.terminationHandler = { [weak self] process in
+            Task { @MainActor in
+                guard let self, self.process === process else { return }
+                self.outputPipe?.fileHandleForReading.readabilityHandler = nil
+                self.readinessTask?.cancel()
+                self.readinessTask = nil
+                self.outputPipe = nil
+                self.process = nil
+                if self.state == .stopping || process.terminationStatus == 0 {
+                    self.state = .stopped
+                } else {
+                    self.state = .failed(
+                        "Server exited with status \(process.terminationStatus).")
+                }
+            }
+        }
+        pipe.fileHandleForReading.readabilityHandler = { [weak self] handle in
+            let data = handle.availableData
+            guard !data.isEmpty,
+                  let text = String(data: data, encoding: .utf8) else { return }
+            Task { @MainActor in
+                guard let self else { return }
+                self.appendLog(text)
+                if text.contains("ready at http://127.0.0.1:") {
+                    self.state = .running
+                }
+            }
+        }
+
+        state = .starting
+        logText = ""
+        self.process = process
+        self.outputPipe = pipe
+        do {
+            try process.run()
+            beginReadinessProbe(for: process)
+        } catch {
+            pipe.fileHandleForReading.readabilityHandler = nil
+            self.process = nil
+            self.outputPipe = nil
+            state = .failed("Could not start the local API: \(error.localizedDescription)")
+        }
+    }
+
+    func stop() {
+        readinessTask?.cancel()
+        readinessTask = nil
+        guard let process, process.isRunning else {
+            state = .stopped
+            return
+        }
+        state = .stopping
+        process.interrupt()
+    }
+
+    func clearFailure() {
+        if case .failed = state { state = .stopped }
+    }
+
+    private func appendLog(_ text: String) {
+        logText += text
+        if logText.count > 8_000 {
+            logText = String(logText.suffix(8_000))
+        }
+    }
+
+    private func beginReadinessProbe(for process: Process) {
+        readinessTask?.cancel()
+        readinessTask = Task { [weak self] in
+            guard let self else { return }
+            let healthURL = URL(string: "\(endpoint)/health")!
+            for _ in 0..<240 {
+                guard !Task.isCancelled, self.process === process, process.isRunning else {
+                    return
+                }
+                do {
+                    var request = URLRequest(url: healthURL)
+                    request.timeoutInterval = 1
+                    let (_, response) = try await URLSession.shared.data(for: request)
+                    if let http = response as? HTTPURLResponse,
+                       (200..<300).contains(http.statusCode) {
+                        if self.state == .starting { self.state = .running }
+                        self.readinessTask = nil
+                        return
+                    }
+                } catch {}
+                try? await Task.sleep(for: .milliseconds(250))
+            }
+            guard !Task.isCancelled, self.process === process, self.state == .starting else {
+                return
+            }
+            self.state = .failed("The Local API did not become ready within 60 seconds.")
+            self.readinessTask = nil
+        }
+    }
+
+    private static func serverExecutableURL() -> URL {
+        Bundle.main.executableURL!
+            .deletingLastPathComponent()
+            .appendingPathComponent("slipstream-server")
+    }
+}
diff --git a/Sources/TurboFieldfareApp/Mac/App/ModelsLibraryView.swift b/Sources/TurboFieldfareApp/Mac/App/ModelsLibraryView.swift
new file mode 100644
index 0000000..f44b67a
--- /dev/null
+++ b/Sources/TurboFieldfareApp/Mac/App/ModelsLibraryView.swift
@@ -0,0 +1,400 @@
+import TurboFieldfare
+import TurboFieldfareAppCore
+import TurboFieldfareMacPresentation
+import SwiftUI
+
+struct ModelsLibraryView: View {
+    enum Mode: String, CaseIterable, Identifiable {
+        case installed = "Installed"
+        case available = "Available"
+        var id: String { rawValue }
+    }
+
+    enum CompatibilityFilter: String, CaseIterable, Identifiable {
+        case installable = "Installable"
+        case validation = "Needs validation"
+        case all = "All matches"
+        var id: String { rawValue }
+    }
+
+    let model: AppModel
+    let onInstallModel: (AppModelInstallDescriptor) -> Void
+    let onOpenInstalled: (AppModelInstallDescriptor) -> Void
+
+    @State private var mode: Mode = .available
+    @State private var compatibilityFilter: CompatibilityFilter = .installable
+    @State private var searchText = ""
+    @State private var snapshot: AppModelCatalogSnapshot?
+    @State private var isRefreshing = false
+    @State private var activeRefreshID: UUID?
+    private let catalogClient = AppModelCatalogClient()
+
+    var body: some View {
+        VStack(spacing: 0) {
+            header
+            TurboFieldfareMacTheme.subtleDivider.frame(height: 1)
+            if model.isInstallingModel {
+                ModelInstallView(model: model)
+            } else {
+                catalogBody
+            }
+        }
+        .background(Color(nsColor: .textBackgroundColor))
+        .task(id: catalogTaskID) { await refreshForCurrentFilter() }
+        .onChange(of: model.installState) { _, state in
+            if case .installed = state {
+                Task { await refresh(search: normalizedSearch) }
+            }
+        }
+    }
+
+    private var header: some View {
+        VStack(spacing: 14) {
+            HStack(alignment: .firstTextBaseline) {
+                VStack(alignment: .leading, spacing: 4) {
+                    Text("Models").font(.title2.weight(.semibold))
+                    Text("Installed models and live, compatibility-checked Hugging Face results.")
+                        .font(.callout)
+                        .foregroundStyle(.secondary)
+                }
+                Spacer()
+                Button {
+                    Task { await refresh(search: normalizedSearch) }
+                } label: {
+                    Label("Refresh", systemImage: "arrow.clockwise")
+                        .labelStyle(.iconOnly)
+                }
+                .buttonStyle(.borderless)
+                .disabled(isRefreshing)
+                .help("Refresh installed models and Hugging Face")
+            }
+
+            HStack(spacing: 12) {
+                Picker("Source", selection: $mode) {
+                    ForEach(Mode.allCases) { Text($0.rawValue).tag($0) }
+                }
+                .pickerStyle(.segmented)
+                .frame(width: 230)
+
+                HStack(spacing: 8) {
+                    Image(systemName: "magnifyingglass")
+                        .foregroundStyle(.secondary)
+                    TextField(
+                        mode == .available
+                            ? "Search a model or paste a Hugging Face repo URL"
+                            : "Filter installed models",
+                        text: $searchText)
+                        .textFieldStyle(.plain)
+                    if !searchText.isEmpty {
+                        Button {
+                            searchText = ""
+                        } label: {
+                            Label("Clear search", systemImage: "xmark.circle.fill")
+                                .labelStyle(.iconOnly)
+                                .foregroundStyle(.tertiary)
+                        }
+                        .buttonStyle(.plain)
+                    }
+                }
+                .padding(.horizontal, 12)
+                .frame(height: 34)
+                .background(Color(nsColor: .controlBackgroundColor), in: .rect(cornerRadius: 8))
+                .overlay {
+                    RoundedRectangle(cornerRadius: 8)
+                        .stroke(TurboFieldfareMacTheme.subtleDivider, lineWidth: 0.6)
+                }
+            }
+        }
+        .padding(.horizontal, 28)
+        .padding(.vertical, 18)
+        .background(Color(nsColor: .windowBackgroundColor))
+        .gesture(WindowDragGesture())
+    }
+
+    @ViewBuilder
+    private var catalogBody: some View {
+        if isRefreshing && snapshot == nil {
+            VStack(spacing: 12) {
+                ProgressView()
+                Text("Checking this Mac and Hugging Face…")
+                    .foregroundStyle(.secondary)
+            }
+            .frame(maxWidth: .infinity, maxHeight: .infinity)
+        } else if mode == .installed {
+            installedModels
+        } else {
+            availableModels
+        }
+    }
+
+    private var installedModels: some View {
+        Group {
+            if filteredInstalled.isEmpty {
+                ContentUnavailableView(
+                    "No installed models",
+                    systemImage: "shippingbox",
+                    description: Text("Choose Available to install a verified compatible model."))
+            } else {
+                ScrollView {
+                    LazyVStack(spacing: 0) {
+                        ForEach(filteredInstalled) { installed in
+                            installedRow(installed)
+                            Divider().padding(.leading, 58)
+                        }
+                    }
+                    .padding(.vertical, 8)
+                }
+            }
+        }
+    }
+
+    private var availableModels: some View {
+        VStack(spacing: 0) {
+            HStack {
+                Picker("Compatibility", selection: $compatibilityFilter) {
+                    ForEach(CompatibilityFilter.allCases) { Text($0.rawValue).tag($0) }
+                }
+                .pickerStyle(.menu)
+                .fixedSize()
+                Text("\(supportedArchitectureCount) native architectures")
+                    .font(.caption)
+                    .foregroundStyle(.secondary)
+                Spacer()
+                if isRefreshing {
+                    ProgressView().controlSize(.mini)
+                }
+                Text(snapshot?.hubWarning == nil ? "Hugging Face live" : "Verified catalog offline")
+                    .font(.caption)
+                    .foregroundStyle(
+                        snapshot?.hubWarning == nil
+                            ? Color(nsColor: .secondaryLabelColor)
+                            : Color.orange)
+                Text("\(filteredAvailable.count) models")
+                    .font(.caption)
+                    .foregroundStyle(.secondary)
+            }
+            .padding(.horizontal, 28)
+            .padding(.vertical, 10)
+
+            if let warning = snapshot?.hubWarning {
+                Label(warning, systemImage: "wifi.exclamationmark")
+                    .font(.callout)
+                    .foregroundStyle(.orange)
+                    .frame(maxWidth: .infinity, alignment: .leading)
+                    .padding(.horizontal, 28)
+                    .padding(.bottom, 8)
+            }
+
+            if filteredAvailable.isEmpty {
+                ContentUnavailableView.search(text: searchText)
+            } else {
+                ScrollView {
+                    LazyVStack(spacing: 0) {
+                        ForEach(filteredAvailable) { available in
+                            availableRow(available)
+                            Divider().padding(.leading, 58)
+                        }
+                    }
+                    .padding(.vertical, 4)
+                }
+            }
+        }
+    }
+
+    private func installedRow(_ installed: AppInstalledModel) -> some View {
+        HStack(spacing: 14) {
+            Image(systemName: "internaldrive.fill")
+                .font(.title3)
+                .foregroundStyle(TurboFieldfareMacTheme.accentColor)
+                .frame(width: 30)
+            VStack(alignment: .leading, spacing: 4) {
+                Text(installed.displayName).font(.body.weight(.medium))
+                Text(installed.descriptor.runtimeCapabilities.inputSummary)
+                    .font(.caption)
+                    .foregroundStyle(.secondary)
+                Text(installed.directory.path)
+                    .font(.caption.monospaced())
+                    .foregroundStyle(.secondary)
+                    .lineLimit(1)
+                    .truncationMode(.middle)
+            }
+            Spacer()
+            Text(MetricFormat.storage(installed.allocatedBytes))
+                .font(.callout.monospacedDigit())
+                .foregroundStyle(.secondary)
+            Button("Open Chat") { onOpenInstalled(installed.descriptor) }
+                .buttonStyle(.borderedProminent)
+        }
+        .padding(.horizontal, 28)
+        .padding(.vertical, 14)
+    }
+
+    private func availableRow(_ available: AppHubModel) -> some View {
+        let installed = available.installDescriptor.map {
+            AppModelInstallationProbe.status(
+                at: AppModelLocation.defaultURL(descriptor: $0),
+                descriptor: $0) == .complete
+        } ?? false
+
+        return HStack(alignment: .center, spacing: 14) {
+            Image(systemName: available.compatibility == .needsValidation
+                  ? "questionmark.diamond" : "checkmark.shield")
+                .font(.title3)
+                .foregroundStyle(compatibilityColor(available.compatibility))
+                .frame(width: 30)
+
+            VStack(alignment: .leading, spacing: 5) {
+                HStack(spacing: 8) {
+                    Text(available.displayName).font(.body.weight(.medium))
+                    Text(familyLabel(available.family))
+                        .font(.caption)
+                        .foregroundStyle(.secondary)
+                    Text(capabilities(for: available).inputSummary)
+                        .font(.caption)
+                        .foregroundStyle(.secondary)
+                    Text(available.compatibility.label)
+                        .font(.caption.weight(.medium))
+                        .foregroundStyle(compatibilityColor(available.compatibility))
+                }
+                Text(available.id)
+                    .font(.caption.monospaced())
+                    .foregroundStyle(.secondary)
+                Text(available.compatibilityDetail)
+                    .font(.caption)
+                    .foregroundStyle(.secondary)
+            }
+
+            Spacer(minLength: 16)
+
+            if available.downloads > 0 {
+                Label(
+                    available.downloads.formatted(.number.notation(.compactName)),
+                    systemImage: "arrow.down")
+                    .font(.caption)
+                    .foregroundStyle(.secondary)
+            }
+
+            if let bytes = available.hubStorageBytes {
+                Text("Hub \(MetricFormat.storage(bytes))")
+                    .font(.caption.monospacedDigit())
+                    .foregroundStyle(.secondary)
+                    .help("Hugging Face repository storage; ElasticUMA streams only required ranges")
+            }
+
+            Link(destination: URL(string: "https://huggingface.co/\(available.id)")!) {
+                Label("View on Hugging Face", systemImage: "arrow.up.right.square")
+                    .labelStyle(.iconOnly)
+            }
+            .buttonStyle(.borderless)
+
+            if let descriptor = available.installDescriptor {
+                if installed {
+                    Button("Open Chat") { onOpenInstalled(descriptor) }
+                        .buttonStyle(.bordered)
+                } else {
+                    Button("Install") { onInstallModel(descriptor) }
+                        .buttonStyle(.borderedProminent)
+                }
+            } else {
+                Button("Not installable") {}
+                    .buttonStyle(.bordered)
+                    .disabled(true)
+            }
+        }
+        .padding(.horizontal, 28)
+        .padding(.vertical, 14)
+    }
+
+    private var filteredInstalled: [AppInstalledModel] {
+        let rows = snapshot?.installed ?? []
+        guard !normalizedSearch.isEmpty else { return rows }
+        return rows.filter {
+            $0.displayName.localizedCaseInsensitiveContains(normalizedSearch)
+                || $0.directory.path.localizedCaseInsensitiveContains(normalizedSearch)
+        }
+    }
+
+    private var filteredAvailable: [AppHubModel] {
+        var rows = snapshot?.available ?? []
+        switch compatibilityFilter {
+        case .installable:
+            rows = rows.filter { $0.canInstall }
+        case .validation:
+            rows = rows.filter { $0.compatibility == .needsValidation }
+        case .all:
+            break
+        }
+        if !normalizedSearch.isEmpty,
+           snapshot?.hubQuery?.localizedCaseInsensitiveCompare(normalizedSearch)
+            != .orderedSame {
+            rows = rows.filter {
+                $0.displayName.localizedCaseInsensitiveContains(normalizedSearch)
+                    || $0.id.localizedCaseInsensitiveContains(normalizedSearch)
+            }
+        }
+        return rows
+    }
+
+    private func compatibilityColor(_ compatibility: AppHubCompatibility) -> Color {
+        switch compatibility {
+        case .verified, .compatible: return TurboFieldfareMacTheme.accentColor
+        case .needsValidation: return .orange
+        }
+    }
+
+    private func familyLabel(_ family: ModelFamily) -> String {
+        switch family {
+        case .qwen36: return "Qwen3.6 MoE"
+        case .gemma4: return "Gemma 4 MoE"
+        }
+    }
+
+    private func capabilities(for model: AppHubModel) -> AppModelRuntimeCapabilities {
+        (model.installDescriptor
+            ?? AppModelInstallDescriptor.descriptor(for: model.family)!)
+            .runtimeCapabilities
+    }
+
+    private var normalizedSearch: String {
+        searchText.trimmingCharacters(in: .whitespacesAndNewlines)
+    }
+
+    private var catalogTaskID: String {
+        "\(mode.rawValue)|\(normalizedSearch.lowercased())"
+    }
+
+    private var supportedArchitectureCount: Int {
+        Set(AppModelInstallDescriptor.supported.map { $0.family.rawValue }).count
+    }
+
+    @MainActor
+    private func refreshForCurrentFilter() async {
+        if mode == .installed {
+            if snapshot == nil { await refresh(search: nil) }
+            return
+        }
+        if !normalizedSearch.isEmpty {
+            do {
+                try await Task.sleep(for: .milliseconds(350))
+            } catch {
+                return
+            }
+        }
+        guard !Task.isCancelled else { return }
+        await refresh(search: normalizedSearch)
+    }
+
+    @MainActor
+    private func refresh(search: String?) async {
+        let refreshID = UUID()
+        activeRefreshID = refreshID
+        isRefreshing = true
+        defer {
+            if activeRefreshID == refreshID { isRefreshing = false }
+        }
+        let query = search?.isEmpty == false ? search : nil
+        let refreshed = await catalogClient.refresh(search: query)
+        guard !Task.isCancelled, activeRefreshID == refreshID else { return }
+        snapshot = refreshed
+    }
+}
diff --git a/Sources/TurboFieldfareApp/Mac/App/PreviewInferenceClient.swift b/Sources/TurboFieldfareApp/Mac/App/PreviewInferenceClient.swift
new file mode 100644
index 0000000..a090217
--- /dev/null
+++ b/Sources/TurboFieldfareApp/Mac/App/PreviewInferenceClient.swift
@@ -0,0 +1,105 @@
+#if DEBUG
+import Foundation
+import TurboFieldfareAppCore
+
+final class PreviewInferenceClient: AppModelLifecycleClient, @unchecked Sendable {
+    private let lock = NSLock()
+    private var task: Task<Void, Never>?
+
+    func ensureLoaded(
+        modelDirectory: URL,
+        maxContextTokens: Int,
+        options: AppRuntimeOptions,
+        forceLogitsHead: Bool,
+        onState: @escaping @Sendable (AppModelLoadState) -> Void
+    ) async throws {
+        _ = maxContextTokens
+        _ = options
+        _ = forceLogitsHead
+        onState(.loading(.validatingDirectory))
+        try await Task.sleep(for: .milliseconds(250))
+        onState(.ready(modelDirectory: modelDirectory, loadSeconds: 0.25))
+    }
+
+    func unload() async {}
+
+    func generate(_ request: AppGenerationRequest)
+        -> AsyncThrowingStream<AppInferenceEvent, Error> {
+        AsyncThrowingStream { continuation in
+            let response = "ElasticUMA is running this preview locally. In the real app, the selected verified model generates the answer and cold expert pages can be reclaimed safely by macOS."
+            let pieces = response.split(separator: " ").map(String.init)
+            let task = Task {
+                continuation.yield(.prefillProgress(done: 1, total: 1))
+                let start = Date()
+                var index = 0
+                if request.thinkingEnabled {
+                    continuation.yield(.token(AppTokenEvent(
+                        index: index, textDelta: "", elapsedDecodeSeconds: 0,
+                        channel: .reasoningStarted)))
+                    index += 1
+                    for thought in [
+                        "Identify the requested constraints and relevant local context.",
+                        "Plan a concise answer and check it against the request.",
+                    ] {
+                        continuation.yield(.token(AppTokenEvent(
+                            index: index,
+                            textDelta: thought + "\n",
+                            elapsedDecodeSeconds: Date().timeIntervalSince(start),
+                            channel: .reasoning)))
+                        index += 1
+                    }
+                    continuation.yield(.token(AppTokenEvent(
+                        index: index, textDelta: "", elapsedDecodeSeconds: 0,
+                        channel: .reasoningEnded)))
+                    index += 1
+                }
+                var isFirstVisible = true
+                for piece in pieces {
+                    guard !Task.isCancelled else { break }
+                    try? await Task.sleep(for: .milliseconds(35))
+                    continuation.yield(.token(AppTokenEvent(
+                        index: index,
+                        textDelta: (isFirstVisible ? "" : " ") + piece,
+                        elapsedDecodeSeconds: Date().timeIntervalSince(start))))
+                    isFirstVisible = false
+                    index += 1
+                }
+                let seconds = max(Date().timeIntervalSince(start), 0.001)
+                continuation.yield(.finished(AppDiagnostics(
+                    generatedTokens: index,
+                    stopReason: .endOfTurn,
+                    promptTokenCount: request.prompt.split(separator: " ").count,
+                    prefillSeconds: 0.08,
+                    timeToFirstTokenSeconds: 0.04,
+                    decodeSeconds: seconds,
+                    tokensPerSecond: Double(index) / seconds,
+                    peakMemoryBytes: 4_800_000_000,
+                    runtimeOptions: request.runtimeOptions,
+                    runner: AppRunnerDiagnostics(
+                        expertHotSlots: request.runtimeOptions.expertCacheHotSlots,
+                        expertVolatileTransitions: 640,
+                        expertEmptyRecoveries: 18,
+                        expertReclaimedBytes: 302_000_000))))
+                continuation.finish()
+            }
+            lock.withLock { self.task = task }
+            continuation.onTermination = { [weak self] _ in self?.cancel() }
+        }
+    }
+
+    func cancel() {
+        lock.withLock {
+            task?.cancel()
+            task = nil
+        }
+    }
+}
+
+private extension NSLock {
+    func withLock<T>(_ body: () -> T) -> T {
+        lock()
+        defer { unlock() }
+        return body()
+    }
+}
+#endif
diff --git a/Sources/TurboFieldfareApp/Mac/App/RootView.swift b/Sources/TurboFieldfareApp/Mac/App/RootView.swift
index f0b81a9..d5b35f4 100644
--- a/Sources/TurboFieldfareApp/Mac/App/RootView.swift
+++ b/Sources/TurboFieldfareApp/Mac/App/RootView.swift
@@ -2,111 +2,318 @@ import TurboFieldfareAppCore
 import TurboFieldfareMacPresentation
 import SwiftUI
 
+enum ElasticUMASection: String, CaseIterable, Identifiable {
+    case chat
+    case search
+    case models
+    case developer
+    case settings
+
+    var id: String { rawValue }
+}
+
+struct RecentChat: Identifiable, Equatable {
+    let id: UUID
+    var title: String
+    var updatedAt: Date
+
+    init(id: UUID = UUID(), title: String, updatedAt: Date = Date()) {
+        self.id = id
+        self.title = title
+        self.updatedAt = updatedAt
+    }
+}
+
 struct RootView: View {
     let model: AppModel
-    @State private var conversationChromeHeight: CGFloat = 0
+    let onSelectModel: (AppModelInstallDescriptor) -> Void
+
+    @State private var selection: ElasticUMASection = .chat
+    @State private var recentChats: [RecentChat] = Self.previewRecentChats
+    @State private var server = LocalAPIServerController()
+    @State private var pendingInstallKey: String?
+    @State private var installFailureMessage = ""
+    @State private var showingInstallFailure = false
 
     var body: some View {
         HStack(spacing: 0) {
-            primaryContent
-                .frame(minWidth: 720, maxWidth: .infinity, maxHeight: .infinity)
+            ElasticSidebarView(
+                model: model,
+                selection: $selection,
+                recentChats: recentChats,
+                onNewChat: newChat)
+                .frame(width: 300)
 
-            Divider()
+            TurboFieldfareMacTheme.subtleDivider.frame(width: 1)
 
-            InspectorView(model: model)
-                .frame(width: 320)
-                .frame(maxHeight: .infinity)
-                .background(Color(nsColor: .windowBackgroundColor))
-        }
-        .containerBackground(for: .window) {
-            LinearGradient(
-                colors: [
-                    Color(nsColor: .windowBackgroundColor),
-                    Color(nsColor: .windowBackgroundColor).mix(
-                        with: TurboFieldfareMacTheme.accentColor,
-                        by: 0.04),
-                ],
-                startPoint: .top,
-                endPoint: .bottom)
+            primaryContent
+                .frame(minWidth: 720, maxWidth: .infinity, maxHeight: .infinity)
         }
+        .background(Color(nsColor: .textBackgroundColor))
         .tint(TurboFieldfareMacTheme.accentColor)
-        .animation(.smooth(duration: 0.3), value: model.requiresModelInstallation)
-        .animation(.smooth(duration: 0.25), value: model.error)
-        .transaction { transaction in
-            if model.isRunning {
-                transaction.animation = nil
-            }
+        .onChange(of: model.outputPromptText) { _, prompt in
+            let title = prompt.trimmingCharacters(in: .whitespacesAndNewlines)
+            guard !title.isEmpty else { return }
+            recentChats.removeAll { $0.title == title }
+            recentChats.insert(RecentChat(title: title), at: 0)
+            recentChats = Array(recentChats.prefix(12))
+        }
+        .task(id: model.installDescriptor.selectionKey) {
+            await startPendingInstall(model.installDescriptor.selectionKey)
+        }
+        .alert("Unable to install model", isPresented: $showingInstallFailure) {
+            Button("OK", role: .cancel) {}
+        } message: {
+            Text(installFailureMessage)
         }
     }
 
+    @ViewBuilder
     private var primaryContent: some View {
-        Group {
-            if model.requiresModelInstallation {
-                ModelInstallView(model: model)
-            } else {
-                conversationView
+        switch selection {
+        case .chat:
+            ChatHomeView(
+                model: model,
+                onOpenModels: { selection = .models },
+                onSelectModel: onSelectModel)
+        case .search:
+            ChatSearchView(
+                chats: recentChats,
+                onSelect: openPrompt)
+        case .models:
+            ModelsLibraryView(
+                model: model,
+                onInstallModel: beginInstall,
+                onOpenInstalled: openInstalled)
+        case .developer:
+            ServerControlView(model: model, server: server)
+        case .settings:
+            VStack(spacing: 0) {
+                sectionHeader(
+                    title: "Settings",
+                    subtitle: "Generation, memory, and model runtime controls.")
+                TurboFieldfareMacTheme.subtleDivider.frame(height: 1)
+                InspectorView(model: model)
+            }
+        }
+    }
+
+    private func newChat() {
+        if model.isRunning { model.cancel() }
+        model.clearOutput()
+        model.promptText = ""
+        selection = .chat
+    }
+
+    private func openPrompt(_ prompt: String) {
+        model.promptText = prompt
+        selection = .chat
+    }
+
+    private func beginInstall(_ descriptor: AppModelInstallDescriptor) {
+        pendingInstallKey = descriptor.selectionKey
+        if descriptor == model.installDescriptor {
+            Task { @MainActor in
+                await startPendingInstall(descriptor.selectionKey)
+            }
+        } else {
+            onSelectModel(descriptor)
+        }
+    }
+
+    @MainActor
+    private func startPendingInstall(_ selectionKey: String) async {
+        guard pendingInstallKey == selectionKey,
+              model.installDescriptor.selectionKey == selectionKey else { return }
+        for _ in 0..<60 where !model.canInstallModel {
+            do {
+                try await Task.sleep(for: .milliseconds(50))
+            } catch {
+                return
             }
         }
-        .safeAreaInset(edge: .top, spacing: 0) {
-            StatusHUDView(model: model)
+        defer { pendingInstallKey = nil }
+        if model.canInstallModel {
+            model.installModel()
+            return
+        }
+        switch model.installReadiness {
+        case .insufficientSpace(let requirement):
+            installFailureMessage = "Free at least \(MetricFormat.storage(requirement.shortfallBytes)) more disk space, then try again."
+        case .failed(let message):
+            installFailureMessage = message
+        case .checking:
+            installFailureMessage = "Disk and model readiness are still being checked. Try again in a moment."
+        case .ready:
+            installFailureMessage = "This model cannot be installed while another model task is active."
+        }
+        showingInstallFailure = true
+    }
+
+    private func openInstalled(_ descriptor: AppModelInstallDescriptor) {
+        onSelectModel(descriptor)
+        selection = .chat
+    }
+
+    private func sectionHeader(title: String, subtitle: String) -> some View {
+        VStack(alignment: .leading, spacing: 4) {
+            Text(title).font(.title2.weight(.semibold))
+            Text(subtitle)
+                .font(.callout)
+                .foregroundStyle(.secondary)
+        }
+        .frame(maxWidth: .infinity, alignment: .leading)
+        .padding(.horizontal, 28)
+        .padding(.vertical, 18)
+        .background(Color(nsColor: .windowBackgroundColor))
+        .gesture(WindowDragGesture())
+    }
+
+    private static var previewRecentChats: [RecentChat] {
+        #if DEBUG
+        guard ProcessInfo.processInfo.environment["ELASTICUMA_UI_PREVIEW"] == "1" else {
+            return []
         }
+        let now = Date()
+        return [
+            RecentChat(title: "Brainstorm product ideas", updatedAt: now.addingTimeInterval(-120)),
+            RecentChat(title: "Explain a concept simply", updatedAt: now.addingTimeInterval(-3_600)),
+            RecentChat(title: "Write a short story", updatedAt: now.addingTimeInterval(-7_200)),
+            RecentChat(title: "Plan a weekend trip", updatedAt: now.addingTimeInterval(-86_400)),
+            RecentChat(title: "Summarize a PDF", updatedAt: now.addingTimeInterval(-90_000)),
+            RecentChat(title: "Help with code refactor", updatedAt: now.addingTimeInterval(-172_800)),
+            RecentChat(title: "Compare ML models", updatedAt: now.addingTimeInterval(-259_200)),
+            RecentChat(title: "Draft project roadmap", updatedAt: now.addingTimeInterval(-345_600)),
+        ]
+        #else
+        []
+        #endif
+    }
+}
+
+private struct ChatSearchView: View {
+    let chats: [RecentChat]
+    let onSelect: (String) -> Void
+    @State private var query = ""
+
+    private var results: [RecentChat] {
+        let needle = query.trimmingCharacters(in: .whitespacesAndNewlines)
+        guard !needle.isEmpty else { return chats }
+        return chats.filter { $0.title.localizedCaseInsensitiveContains(needle) }
     }
 
-    private var conversationView: some View {
-        GeometryReader { geometry in
-            ZStack(alignment: .bottom) {
-                if model.hasOutputTranscript {
-                    OutputPaneView(model: model)
-                        .padding(.bottom, conversationChromeHeight)
-                } else if conversationChromeHeight > 0 {
-                    OutputPaneView(model: model)
-                        .frame(
-                            height: max(
-                                0,
-                                geometry.size.height - conversationChromeHeight))
-                        .frame(maxHeight: .infinity, alignment: .top)
+    var body: some View {
+        VStack(spacing: 0) {
+            VStack(alignment: .leading, spacing: 14) {
+                VStack(alignment: .leading, spacing: 4) {
+                    Text("Search")
+                        .font(.title2.weight(.semibold))
+                    Text("Find a conversation stored on this Mac.")
+                        .font(.callout)
+                        .foregroundStyle(.secondary)
                 }
 
-                conversationChrome
-                    .background {
-                        GeometryReader { chromeGeometry in
-                            Color.clear.preference(
-                                key: ConversationChromeHeightKey.self,
-                                value: chromeGeometry.size.height)
+                HStack(spacing: 10) {
+                    Image(systemName: "magnifyingglass")
+                        .foregroundStyle(.secondary)
+                    TextField("Search conversations", text: $query)
+                        .textFieldStyle(.plain)
+                        .font(.body)
+                    if !query.isEmpty {
+                        Button {
+                            query = ""
+                        } label: {
+                            Label("Clear search", systemImage: "xmark.circle.fill")
+                                .labelStyle(.iconOnly)
+                                .foregroundStyle(.tertiary)
                         }
+                        .buttonStyle(.plain)
                     }
-            }
-            .onPreferenceChange(ConversationChromeHeightKey.self) { height in
-                guard height > 0 else { return }
-                var transaction = Transaction()
-                transaction.disablesAnimations = true
-                withTransaction(transaction) {
-                    conversationChromeHeight = height
+                }
+                .padding(.horizontal, 14)
+                .frame(height: 42)
+                .background(
+                    Color(nsColor: .controlBackgroundColor),
+                    in: .rect(cornerRadius: 10))
+                .overlay {
+                    RoundedRectangle(cornerRadius: 10)
+                        .stroke(TurboFieldfareMacTheme.subtleDivider, lineWidth: 0.75)
                 }
             }
-        }
-    }
+            .frame(maxWidth: .infinity, alignment: .leading)
+            .padding(.horizontal, 28)
+            .padding(.vertical, 18)
+            .background(Color(nsColor: .windowBackgroundColor))
+            .gesture(WindowDragGesture())
+
+            TurboFieldfareMacTheme.subtleDivider.frame(height: 1)
+
+            if results.isEmpty {
+                ContentUnavailableView.search(text: query)
+                    .frame(maxWidth: .infinity, maxHeight: .infinity)
+            } else {
+                ScrollView {
+                    LazyVStack(alignment: .leading, spacing: 10) {
+                        Text(resultLabel)
+                            .font(.caption.weight(.medium))
+                            .foregroundStyle(.secondary)
+                            .padding(.horizontal, 2)
+
+                        ForEach(results) { chat in
+                            Button {
+                                onSelect(chat.title)
+                            } label: {
+                                HStack(spacing: 14) {
+                                    Image(systemName: "bubble.left")
+                                        .font(.body)
+                                        .foregroundStyle(TurboFieldfareMacTheme.accentColor)
+                                        .frame(width: 34, height: 34)
+                                        .background(
+                                            TurboFieldfareMacTheme.accentColor.opacity(0.08),
+                                            in: .rect(cornerRadius: 9))
 
-    private var conversationChrome: some View {
-        VStack(spacing: 10) {
-            ErrorBanner(model: model)
-            if model.promptText.isEmpty {
-                PromptExamplesView { preset in
-                    model.promptText = preset.prompt
+                                    VStack(alignment: .leading, spacing: 4) {
+                                        Text(chat.title)
+                                            .font(.body.weight(.medium))
+                                            .lineLimit(2)
+                                        Text("Local conversation")
+                                            .font(.caption)
+                                            .foregroundStyle(.secondary)
+                                    }
+
+                                    Spacer(minLength: 16)
+                                    CompactRelativeTime(date: chat.updatedAt)
+                                    Image(systemName: "chevron.right")
+                                        .font(.caption.weight(.semibold))
+                                        .foregroundStyle(.tertiary)
+                                }
+                                .padding(.horizontal, 16)
+                                .padding(.vertical, 13)
+                                .frame(maxWidth: .infinity, alignment: .leading)
+                                .background(
+                                    Color(nsColor: .controlBackgroundColor),
+                                    in: .rect(cornerRadius: 12))
+                                .overlay {
+                                    RoundedRectangle(cornerRadius: 12)
+                                        .stroke(
+                                            TurboFieldfareMacTheme.subtleDivider,
+                                            lineWidth: 0.75)
+                                }
+                            }
+                            .buttonStyle(.plain)
+                        }
+                    }
+                    .frame(maxWidth: 840)
+                    .padding(24)
+                    .frame(maxWidth: .infinity, alignment: .top)
                 }
             }
-            PromptComposerView(model: model)
         }
-        .padding(.horizontal, 20)
-        .padding(.bottom, 16)
-        .animation(.smooth(duration: 0.2), value: model.promptText.isEmpty)
+        .background(Color(nsColor: .textBackgroundColor))
     }
-}
-
-private struct ConversationChromeHeightKey: PreferenceKey {
-    static let defaultValue: CGFloat = 0
 
-    static func reduce(value: inout CGFloat, nextValue: () -> CGFloat) {
-        value = max(value, nextValue())
+    private var resultLabel: String {
+        let count = results.count
+        return "\(count) conversation\(count == 1 ? "" : "s")"
     }
 }
diff --git a/Sources/TurboFieldfareApp/Mac/App/ServerControlView.swift b/Sources/TurboFieldfareApp/Mac/App/ServerControlView.swift
new file mode 100644
index 0000000..557198b
--- /dev/null
+++ b/Sources/TurboFieldfareApp/Mac/App/ServerControlView.swift
@@ -0,0 +1,205 @@
+import AppKit
+import TurboFieldfareAppCore
+import TurboFieldfareMacPresentation
+import SwiftUI
+
+struct ServerControlView: View {
+    let model: AppModel
+    let server: LocalAPIServerController
+    @State private var startAfterUnload = false
+
+    var body: some View {
+        VStack(spacing: 0) {
+            header
+            TurboFieldfareMacTheme.subtleDivider.frame(height: 1)
+            ScrollView {
+                VStack(alignment: .leading, spacing: 26) {
+                    runtimeSummary
+                    Divider()
+                    status
+                    Divider()
+                    compatibility
+                    if !server.logText.isEmpty { logs }
+                }
+                .frame(maxWidth: 700, alignment: .leading)
+                .padding(34)
+                .frame(maxWidth: .infinity, alignment: .top)
+            }
+        }
+        .background(Color(nsColor: .textBackgroundColor))
+        .onChange(of: model.loadState) { _, state in
+            guard startAfterUnload, state == .notLoaded else { return }
+            startAfterUnload = false
+            startServer()
+        }
+    }
+
+    private var header: some View {
+        VStack(alignment: .leading, spacing: 5) {
+            Text("Developer")
+                .font(.title2.weight(.semibold))
+            Text("Local API, runtime state, and memory visibility.")
+                .font(.callout)
+                .foregroundStyle(.secondary)
+        }
+        .frame(maxWidth: .infinity, alignment: .leading)
+        .padding(.horizontal, 24)
+        .padding(.vertical, 18)
+        .background(Color(nsColor: .windowBackgroundColor))
+        .gesture(WindowDragGesture())
+    }
+
+    private var runtimeSummary: some View {
+        HStack(spacing: 26) {
+            summaryItem(
+                "Unified memory",
+                MetricFormat.memory(ProcessInfo.processInfo.physicalMemory),
+                "memorychip")
+            summaryItem(
+                "Model memory",
+                MetricFormat.memory(model.currentProcessMemoryBytes),
+                "gauge.with.dots.needle.33percent")
+            summaryItem(
+                "Expert cache",
+                "\(model.runtimeOptions.expertCacheSlots) logical · \(model.runtimeOptions.expertCacheHotSlots) hot",
+                "internaldrive")
+            Spacer()
+        }
+    }
+
+    private func summaryItem(_ label: String, _ value: String, _ symbol: String) -> some View {
+        VStack(alignment: .leading, spacing: 5) {
+            Label(label, systemImage: symbol)
+                .font(.caption)
+                .foregroundStyle(.secondary)
+            Text(value)
+                .font(.callout.weight(.medium))
+                .monospacedDigit()
+        }
+    }
+
+    private var status: some View {
+        VStack(alignment: .leading, spacing: 16) {
+            HStack(alignment: .top, spacing: 18) {
+                Image(systemName: "rectangle.connected.to.line.below")
+                    .font(.system(size: 30))
+                    .foregroundStyle(TurboFieldfareMacTheme.accentColor)
+                    .frame(width: 54, height: 54)
+                    .background(
+                        TurboFieldfareMacTheme.accentColor.opacity(0.08),
+                        in: .rect(cornerRadius: 12))
+
+                VStack(alignment: .leading, spacing: 6) {
+                    Text(server.state.label)
+                        .font(.title3.weight(.semibold))
+                    Text(model.installDescriptor.displayName)
+                        .foregroundStyle(.secondary)
+                    if case .failed(let message) = server.state {
+                        Text(message)
+                            .font(.callout)
+                            .foregroundStyle(.red)
+                    }
+                }
+
+                Spacer()
+
+                serverAction
+            }
+
+            LabeledContent("Endpoint") {
+                HStack(spacing: 8) {
+                    Text(server.endpoint)
+                        .font(.callout.monospaced())
+                    Button {
+                        NSPasteboard.general.clearContents()
+                        NSPasteboard.general.setString(server.endpoint, forType: .string)
+                    } label: {
+                        Label("Copy endpoint", systemImage: "doc.on.doc")
+                            .labelStyle(.iconOnly)
+                    }
+                    .buttonStyle(.borderless)
+                }
+            }
+
+            LabeledContent("Network") {
+                Text("This Mac only (loopback)")
+                    .foregroundStyle(.secondary)
+            }
+
+            if model.isModelAvailable && !server.isActive {
+                Label(
+                    "Chat pauses while the Local API owns the model. ElasticUMA switches ownership automatically.",
+                    systemImage: "info.circle")
+                    .font(.callout)
+                    .foregroundStyle(.secondary)
+            }
+        }
+    }
+
+    @ViewBuilder
+    private var serverAction: some View {
+        switch server.state {
+        case .running, .starting:
+            Button("Stop Local API", action: server.stop)
+                .buttonStyle(.bordered)
+                .controlSize(.large)
+        case .stopping:
+            ProgressView().controlSize(.small)
+        case .stopped, .failed:
+            Button("Start Local API") {
+                if model.isModelAvailable {
+                    startAfterUnload = true
+                    model.unloadModel()
+                } else {
+                    startServer()
+                }
+            }
+            .buttonStyle(PrimaryMacButtonStyle())
+            .disabled(model.requiresModelInstallation || model.isRunning)
+        }
+    }
+
+    private func startServer() {
+        server.clearFailure()
+        server.start(
+            modelPath: model.modelPathText,
+            modelID: model.installDescriptor.id,
+            maxContextTokens: model.maxContextTokens,
+            cacheSlots: model.runtimeOptions.expertCacheSlots,
+            hotSlots: model.runtimeOptions.expertCacheHotSlots)
+    }
+
+    private var compatibility: some View {
+        VStack(alignment: .leading, spacing: 14) {
+            Text("API compatibility")
+                .font(.headline)
+            LabeledContent("OpenAI") {
+                Text("/v1/chat/completions").font(.callout.monospaced())
+            }
+            LabeledContent("Anthropic") {
+                Text("/v1/messages").font(.callout.monospaced())
+            }
+            Label("One shared model copy", systemImage: "internaldrive")
+            Label(
+                "\(model.installDescriptor.runtimeCapabilities.inputSummary) for this model",
+                systemImage: "text.alignleft")
+            Text(
+                "Expert residency is \(model.runtimeOptions.expertCacheResidency.label.lowercased()): "
+                    + "\(model.runtimeOptions.expertCacheSlots) logical slots, "
+                    + "\(model.runtimeOptions.expertCacheHotSlots) kept hot. "
+                    + "The server never binds beyond 127.0.0.1.")
+                .font(.callout)
+                .foregroundStyle(.secondary)
+        }
+    }
+
+    private var logs: some View {
+        DisclosureGroup("Server log") {
+            Text(server.logText)
+                .font(.caption.monospaced())
+                .textSelection(.enabled)
+                .frame(maxWidth: .infinity, alignment: .leading)
+                .padding(.top, 8)
+        }
+    }
+}
diff --git a/Sources/TurboFieldfareApp/Mac/App/TurboFieldfareMacApp.swift b/Sources/TurboFieldfareApp/Mac/App/TurboFieldfareMacApp.swift
index 1eb67e5..f3c346c 100644
--- a/Sources/TurboFieldfareApp/Mac/App/TurboFieldfareMacApp.swift
+++ b/Sources/TurboFieldfareApp/Mac/App/TurboFieldfareMacApp.swift
@@ -29,18 +29,17 @@ struct TurboFieldfareMacApp: App {
     @State private var model: AppModel
 
     init() {
-        _model = State(initialValue: AppModel(
-            client: DecodeServiceInferenceClient(),
-            settingsPersistenceEnabled: true))
+        _model = State(initialValue: Self.makeModel(.selected))
     }
 
     var body: some Scene {
-        Window("TurboFieldfare", id: "main") {
-            RootView(model: model)
-                .frame(minWidth: 1040, minHeight: 560)
+        Window("ElasticUMA", id: "main") {
+            RootView(model: model, onSelectModel: selectModel)
+                .frame(minWidth: 1120, minHeight: 680)
+                .preferredColorScheme(Self.isUIPreview ? .light : nil)
         }
         .windowStyle(.hiddenTitleBar)
-        .defaultSize(width: 1040, height: 720)
+        .defaultSize(width: 1280, height: 820)
         .windowResizability(.contentMinSize)
         .commands {
             CommandMenu("Generation") {
@@ -60,4 +59,47 @@ struct TurboFieldfareMacApp: App {
             }
         }
     }
+
+    private func selectModel(_ descriptor: AppModelInstallDescriptor) {
+        guard descriptor != model.installDescriptor,
+              !model.isRunning,
+              !model.isInstallingModel else { return }
+        descriptor.persistSelection()
+        model = Self.makeModel(descriptor)
+    }
+
+    @MainActor
+    private static func makeModel(_ descriptor: AppModelInstallDescriptor) -> AppModel {
+        let preview = isUIPreview
+        let directory: URL
+        if preview {
+            directory = FileManager.default.temporaryDirectory
+                .appendingPathComponent("elasticuma-ui-preview", isDirectory: true)
+            try? FileManager.default.createDirectory(
+                at: directory, withIntermediateDirectories: true)
+        } else {
+            directory = AppModelLocation.defaultURL(descriptor: descriptor)
+        }
+        #if DEBUG
+        let client: any AppInferenceClient = preview
+            ? PreviewInferenceClient()
+            : DecodeServiceInferenceClient()
+        #else
+        let client: any AppInferenceClient = DecodeServiceInferenceClient()
+        #endif
+        return AppModel(
+            modelDirectory: directory,
+            client: client,
+            installer: RepackModelInstallerClient(descriptor: descriptor),
+            settingsPersistenceEnabled: !preview,
+            installationStatusOverride: preview ? .complete : nil)
+    }
+
+    private static var isUIPreview: Bool {
+        #if DEBUG
+        ProcessInfo.processInfo.environment["ELASTICUMA_UI_PREVIEW"] == "1"
+        #else
+        false
+        #endif
+    }
 }
diff --git a/Sources/TurboFieldfareApp/Mac/Components/GenerateControl.swift b/Sources/TurboFieldfareApp/Mac/Components/GenerateControl.swift
index 8c8f299..ef8b7ef 100644
--- a/Sources/TurboFieldfareApp/Mac/Components/GenerateControl.swift
+++ b/Sources/TurboFieldfareApp/Mac/Components/GenerateControl.swift
@@ -4,75 +4,57 @@ import SwiftUI
 
 struct GenerateControl: View {
     let model: AppModel
-    private let controlHeight: CGFloat = 34
 
     var body: some View {
         if model.isRunning {
-            runningPill
+            runningControl
         } else {
             generateButton
         }
     }
 
     private var generateButton: some View {
-        Button {
-            model.run()
-        } label: {
+        Button(action: model.run) {
             Label("Generate", systemImage: "arrow.up")
-                .font(.callout.weight(.semibold))
-                .padding(.horizontal, 24)
-                .frame(minWidth: 124, minHeight: controlHeight)
-                .contentShape(Capsule())
+                .labelStyle(.iconOnly)
+                .font(.body.weight(.semibold))
+                .frame(width: 42, height: 42)
+                .contentShape(Circle())
         }
         .buttonStyle(.plain)
         .foregroundStyle(.white)
-        .background(TurboFieldfareMacTheme.accentColor, in: .capsule)
-        .overlay {
-            Capsule().stroke(.white.opacity(0.16), lineWidth: 0.5)
-        }
+        .background(TurboFieldfareMacTheme.accentColor, in: .circle)
         .keyboardShortcut(.return, modifiers: .command)
         .disabled(!model.canRun)
-        .opacity(model.canRun ? 1 : 0.62)
+        .opacity(model.canRun ? 1 : 0.35)
+        .help("Generate · ⌘ Return")
     }
 
-    private var runningPill: some View {
-        Button {
-            model.cancel()
-        } label: {
-            HStack(spacing: 10) {
-                if model.isCancellationPending {
-                    Text("Stopping")
-                        .font(.callout.weight(.medium))
-                } else if model.phase == .prefill {
-                    Text(model.presentation.label)
-                        .font(.callout.weight(.medium))
-                        .monospacedDigit()
-                        .contentTransition(.numericText())
-                } else {
-                    Text("\(MetricFormat.rate(model.liveTokensPerSecond)) tok/s")
-                        .font(.callout.weight(.semibold))
-                        .monospacedDigit()
-                        .contentTransition(.numericText())
-                }
+    private var runningControl: some View {
+        HStack(spacing: 10) {
+            Text(runningLabel)
+                .font(.callout.weight(.medium))
+                .monospacedDigit()
+                .foregroundStyle(.secondary)
+            Button(action: model.cancel) {
                 Label("Stop generation", systemImage: "stop.fill")
                     .labelStyle(.iconOnly)
-                    .font(.callout)
-                    .frame(width: 28, height: 28)
+                    .font(.caption.weight(.bold))
+                    .frame(width: 42, height: 42)
+                    .contentShape(Circle())
             }
-            .padding(.leading, 18)
-            .padding(.trailing, 4)
-            .frame(minWidth: 140, minHeight: controlHeight)
-            .contentShape(Capsule())
-        }
-        .buttonStyle(.plain)
-        .foregroundStyle(.white)
-        .background(TurboFieldfareMacTheme.accentColor, in: .capsule)
-        .overlay {
-            Capsule().stroke(.white.opacity(0.16), lineWidth: 0.5)
+            .buttonStyle(.plain)
+            .foregroundStyle(.white)
+            .background(TurboFieldfareMacTheme.accentColor, in: .circle)
+            .keyboardShortcut(.cancelAction)
+            .disabled(!model.canCancel)
+            .help("Stop generation")
         }
-        .keyboardShortcut(.cancelAction)
-        .disabled(!model.canCancel)
-        .help("Stop generation")
-        .animation(.smooth(duration: 0.2), value: model.presentation.label)
+    }
+
+    private var runningLabel: String {
+        if model.isCancellationPending { return "Stopping" }
+        if model.phase == .prefill { return model.presentation.label }
+        return "\(MetricFormat.rate(model.liveTokensPerSecond)) tok/s"
     }
 }
diff --git a/Sources/TurboFieldfareApp/Mac/Diagnostics/InspectorView.swift b/Sources/TurboFieldfareApp/Mac/Diagnostics/InspectorView.swift
index c38ea22..d602243 100644
--- a/Sources/TurboFieldfareApp/Mac/Diagnostics/InspectorView.swift
+++ b/Sources/TurboFieldfareApp/Mac/Diagnostics/InspectorView.swift
@@ -1,5 +1,7 @@
 import AppKit
+import TurboFieldfare
 import TurboFieldfareAppCore
+import TurboFieldfareMacPresentation
 import SwiftUI
 
 struct InspectorView: View {
@@ -20,14 +22,48 @@ struct InspectorView: View {
 
     private var modelSection: some View {
         Section("Model") {
-            LabeledContent("Path") {
-                HStack(spacing: 6) {
-                    Text(model.modelPathText)
+            HStack(spacing: 14) {
+                Image(systemName: "shippingbox.fill")
+                    .font(.title3)
+                    .foregroundStyle(TurboFieldfareMacTheme.accentColor)
+                    .frame(width: 40, height: 40)
+                    .background(
+                        TurboFieldfareMacTheme.accentColor.opacity(0.08),
+                        in: .rect(cornerRadius: 10))
+
+                VStack(alignment: .leading, spacing: 3) {
+                    Text(model.installDescriptor.displayName)
+                        .font(.body.weight(.semibold))
+                    Text(modelStateLabel)
                         .font(.caption)
+                        .foregroundStyle(.secondary)
+                }
+
+                Spacer(minLength: 16)
+                if model.canUnloadModel {
+                    Button(action: model.unloadModel) {
+                        Label("Free Memory", systemImage: "memorychip")
+                    }
+                    .buttonStyle(.bordered)
+                    .help("Unloads the model from memory but keeps it installed on disk")
+                }
+            }
+
+            if model.canUnloadModel {
+                Text("Free Memory closes the active model session. The verified model stays installed and reloads when you return to Chat.")
+                    .font(.caption)
+                    .foregroundStyle(.secondary)
+            }
+
+            DisclosureGroup("Model files") {
+                HStack(spacing: 8) {
+                    Text(model.modelPathText)
+                        .font(.caption.monospaced())
                         .truncationMode(.middle)
-                        .lineLimit(1)
+                        .lineLimit(2)
                         .foregroundStyle(.secondary)
                         .help(model.modelPathText)
+                    Spacer(minLength: 8)
                     Button {
                         NSPasteboard.general.clearContents()
                         NSPasteboard.general.setString(model.modelPathText, forType: .string)
@@ -38,14 +74,7 @@ struct InspectorView: View {
                     .buttonStyle(.borderless)
                     .help("Copy model path")
                 }
-            }
-            if model.canUnloadModel {
-                Button("Unload Model", action: model.unloadModel)
-            }
-            LabeledContent("State") {
-                Text(model.presentation.label)
-                    .font(.caption)
-                    .foregroundStyle(.secondary)
+                .padding(.top, 6)
             }
             if model.requiresModelInstallation {
                 LabeledContent("Download") {
@@ -71,7 +100,7 @@ struct InspectorView: View {
     }
 
     private var memorySection: some View {
-        Section("Memory") {
+        Section("Memory & context") {
             LabeledContent("Context") {
                 Picker("Context", selection: $model.maxContextTokens) {
                     ForEach(AppContextLengthOption.allCases) { option in
@@ -81,6 +110,11 @@ struct InspectorView: View {
                 .pickerStyle(.menu)
                 .labelsHidden()
                 .fixedSize()
+                .onChange(of: model.runtimeOptions.expertCacheSlots) { _, slots in
+                    model.runtimeOptions.expertCacheHotSlots = min(
+                        model.runtimeOptions.expertCacheHotSlots,
+                        slots)
+                }
             }
             LabeledContent("Slots") {
                 Picker("Slots", selection: $model.runtimeOptions.expertCacheSlots) {
@@ -92,7 +126,28 @@ struct InspectorView: View {
                 .labelsHidden()
                 .fixedSize()
             }
-            Text("More slots can improve decode speed by keeping more experts in memory, but they also use more RAM. Changes are compared with 4K context and 16 slots and apply after reloading the model.")
+            LabeledContent("Residency") {
+                Picker("Residency", selection: $model.runtimeOptions.expertCacheResidency) {
+                    ForEach(AppExpertCacheResidency.allCases) { residency in
+                        Text(residency.label).tag(residency)
+                    }
+                }
+                .pickerStyle(.menu)
+                .labelsHidden()
+                .fixedSize()
+            }
+            if model.runtimeOptions.expertCacheResidency == .osManaged {
+                LabeledContent("Hot slots") {
+                    Stepper(
+                        value: $model.runtimeOptions.expertCacheHotSlots,
+                        in: 0...model.runtimeOptions.expertCacheSlots) {
+                        Text("\(model.runtimeOptions.expertCacheHotSlots)")
+                            .monospacedDigit()
+                    }
+                    .fixedSize()
+                }
+            }
+            Text("OS-managed cache keeps frequently used experts ready while macOS may reclaim cold pages. Reclaimed bytes are reloaded exactly before reuse. Changes apply after the model reloads.")
                 .font(.caption)
                 .foregroundStyle(.secondary)
         }
@@ -135,26 +190,49 @@ struct InspectorView: View {
                     }
                 }
             }
+            if model.installDescriptor.family == .qwen36 {
+                Toggle("Show model thinking", isOn: $model.thinkingEnabled)
+                    .toggleStyle(.switch)
+                Text("Shows Qwen reasoning in a separate collapsible block. Thinking can improve difficult answers but takes longer and uses more tokens.")
+                    .font(.caption)
+                    .foregroundStyle(.secondary)
+            }
         }
         .disabled(model.isRunning || model.loadState.isLoading)
     }
 
     private var runtimeSection: some View {
         Section("Runtime") {
-            Toggle("Prefill", isOn: $model.runtimeOptions.prefillEnabled)
-            VStack(alignment: .leading, spacing: 8) {
-                Text("RDADVISE")
-                Picker("RDADVISE", selection: $model.runtimeOptions.rdadvisePolicy) {
-                    ForEach(AppRDAdvicePolicy.allCases) { policy in
-                        Text(policy.label).tag(policy)
+            Toggle(
+                "Fast prompt processing",
+                isOn: $model.runtimeOptions.prefillEnabled)
+            Text("Processes prompt tokens in chunks so routed experts can be reused across the prompt.")
+                .font(.caption)
+                .foregroundStyle(.secondary)
+
+            DisclosureGroup("Advanced SSD I/O") {
+                VStack(alignment: .leading, spacing: 10) {
+                    LabeledContent("Expert read-ahead") {
+                        Picker(
+                            "Expert read-ahead",
+                            selection: $model.runtimeOptions.rdadvisePolicy
+                        ) {
+                            ForEach(AppRDAdvicePolicy.allCases) { policy in
+                                Text(readAheadLabel(policy)).tag(policy)
+                            }
+                        }
+                        .pickerStyle(.menu)
+                        .labelsHidden()
+                        .fixedSize()
                     }
+
+                    Text("Asks macOS to prefetch upcoming expert ranges from SSD. It can help some short runs but may add overhead; leave it Off unless you are benchmarking.")
+                        .font(.caption)
+                        .foregroundStyle(.secondary)
+                        .fixedSize(horizontal: false, vertical: true)
                 }
-                .pickerStyle(.segmented)
-                .labelsHidden()
+                .padding(.top, 8)
             }
-            Text("RDADVISE is experimental. It may speed up short decodes but slow down long decodes.")
-                .font(.caption)
-                .foregroundStyle(.secondary)
             if model.hasStaleLoadedRuntime {
                 Text("Reload required")
                     .font(.caption)
@@ -164,4 +242,21 @@ struct InspectorView: View {
         .disabled(model.isRunning || model.loadState.isLoading)
     }
 
+    private var modelStateLabel: String {
+        if model.isRunning { return "Generating" }
+        if model.loadState.isLoading { return "Loading" }
+        if model.loadState.isReady { return "Loaded and ready" }
+        if model.loadState.isFailed { return "Needs attention" }
+        return model.isModelInstalled ? "Installed on disk" : "Not installed"
+    }
+
+    private func readAheadLabel(_ policy: AppRDAdvicePolicy) -> String {
+        switch policy {
+        case .off: return "Off (recommended)"
+        case .default: return "Standard"
+        case .bounded: return "Bounded"
+        case .adaptive: return "Adaptive"
+        }
+    }
+
 }
diff --git a/Sources/TurboFieldfareApp/Mac/Generation/OutputPaneView.swift b/Sources/TurboFieldfareApp/Mac/Generation/OutputPaneView.swift
index 5307de5..2dbf879 100644
--- a/Sources/TurboFieldfareApp/Mac/Generation/OutputPaneView.swift
+++ b/Sources/TurboFieldfareApp/Mac/Generation/OutputPaneView.swift
@@ -59,22 +59,32 @@ struct OutputPaneView: View {
     }
 
     private var transcript: some View {
-        IncrementalTranscriptView(
-            prompt: model.outputPromptText,
-            output: model.outputText,
-            mailbox: model.generationTranscriptMailbox,
-            isTerminal: !model.isRunning,
-            showsPrefillPlaceholder: model.isRunning
-                && model.outputResponsePlainText.isEmpty)
-            .frame(maxWidth: .infinity, maxHeight: .infinity)
-            .overlay(alignment: .topTrailing) {
-                if !model.isRunning && !model.outputResponsePlainText.isEmpty {
-                    copyResponseButton
-                        .padding(8)
-                }
+        VStack(spacing: 0) {
+            if !model.thinkingBlocks.isEmpty {
+                ThinkingPanel(
+                    blocks: model.thinkingBlocks,
+                    isThinking: model.isThinking)
+                    .padding(.horizontal, 24)
+                    .padding(.top, 16)
             }
-            .padding(.horizontal, 24)
-            .padding(.vertical, 20)
+
+            IncrementalTranscriptView(
+                prompt: model.outputPromptText,
+                output: model.outputText,
+                mailbox: model.generationTranscriptMailbox,
+                isTerminal: !model.isRunning,
+                showsPrefillPlaceholder: model.isRunning
+                    && model.phase == .prefill)
+                .frame(maxWidth: .infinity, maxHeight: .infinity)
+                .overlay(alignment: .topTrailing) {
+                    if !model.isRunning && !model.outputResponsePlainText.isEmpty {
+                        copyResponseButton
+                            .padding(8)
+                    }
+                }
+                .padding(.horizontal, 24)
+                .padding(.vertical, 16)
+        }
     }
 
     private var copyResponseButton: some View {
@@ -182,6 +192,60 @@ struct OutputPaneView: View {
     }
 }
 
+private struct ThinkingPanel: View {
+    let blocks: [AppThinkingBlock]
+    let isThinking: Bool
+    @State private var isExpanded = true
+
+    var body: some View {
+        DisclosureGroup(isExpanded: $isExpanded) {
+            ScrollView {
+                VStack(alignment: .leading, spacing: 10) {
+                    ForEach(Array(blocks.enumerated()), id: \.element.id) { index, block in
+                        if blocks.count > 1 {
+                            Text("Thought \(index + 1)")
+                                .font(.caption.weight(.semibold))
+                                .foregroundStyle(.tertiary)
+                        }
+                        Text(block.text.isEmpty ? "Starting…" : block.text)
+                            .font(.callout)
+                            .foregroundStyle(.secondary)
+                            .textSelection(.enabled)
+                            .frame(maxWidth: .infinity, alignment: .leading)
+                        if index < blocks.count - 1 { Divider() }
+                    }
+                }
+                .padding(.top, 10)
+            }
+            .frame(maxHeight: 180)
+        } label: {
+            HStack(spacing: 8) {
+                Image(systemName: "brain")
+                    .foregroundStyle(TurboFieldfareMacTheme.accentColor)
+                Text(isThinking ? "Thinking…" : "Thinking")
+                    .font(.callout.weight(.medium))
+                Spacer()
+                if blocks.count > 1 {
+                    Text("\(blocks.count) blocks")
+                        .font(.caption)
+                        .foregroundStyle(.tertiary)
+                }
+            }
+        }
+        .padding(14)
+        .background(
+            Color(nsColor: .controlBackgroundColor).opacity(0.72),
+            in: .rect(cornerRadius: 12))
+        .overlay {
+            RoundedRectangle(cornerRadius: 12)
+                .stroke(TurboFieldfareMacTheme.subtleDivider, lineWidth: 0.75)
+        }
+        .onChange(of: isThinking) { _, active in
+            if active { isExpanded = true }
+        }
+    }
+}
+
 private struct EmptyPlaceholderIcon: View {
     let systemName: String
 
diff --git a/Sources/TurboFieldfareApp/Mac/Generation/PromptComposerView.swift b/Sources/TurboFieldfareApp/Mac/Generation/PromptComposerView.swift
index 00e5c84..ca414df 100644
--- a/Sources/TurboFieldfareApp/Mac/Generation/PromptComposerView.swift
+++ b/Sources/TurboFieldfareApp/Mac/Generation/PromptComposerView.swift
@@ -1,9 +1,13 @@
+import AppKit
 import TurboFieldfareAppCore
+import TurboFieldfareMacPresentation
 import SwiftUI
 
 struct PromptComposerView: View {
     @Bindable var model: AppModel
+    let onSelectModel: (AppModelInstallDescriptor) -> Void
     @FocusState private var promptFocused: Bool
+    @State private var showingContextActions = false
     @State private var showingPromptTips = false
 
     var body: some View {
@@ -11,94 +15,200 @@ struct PromptComposerView: View {
             editor
             footer
         }
-        .padding(14)
+        .padding(12)
         .background {
-            RoundedRectangle(cornerRadius: 22)
+            RoundedRectangle(cornerRadius: 20)
                 .fill(Color(nsColor: .controlBackgroundColor))
                 .overlay {
-                    RoundedRectangle(cornerRadius: 22)
-                        .stroke(.separator.opacity(0.5), lineWidth: 0.5)
+                    RoundedRectangle(cornerRadius: 20)
+                        .stroke(TurboFieldfareMacTheme.subtleDivider, lineWidth: 0.8)
                 }
+                .shadow(color: .black.opacity(0.08), radius: 10, y: 4)
         }
     }
 
     private var editor: some View {
-        TextEditor(text: $model.promptText)
+        TextField("Ask anything…", text: $model.promptText, axis: .vertical)
             .accessibilityLabel("Prompt")
             .font(.body)
-            .scrollContentBackground(.hidden)
+            .textFieldStyle(.plain)
+            .lineLimit(1...6)
             .focused($promptFocused)
-            .frame(height: editorHeight)
-            .overlay(alignment: .topLeading) {
-                if model.promptText.isEmpty {
-                    // Matches the NSTextView text origin: 5pt line fragment
-                    // padding, no vertical inset.
-                    Text("Prompt")
-                        .font(.body)
-                        .foregroundStyle(.tertiary)
-                        .padding(.leading, 5)
-                        .allowsHitTesting(false)
-                }
-            }
-    }
-
-    private var editorHeight: CGFloat {
-        model.promptText.isEmpty ? 46 : 84
+            .frame(minHeight: 22, alignment: .topLeading)
     }
 
     private var footer: some View {
         HStack(spacing: 10) {
+            contextMenu
             promptTips
-            Spacer()
             clearAction
+            Spacer(minLength: 14)
+            modelSelector
             GenerateControl(model: model)
         }
     }
 
+    private var contextMenu: some View {
+        Button {
+            showingContextActions.toggle()
+        } label: {
+            ZStack {
+                Circle().fill(Color.primary.opacity(0.045))
+                Circle().stroke(
+                    Color(nsColor: .separatorColor).opacity(0.65), lineWidth: 0.8)
+                Image(systemName: "plus")
+            }
+            .frame(width: 40, height: 40)
+            .contentShape(Circle())
+            .accessibilityLabel("Add context")
+        }
+        .buttonStyle(.borderless)
+        .foregroundStyle(Color(nsColor: .labelColor))
+        .help(
+            "Add context · "
+                + model.installDescriptor.runtimeCapabilities.inputSummary)
+        .popover(
+            isPresented: $showingContextActions,
+            attachmentAnchor: .point(.top),
+            arrowEdge: .top
+        ) {
+            contextActions
+        }
+    }
+
+    private var contextActions: some View {
+        VStack(alignment: .leading, spacing: 12) {
+            Text("Add context").font(.headline)
+            Button {
+                pasteClipboard()
+                showingContextActions = false
+            } label: {
+                Label("Paste text from Clipboard", systemImage: "doc.on.clipboard")
+            }
+            .disabled(clipboardText == nil)
+
+            if !model.promptText.isEmpty {
+                Button(role: .destructive) {
+                    model.promptText = ""
+                    promptFocused = true
+                    showingContextActions = false
+                } label: {
+                    Label("Clear prompt", systemImage: "trash")
+                }
+            }
+
+            Divider()
+            modalityRow(.text, symbol: "text.alignleft")
+            modalityRow(.image, symbol: "photo")
+            modalityRow(.audio, symbol: "waveform")
+            Text(model.installDescriptor.runtimeCapabilities.detail)
+                .font(.caption)
+                .foregroundStyle(.secondary)
+                .fixedSize(horizontal: false, vertical: true)
+        }
+        .frame(width: 300, alignment: .leading)
+        .padding(16)
+    }
+
+    private func modalityRow(_ modality: AppModelInputModality, symbol: String) -> some View {
+        HStack {
+            Label("\(modality.label) input", systemImage: symbol)
+            Spacer()
+            Text(
+                model.installDescriptor.runtimeCapabilities.supports(modality)
+                    ? "Supported"
+                    : "Unavailable")
+                .foregroundStyle(
+                    model.installDescriptor.runtimeCapabilities.supports(modality)
+                        ? TurboFieldfareMacTheme.accentColor
+                        : Color(nsColor: .secondaryLabelColor))
+        }
+        .font(.callout)
+    }
+
     private var promptTips: some View {
         Button {
             showingPromptTips.toggle()
         } label: {
-            Label("Prompt tips", systemImage: "questionmark.circle")
+            Label("Prompt tips", systemImage: "lightbulb")
                 .labelStyle(.iconOnly)
-                .frame(width: 28, height: 28)
+                .frame(width: 40, height: 40)
                 .contentShape(Circle())
         }
         .buttonStyle(.borderless)
-        .foregroundStyle(.secondary)
+        .foregroundStyle(Color(nsColor: .labelColor))
+        .background(Color.primary.opacity(0.045), in: .circle)
+        .overlay {
+            Circle().stroke(TurboFieldfareMacTheme.subtleDivider, lineWidth: 0.6)
+        }
         .help("Prompt tips")
-        .popover(isPresented: $showingPromptTips,
-                 attachmentAnchor: .point(.top),
-                 arrowEdge: .top) {
+        .popover(
+            isPresented: $showingPromptTips,
+            attachmentAnchor: .point(.top),
+            arrowEdge: .top
+        ) {
             promptGuide
         }
     }
 
+    private var modelSelector: some View {
+        Menu {
+            ForEach(
+                AppModelInstallDescriptor.selectionChoices(
+                    including: model.installDescriptor),
+                id: \.selectionKey
+            ) { descriptor in
+                Button {
+                    onSelectModel(descriptor)
+                } label: {
+                    if descriptor == model.installDescriptor {
+                        Label(descriptor.displayName, systemImage: "checkmark")
+                    } else {
+                        Text(descriptor.displayName)
+                    }
+                }
+                .disabled(model.isRunning || model.isInstallingModel)
+            }
+        } label: {
+            ZStack {
+                Capsule().fill(Color(nsColor: .windowBackgroundColor))
+                Capsule().stroke(
+                    Color(nsColor: .separatorColor).opacity(0.65), lineWidth: 0.8)
+                Text(model.installDescriptor.displayName)
+                    .lineLimit(1)
+                    .font(.callout)
+                    .padding(.horizontal, 12)
+            }
+            .frame(height: 40)
+        }
+        .menuStyle(.borderlessButton)
+        .tint(Color(nsColor: .labelColor))
+    }
+
     private var promptGuide: some View {
         VStack(alignment: .leading, spacing: 14) {
-            Text("Prompting this model")
-                .font(.headline)
-
-            tipSection("Ask for a clear task",
-                       "Say what you want the model to create, explain, plan, or transform. Put the essential context in the same prompt.")
-            tipSection("Shape the answer",
-                       "Specify a useful length, sections, tone, or output format. Concrete constraints work better than a long list of vague preferences.")
-            tipSection("Anchor important facts",
-                       "Include facts the answer must preserve and say what should be checked. Generated factual claims can still be wrong or outdated.")
-            tipSection("For code and calculations",
-                       "Provide types, dimensions, interfaces, edge cases, or a small scaffold. Compile or run the result before relying on it.")
-            tipSection("Try a focused revision",
-                       "If the answer drifts, shorten the task and make the missing requirement explicit. The default temperature is 0.20 for steadier responses.")
+            Text("Prompting this model").font(.headline)
+            tipSection(
+                "Ask for a clear task",
+                "Say what you want the model to create, explain, plan, or transform."
+            )
+            tipSection(
+                "Shape the answer",
+                "Specify a useful length, sections, tone, or output format."
+            )
+            tipSection(
+                "Anchor important facts",
+                "Include facts the answer must preserve and what should be checked."
+            )
         }
         .font(.callout)
-        .frame(width: 390, alignment: .leading)
+        .frame(width: 360, alignment: .leading)
         .padding(18)
     }
 
     private func tipSection(_ title: String, _ detail: String) -> some View {
         VStack(alignment: .leading, spacing: 3) {
-            Text(title)
-                .fontWeight(.semibold)
+            Text(title).fontWeight(.semibold)
             Text(detail)
                 .foregroundStyle(.secondary)
                 .fixedSize(horizontal: false, vertical: true)
@@ -108,29 +218,29 @@ struct PromptComposerView: View {
     @ViewBuilder
     private var clearAction: some View {
         if !model.isRunning && model.hasOutputTranscript {
-            Button {
-                model.clearOutput()
-            } label: {
+            Button(action: model.clearOutput) {
                 Label("Clear output", systemImage: "trash")
                     .labelStyle(.iconOnly)
-                    .frame(width: 28, height: 28)
+                    .frame(width: 40, height: 40)
                     .contentShape(Circle())
             }
             .buttonStyle(.borderless)
+            .foregroundStyle(.secondary)
             .help("Clear output")
-        } else if !model.isRunning && !model.promptText.isEmpty {
-            Button {
-                model.promptText = ""
-                promptFocused = true
-            } label: {
-                Label("Clear prompt", systemImage: "xmark.circle.fill")
-                    .labelStyle(.iconOnly)
-                    .symbolRenderingMode(.hierarchical)
-                    .frame(width: 28, height: 28)
-                    .contentShape(Circle())
-            }
-            .buttonStyle(.borderless)
-            .help("Clear prompt")
         }
     }
+
+    private var clipboardText: String? {
+        NSPasteboard.general.string(forType: .string)
+    }
+
+    private func pasteClipboard() {
+        guard let text = clipboardText, !text.isEmpty else { return }
+        if model.promptText.isEmpty {
+            model.promptText = text
+        } else {
+            model.promptText += "\n\n" + text
+        }
+        promptFocused = true
+    }
 }
diff --git a/Sources/TurboFieldfareApp/Mac/Installation/ModelInstallView.swift b/Sources/TurboFieldfareApp/Mac/Installation/ModelInstallView.swift
index 97f5ca3..141ed63 100644
--- a/Sources/TurboFieldfareApp/Mac/Installation/ModelInstallView.swift
+++ b/Sources/TurboFieldfareApp/Mac/Installation/ModelInstallView.swift
@@ -42,10 +42,15 @@ struct ModelInstallView: View {
             Text("Model required")
                 .font(.title.bold())
                 .accessibilityHeading(.h1)
-            Text("TurboFieldfare needs \(model.installDescriptor.displayName) before it can generate text.")
+            Text("ElasticUMA needs \(model.installDescriptor.displayName) before it can chat locally.")
                 .font(.body)
                 .foregroundStyle(.secondary)
                 .multilineTextAlignment(.center)
+            Label(
+                model.installDescriptor.runtimeCapabilities.inputSummary,
+                systemImage: "text.alignleft")
+                .font(.callout)
+                .foregroundStyle(.secondary)
         }
     }
 
@@ -177,7 +182,7 @@ struct ModelInstallView: View {
                 .buttonStyle(.bordered)
                 .disabled(model.isInstallingModel)
 
-                Button(model.hasPartialModelDownload ? "Resume" : "Download",
+                Button(model.hasPartialModelDownload ? "Resume Install" : "Install Model",
                        action: model.installModel)
                     .buttonStyle(.borderedProminent)
                     .disabled(!model.canInstallModel)
diff --git a/Sources/TurboFieldfareApp/MacPresentation/TurboFieldfareMacTheme.swift b/Sources/TurboFieldfareApp/MacPresentation/TurboFieldfareMacTheme.swift
index 8bf98dd..cfa40df 100644
--- a/Sources/TurboFieldfareApp/MacPresentation/TurboFieldfareMacTheme.swift
+++ b/Sources/TurboFieldfareApp/MacPresentation/TurboFieldfareMacTheme.swift
@@ -3,10 +3,34 @@ import SwiftUI
 
 public enum TurboFieldfareMacTheme {
     public static let accentNSColor = NSColor(
-        srgbRed: 106.0 / 255.0,
-        green: 186.0 / 255.0,
-        blue: 113.0 / 255.0,
+        srgbRed: 45.0 / 255.0,
+        green: 139.0 / 255.0,
+        blue: 63.0 / 255.0,
         alpha: 1)
 
     public static let accentColor = Color(nsColor: accentNSColor)
+
+    public static let sidebarNSColor = NSColor(
+        name: "ElasticUMA.Sidebar"
+    ) { appearance in
+        appearance.bestMatch(from: [.darkAqua, .aqua]) == .darkAqua
+            ? NSColor(srgbRed: 30.0 / 255.0, green: 32.0 / 255.0,
+                      blue: 30.0 / 255.0, alpha: 1)
+            : NSColor(srgbRed: 248.0 / 255.0, green: 247.0 / 255.0,
+                      blue: 243.0 / 255.0, alpha: 1)
+    }
+
+    public static let inspectorNSColor = NSColor(
+        name: "ElasticUMA.Inspector"
+    ) { appearance in
+        appearance.bestMatch(from: [.darkAqua, .aqua]) == .darkAqua
+            ? NSColor(srgbRed: 25.0 / 255.0, green: 26.0 / 255.0,
+                      blue: 25.0 / 255.0, alpha: 1)
+            : NSColor(srgbRed: 252.0 / 255.0, green: 252.0 / 255.0,
+                      blue: 250.0 / 255.0, alpha: 1)
+    }
+
+    public static let sidebarColor = Color(nsColor: sidebarNSColor)
+    public static let inspectorColor = Color(nsColor: inspectorNSColor)
+    public static let subtleDivider = Color(nsColor: .separatorColor).opacity(0.45)
 }
diff --git a/Sources/TurboFieldfareCLI/Args.swift b/Sources/TurboFieldfareCLI/Args.swift
index a494205..711a50c 100644
--- a/Sources/TurboFieldfareCLI/Args.swift
+++ b/Sources/TurboFieldfareCLI/Args.swift
@@ -12,6 +12,7 @@ public enum PrefillChunkChoice: Equatable, Sendable {
 public struct Args: Equatable, Sendable {
     public var model: String
     public var prompt: String?
+    public var chatPrompt: String?
     public var messagesFile: String?
     public var maxNew: Int
     public var maxContext: Int
@@ -31,6 +32,7 @@ public struct Args: Equatable, Sendable {
 
     public init(model: String,
                 prompt: String? = nil,
+                chatPrompt: String? = nil,
                 messagesFile: String? = nil,
                 maxNew: Int = 1_024,
                 maxContext: Int = 4096,
@@ -49,6 +51,7 @@ public struct Args: Equatable, Sendable {
                 kvSnapshot: String? = nil) {
         self.model = model
         self.prompt = prompt
+        self.chatPrompt = chatPrompt
         self.messagesFile = messagesFile
         self.maxNew = maxNew
         self.maxContext = maxContext
@@ -85,7 +88,8 @@ public enum ArgsError: Error, Equatable, CustomStringConvertible {
         case .invalidValue(let flag, let value): return "invalid value for \(flag): \(value)"
         case .requiredMissing(let flag): return "required flag missing: \(flag)"
         case .mutuallyExclusive(let a, let b): return "\(a) and \(b) are mutually exclusive"
-        case .modeMissing: return "one of --prompt or --messages-file is required"
+        case .modeMissing:
+            return "one of --chat-prompt, --prompt, or --messages-file is required"
         }
     }
 }
@@ -94,10 +98,11 @@ extension Args {
     public static let usage = """
     TurboFieldfareCLI — Gemma 4 26B-A4B / Qwen3.6 35B-A3B text generation
 
-    usage: TurboFieldfareCLI --model <dir> (--prompt <string> | --messages-file <path>) [options]
+    usage: TurboFieldfareCLI --model <dir> (--chat-prompt <string> | --prompt <string> | --messages-file <path>) [options]
 
     required:
       --model <dir>             Path to a .gturbo model directory.
+      --chat-prompt <string>    One user message with thinking disabled.
       --prompt <string>         Raw-completion prompt.
       --messages-file <path>    JSON chat messages with role and content fields.
 
@@ -141,6 +146,7 @@ extension Args {
     public static func parse(_ argv: [String]) throws -> Args {
         var model: String?
         var prompt: String?
+        var chatPrompt: String?
         var messagesFile: String?
         var maxNew = 1_024
         var maxContext = 4096
@@ -188,6 +194,8 @@ extension Args {
                 model = try takeValue(argv, &index, flag: flag)
             case "--prompt":
                 prompt = try takeValue(argv, &index, flag: flag)
+            case "--chat-prompt":
+                chatPrompt = try takeValue(argv, &index, flag: flag)
             case "--messages-file":
                 messagesFile = try takeValue(argv, &index, flag: flag)
             case "--max-new":
@@ -265,10 +273,15 @@ extension Args {
         }
 
         guard let model else { throw ArgsError.requiredMissing("--model") }
-        if prompt != nil && messagesFile != nil {
-            throw ArgsError.mutuallyExclusive("--prompt", "--messages-file")
+        let modes = [
+            ("--chat-prompt", chatPrompt),
+            ("--prompt", prompt),
+            ("--messages-file", messagesFile),
+        ].filter { $0.1 != nil }
+        if modes.count > 1 {
+            throw ArgsError.mutuallyExclusive(modes[0].0, modes[1].0)
         }
-        if prompt == nil && messagesFile == nil { throw ArgsError.modeMissing }
+        if modes.isEmpty { throw ArgsError.modeMissing }
         if temperature > 0, topK == nil, let topP, topP < 1 {
             throw ArgsError.invalidValue(
                 flag: "--top-p",
@@ -283,6 +296,7 @@ extension Args {
         }
         return Args(model: model,
                     prompt: prompt,
+                    chatPrompt: chatPrompt,
                     messagesFile: messagesFile,
                     maxNew: maxNew,
                     maxContext: maxContext,
diff --git a/Sources/TurboFieldfareCLI/Run.swift b/Sources/TurboFieldfareCLI/Run.swift
index 11c7314..2dd08d6 100644
--- a/Sources/TurboFieldfareCLI/Run.swift
+++ b/Sources/TurboFieldfareCLI/Run.swift
@@ -21,6 +21,11 @@ public func run(args: Args,
         let promptIds: [Int32]
         if let rawPrompt = args.prompt {
             promptIds = tokenizer.encode(rawPrompt, addBOS: true)
+        } else if let chatPrompt = args.chatPrompt {
+            let rendered = try tokenizer.applyChatTemplateWithoutThinking([
+                GFTokenizer.Message(role: .user, content: chatPrompt),
+            ])
+            promptIds = tokenizer.encode(rendered, addBOS: false)
         } else if let messagesFile = args.messagesFile {
             let data = try Data(contentsOf: URL(fileURLWithPath: messagesFile),
                                 options: [.mappedIfSafe])
diff --git a/Sources/TurboFieldfareDecodeProtocol/DecodeProtocol.swift b/Sources/TurboFieldfareDecodeProtocol/DecodeProtocol.swift
index 8f23b6f..c7afbb4 100644
--- a/Sources/TurboFieldfareDecodeProtocol/DecodeProtocol.swift
+++ b/Sources/TurboFieldfareDecodeProtocol/DecodeProtocol.swift
@@ -7,6 +7,8 @@ public struct DecodeRuntimeOptions: Codable, Sendable, Equatable {
     public var prefillChunkTokens: Int
     public var rdadvisePolicy: String
     public var modelVerification: String
+    public var expertCacheResidency: String
+    public var expertCacheHotSlots: Int
 
     /// These defaults are decorative: the only production construction
     /// (DecodeServiceInferenceClient) passes every field explicitly from
@@ -14,18 +16,22 @@ public struct DecodeRuntimeOptions: Codable, Sendable, Equatable {
     /// because this module deliberately has no dependencies — it is the wire
     /// format. RuntimeDefaultsDriftTests pins them to the canonical values,
     /// so they cannot rot silently.
-    public init(expertCacheSlots: Int = 64,
+    public init(expertCacheSlots: Int = 96,
                 expertCachePolicy: String = "lfu",
                 prefillEnabled: Bool = true,
                 prefillChunkTokens: Int = 128,
                 rdadvisePolicy: String = "off",
-                modelVerification: String = "full-sha256") {
+                modelVerification: String = "full-sha256",
+                expertCacheResidency: String = "os-managed",
+                expertCacheHotSlots: Int = 16) {
         self.expertCacheSlots = expertCacheSlots
         self.expertCachePolicy = expertCachePolicy
         self.prefillEnabled = prefillEnabled
         self.prefillChunkTokens = prefillChunkTokens
         self.rdadvisePolicy = rdadvisePolicy
         self.modelVerification = modelVerification
+        self.expertCacheResidency = expertCacheResidency
+        self.expertCacheHotSlots = expertCacheHotSlots
     }
 }
 
@@ -54,11 +60,13 @@ public struct DecodeGenerationRequest: Codable, Sendable {
     public var maxContextTokens: Int
     public var temperature: Float
     public var repetitionPenalty: Float
+    public var thinkingEnabled: Bool
     public var runtimeOptions: DecodeRuntimeOptions
     public var generationID: UUID
 
     public init(prompt: String, maxNewTokens: Int, maxContextTokens: Int,
                 temperature: Float, repetitionPenalty: Float = 1,
+                thinkingEnabled: Bool = false,
                 runtimeOptions: DecodeRuntimeOptions = DecodeRuntimeOptions(),
                 generationID: UUID = UUID()) {
         self.prompt = prompt
@@ -66,6 +74,7 @@ public struct DecodeGenerationRequest: Codable, Sendable {
         self.maxContextTokens = maxContextTokens
         self.temperature = temperature
         self.repetitionPenalty = repetitionPenalty
+        self.thinkingEnabled = thinkingEnabled
         self.runtimeOptions = runtimeOptions
         self.generationID = generationID
     }
@@ -100,6 +109,10 @@ public struct DecodeRunnerDiagnostics: Codable, Sendable, Equatable {
     public var rdadviseMegabytesPerToken: Double
     public var rdadviseSkippedPerToken: Double
     public var rdadviseFailures: UInt64
+    public var expertHotSlots: Int?
+    public var expertVolatileTransitions: UInt64
+    public var expertEmptyRecoveries: UInt64
+    public var expertReclaimedBytes: UInt64
 
     public init(cb1MillisecondsPerToken: Double,
                 ioMillisecondsPerToken: Double,
@@ -109,7 +122,11 @@ public struct DecodeRunnerDiagnostics: Codable, Sendable, Equatable {
                 rdadviseCallsPerToken: Double,
                 rdadviseMegabytesPerToken: Double,
                 rdadviseSkippedPerToken: Double,
-                rdadviseFailures: UInt64) {
+                rdadviseFailures: UInt64,
+                expertHotSlots: Int? = nil,
+                expertVolatileTransitions: UInt64 = 0,
+                expertEmptyRecoveries: UInt64 = 0,
+                expertReclaimedBytes: UInt64 = 0) {
         self.cb1MillisecondsPerToken = cb1MillisecondsPerToken
         self.ioMillisecondsPerToken = ioMillisecondsPerToken
         self.cb2MillisecondsPerToken = cb2MillisecondsPerToken
@@ -119,6 +136,10 @@ public struct DecodeRunnerDiagnostics: Codable, Sendable, Equatable {
         self.rdadviseMegabytesPerToken = rdadviseMegabytesPerToken
         self.rdadviseSkippedPerToken = rdadviseSkippedPerToken
         self.rdadviseFailures = rdadviseFailures
+        self.expertHotSlots = expertHotSlots
+        self.expertVolatileTransitions = expertVolatileTransitions
+        self.expertEmptyRecoveries = expertEmptyRecoveries
+        self.expertReclaimedBytes = expertReclaimedBytes
     }
 }
 
@@ -145,6 +166,7 @@ public struct DecodeServiceEvent: Codable, Sendable {
     public var generationID: UUID
     public var sequence: UInt64
     public var textDelta: String
+    public var tokenChannel: String?
     public var tokenCount: Int
     public var promptTokenCount: Int?
     public var prefillDone: Int?
@@ -162,6 +184,7 @@ public struct DecodeServiceEvent: Codable, Sendable {
 
     public init(kind: DecodeServiceEventKind, generationID: UUID,
                 sequence: UInt64 = 0, textDelta: String = "",
+                tokenChannel: String? = nil,
                 tokenCount: Int = 0, promptTokenCount: Int? = nil,
                 prefillDone: Int? = nil, prefillTotal: Int? = nil,
                 prefillSeconds: Double? = nil,
@@ -175,6 +198,7 @@ public struct DecodeServiceEvent: Codable, Sendable {
         self.generationID = generationID
         self.sequence = sequence
         self.textDelta = textDelta
+        self.tokenChannel = tokenChannel
         self.tokenCount = tokenCount
         self.promptTokenCount = promptTokenCount
         self.prefillDone = prefillDone
diff --git a/Sources/TurboFieldfareDecodeService/DecodeServiceOutbox.swift b/Sources/TurboFieldfareDecodeService/DecodeServiceOutbox.swift
index ca48f53..c8af13b 100644
--- a/Sources/TurboFieldfareDecodeService/DecodeServiceOutbox.swift
+++ b/Sources/TurboFieldfareDecodeService/DecodeServiceOutbox.swift
@@ -10,9 +10,8 @@ final class DecodeServiceOutbox: @unchecked Sendable {
     }
 
     private struct State {
-        var pendingText = ""
+        var pendingTokens: [AppTokenEvent] = []
         var latestPrefill: PrefillProgress?
-        var latestToken: AppTokenEvent?
         var terminal: DecodeServiceEvent?
         var finished = false
         var sequence: UInt64 = 0
@@ -35,8 +34,8 @@ final class DecodeServiceOutbox: @unchecked Sendable {
             state.latestPrefill = PrefillProgress(done: done, total: total)
             condition.signal()
         case .token(let token):
-            state.pendingText += token.textDelta
-            state.latestToken = token
+            state.pendingTokens.append(token)
+            condition.signal()
         case .finished(let diagnostics):
             state.terminal = terminal(.finished, diagnostics: diagnostics)
         case .cancelled(let diagnostics):
@@ -67,23 +66,22 @@ final class DecodeServiceOutbox: @unchecked Sendable {
                 _ = condition.wait(until: Date().addingTimeInterval(0.1))
             }
             let prefill = state.latestPrefill
-            let text = state.pendingText
-            let token = state.latestToken
+            let tokens = state.pendingTokens
             let terminal = state.terminal
             let done = state.finished
             state.latestPrefill = nil
-            state.pendingText = ""
-            state.latestToken = nil
+            state.pendingTokens = []
             state.terminal = nil
             var prefillSequence: UInt64?
             if prefill != nil {
                 state.sequence &+= 1
                 prefillSequence = state.sequence
             }
-            var tokenSequence: UInt64?
-            if !text.isEmpty || token != nil {
+            var tokenSequences: [UInt64] = []
+            tokenSequences.reserveCapacity(tokens.count)
+            for _ in tokens {
                 state.sequence &+= 1
-                tokenSequence = state.sequence
+                tokenSequences.append(state.sequence)
             }
             condition.unlock()
 
@@ -94,15 +92,19 @@ final class DecodeServiceOutbox: @unchecked Sendable {
                     prefillDone: prefill.done, prefillTotal: prefill.total)
                 try handle.write(contentsOf: DecodeFrameCodec.encode(snapshot))
             }
-            if !text.isEmpty || token != nil {
-                let elapsed = token?.elapsedDecodeSeconds ?? 0
-                let count = (token?.index ?? -1) + 1
+            let currentMemory = tokens.isEmpty ? nil : memorySampler.sample()
+            for (token, tokenSequence) in zip(tokens, tokenSequences) {
+                let elapsed = token.elapsedDecodeSeconds
+                let count = token.index + 1
                 let snapshot = DecodeServiceEvent(
                     kind: .snapshot, generationID: generationID,
-                    sequence: tokenSequence ?? 0, textDelta: text, tokenCount: count,
+                    sequence: tokenSequence,
+                    textDelta: token.textDelta,
+                    tokenChannel: token.channel.rawValue,
+                    tokenCount: count,
                     decodeSeconds: elapsed,
                     tokensPerSecond: elapsed > 0 ? Double(count) / elapsed : 0,
-                    currentMemoryBytes: memorySampler.sample(),
+                    currentMemoryBytes: currentMemory,
                     peakMemoryBytes: memorySampler.peakBytes)
                 try handle.write(contentsOf: DecodeFrameCodec.encode(snapshot))
             }
@@ -154,6 +156,10 @@ final class DecodeServiceOutbox: @unchecked Sendable {
             rdadviseCallsPerToken: value.rdadviseCallsPerToken,
             rdadviseMegabytesPerToken: value.rdadviseMegabytesPerToken,
             rdadviseSkippedPerToken: value.rdadviseSkippedPerToken,
-            rdadviseFailures: value.rdadviseFailures)
+            rdadviseFailures: value.rdadviseFailures,
+            expertHotSlots: value.expertHotSlots,
+            expertVolatileTransitions: value.expertVolatileTransitions,
+            expertEmptyRecoveries: value.expertEmptyRecoveries,
+            expertReclaimedBytes: value.expertReclaimedBytes)
     }
 }
diff --git a/Sources/TurboFieldfareDecodeService/Entry.swift b/Sources/TurboFieldfareDecodeService/Entry.swift
index 202be99..1958618 100644
--- a/Sources/TurboFieldfareDecodeService/Entry.swift
+++ b/Sources/TurboFieldfareDecodeService/Entry.swift
@@ -103,6 +103,7 @@ import TurboFieldfareDecodeProtocol
                         maxContextTokens: request.maxContextTokens,
                         temperature: request.temperature,
                         repetitionPenalty: request.repetitionPenalty,
+                        thinkingEnabled: request.thinkingEnabled,
                         runtimeOptions: options)
                     for try await event in client.generate(generation) {
                         outbox.publish(event)
@@ -163,13 +164,20 @@ import TurboFieldfareDecodeProtocol
             throw AppInferenceError.invalidRequest(
                 "unknown model verification \(options.modelVerification)")
         }
+        guard let expertCacheResidency = AppExpertCacheResidency(
+            rawValue: options.expertCacheResidency) else {
+            throw AppInferenceError.invalidRequest(
+                "unknown expert cache residency \(options.expertCacheResidency)")
+        }
         let resolved = AppRuntimeOptions(
             expertCacheSlots: options.expertCacheSlots,
             expertCachePolicy: cachePolicy,
             prefillEnabled: options.prefillEnabled,
             prefillChunkTokens: options.prefillChunkTokens,
             rdadvisePolicy: rdadvisePolicy,
-            modelVerification: modelVerification)
+            modelVerification: modelVerification,
+            expertCacheResidency: expertCacheResidency,
+            expertCacheHotSlots: options.expertCacheHotSlots)
         try resolved.validate()
         return resolved
     }
diff --git a/Sources/TurboFieldfareRepack/Core/Remote/SupportedModelSource.swift b/Sources/TurboFieldfareRepack/Core/Remote/SupportedModelSource.swift
index c33ca8a..6ecb864 100644
--- a/Sources/TurboFieldfareRepack/Core/Remote/SupportedModelSource.swift
+++ b/Sources/TurboFieldfareRepack/Core/Remote/SupportedModelSource.swift
@@ -57,7 +57,7 @@ public struct SupportedModelSource: Sendable, Equatable {
         sourceIndexSHA256:
             "0b28df60e33753a14e816d3b31577ae2c93884c58430a4a6de6ae9ea483842ea",
         modelID: "qwen3.6-35b-a3b-4bit",
-        approximateDownloadBytes: 19_529_025_048,
+        approximateDownloadBytes: 20_146_183_200,
         installedBytes: 19_546_491_213,
         reserveBytes: 1_073_741_824)
 
diff --git a/Sources/TurboFieldfareServer/Core/ServerInference.swift b/Sources/TurboFieldfareServer/Core/ServerInference.swift
index 10bbb98..1394d8d 100644
--- a/Sources/TurboFieldfareServer/Core/ServerInference.swift
+++ b/Sources/TurboFieldfareServer/Core/ServerInference.swift
@@ -256,7 +256,7 @@ public actor ServerModelSession: ServerInferenceBackend {
         if needsToolTemplate {
             promptIDs = try tokenizer.encodeToolChat(messages: request.messages, tools: request.tools)
         } else {
-            let rendered = try tokenizer.applyChatTemplate(request.messages)
+            let rendered = try tokenizer.applyChatTemplateWithoutThinking(request.messages)
             promptIDs = tokenizer.encode(rendered, addBOS: false)
         }
         let renderSeconds = Date().timeIntervalSince(renderStart)
@@ -305,11 +305,11 @@ public actor ServerModelSession: ServerInferenceBackend {
             maxContext - effectivePromptIDs.count)
         config.stopStrings = []
 
-        // ChatML's generation prompt leaves `<think>` open, so a decoder is
-        // needed even without tools: nothing else would keep reasoning out of
-        // the client's content stream.
-        let thinkingPreopened = tokenizer.dialect == .chatml
-        let decoder = needsToolTemplate || thinkingPreopened
+        // Tool chat keeps Qwen thinking enabled; simple text requests use the
+        // closed-think template for lower latency. A ChatML decoder remains in
+        // place so unexpected/interleaved think spans never leak as content.
+        let thinkingPreopened = tokenizer.dialect == .chatml && needsToolTemplate
+        let decoder = needsToolTemplate || tokenizer.dialect == .chatml
             ? StructuredAssistantDecoder(
                 tokenizer: tokenizer,
                 allowedTools: Set(request.tools.map(\.name)),
@@ -354,6 +354,8 @@ public actor ServerModelSession: ServerInferenceBackend {
                             case .toolCall(let call):
                                 calls.append(call)
                                 onEvent(.toolCall(call))
+                            case .reasoningStarted, .reasoning, .reasoningEnded:
+                                break
                             }
                         }
                     case .tail(let text):
diff --git a/Tests/TurboFieldfare/Core/CLI/CLIArgumentsTests.swift b/Tests/TurboFieldfare/Core/CLI/CLIArgumentsTests.swift
index c044ec0..c75acc0 100644
--- a/Tests/TurboFieldfare/Core/CLI/CLIArgumentsTests.swift
+++ b/Tests/TurboFieldfare/Core/CLI/CLIArgumentsTests.swift
@@ -6,6 +6,7 @@ import Testing
         let arguments = try Args.parse(["--model", "m.gturbo", "--prompt", "hi"])
         #expect(arguments.model == "m.gturbo")
         #expect(arguments.prompt == "hi")
+        #expect(arguments.chatPrompt == nil)
         #expect(arguments.messagesFile == nil)
         #expect(arguments.maxNew == 1_024)
         #expect(arguments.maxContext == 4096)
@@ -64,7 +65,8 @@ import Testing
 
     @Test func helpListsExactlyThePublicOptions() {
         let expected: Set<String> = [
-            "--model", "--prompt", "--messages-file", "--max-new", "--max-context",
+            "--model", "--chat-prompt", "--prompt", "--messages-file",
+            "--max-new", "--max-context",
             "--temperature", "--top-k", "--top-p", "--repetition-penalty",
             "--seed", "--stop", "--quiet", "--help",
             "--rdadvise", "--expert-cache-slots", "--prefill-chunk",
@@ -120,6 +122,15 @@ import Testing
         #expect(arguments.messagesFile == "chat.json")
     }
 
+    @Test func chatPromptSelectsNonThinkingChatMode() throws {
+        let arguments = try Args.parse([
+            "--model", "m.gturbo", "--chat-prompt", "hello",
+        ])
+        #expect(arguments.chatPrompt == "hello")
+        #expect(arguments.prompt == nil)
+        #expect(arguments.messagesFile == nil)
+    }
+
     @Test func promptAndMessagesFileAreMutuallyExclusive() {
         #expect(throws: ArgsError.mutuallyExclusive("--prompt", "--messages-file")) {
             _ = try Args.parse([
@@ -127,6 +138,12 @@ import Testing
                 "--messages-file", "chat.json",
             ])
         }
+        #expect(throws: ArgsError.mutuallyExclusive("--chat-prompt", "--prompt")) {
+            _ = try Args.parse([
+                "--model", "m.gturbo", "--chat-prompt", "hello",
+                "--prompt", "raw",
+            ])
+        }
     }
 
     /// Default moved from a fixed 128 to auto on 2026-08-05. Measured warm on
diff --git a/Tests/TurboFieldfare/Core/Tokenization/ChatMLDecoderTests.swift b/Tests/TurboFieldfare/Core/Tokenization/ChatMLDecoderTests.swift
index cd4ccef..1b217d0 100644
--- a/Tests/TurboFieldfare/Core/Tokenization/ChatMLDecoderTests.swift
+++ b/Tests/TurboFieldfare/Core/Tokenization/ChatMLDecoderTests.swift
@@ -74,6 +74,20 @@ struct ChatMLDecoderTests {
         #expect(d.reasoningTokens == 0)
     }
 
+    @Test("Trailing detokenizer text respects the active channel")
+    func trailingTextRespectsChannel() throws {
+        let d = StructuredAssistantDecoder(tokenizer: tok,
+                                           allowedTools: [],
+                                           idGenerator: { "call_fixed" },
+                                           thinkingPreopened: true)
+        #expect(try d.consumeTrailingText("hidden tail") == [.reasoning("hidden tail")])
+        let events = try feed("</think>\n\nvisible", into: d)
+            + d.consumeTrailingText(" tail")
+        #expect(visibleText(events) == "visible tail")
+        #expect(d.reasoningText == "hidden tail")
+        try d.finish()
+    }
+
     @Test("Visible text streams through unchanged")
     func plainText() throws {
         let d = decoder()
@@ -93,6 +107,26 @@ struct ChatMLDecoderTests {
         try d.finish()
     }
 
+    @Test("Interleaved think blocks preserve visible answer order")
+    func interleavedThinking() throws {
+        let d = decoder(allowedTools: [])
+        let events = try feed(
+            "<think>first thought</think>first answer "
+                + "<think>second thought</think>second answer",
+            into: d)
+        #expect(visibleText(events) == "first answer second answer")
+        #expect(d.reasoningText == "first thoughtsecond thought")
+        #expect(events.filter {
+            if case .reasoningStarted = $0 { return true }
+            return false
+        }.count == 2)
+        #expect(events.filter {
+            if case .reasoningEnded = $0 { return true }
+            return false
+        }.count == 2)
+        try d.finish()
+    }
+
     @Test("Tool call spans buffer and emit a parsed call")
     func toolCallBuffering() throws {
         let d = decoder()
diff --git a/Tests/TurboFieldfare/Core/Tokenization/ChatMLTemplateTests.swift b/Tests/TurboFieldfare/Core/Tokenization/ChatMLTemplateTests.swift
index e78a62c..0caf2dc 100644
--- a/Tests/TurboFieldfare/Core/Tokenization/ChatMLTemplateTests.swift
+++ b/Tests/TurboFieldfare/Core/Tokenization/ChatMLTemplateTests.swift
@@ -79,6 +79,18 @@ struct ChatMLTemplateTests {
             + "<|im_start|>assistant\n<think>\n")
     }
 
+    @Test("Non-thinking chat closes the think block before generation")
+    func nonThinkingChat() throws {
+        let prompt = try tok.applyChatTemplateWithoutThinking([
+            Message(role: .user, content: "Hi"),
+        ])
+        #expect(prompt == "<|im_start|>user\nHi<|im_end|>\n"
+            + "<|im_start|>assistant\n<think>\n\n</think>\n\n")
+        let ids = tok.encode(prompt, addBOS: false)
+        #expect(ids.contains(tok.thinkStartID ?? -1))
+        #expect(ids.contains(tok.thinkEndID ?? -1))
+    }
+
     @Test("Message content is trimmed like the Gemma path")
     func contentTrimming() throws {
         let p = try tok.applyChatTemplate([Message(role: .user, content: "  Hi \n")])
diff --git a/Tests/TurboFieldfareApp/Core/Configuration/AppRuntimeOptionsTests.swift b/Tests/TurboFieldfareApp/Core/Configuration/AppRuntimeOptionsTests.swift
index 96236fa..8d5036e 100644
--- a/Tests/TurboFieldfareApp/Core/Configuration/AppRuntimeOptionsTests.swift
+++ b/Tests/TurboFieldfareApp/Core/Configuration/AppRuntimeOptionsTests.swift
@@ -6,17 +6,19 @@ import TurboFieldfare
 @Suite struct AppRuntimeOptionsTests {
     @Test func defaultsMatchProduction() throws {
         let options = AppRuntimeOptions()
-        #expect(options.expertCacheSlots == 64)
+        #expect(options.expertCacheSlots == 96)
         #expect(options.expertCachePolicy == .lfu)
         #expect(options.prefillEnabled)
         #expect(options.prefillChunkTokens == 128)
         #expect(options.rdadvisePolicy == .off)
         #expect(options.modelVerification == .fullSha256)
+        #expect(options.expertCacheResidency == .osManaged)
+        #expect(options.expertCacheHotSlots == 16)
 
         let runtime = try options.resolvedRuntimeConfiguration(forceLogitsHead: false)
-        #expect(runtime == .production)
+        #expect(runtime.expertCacheSlots == 96)
         #expect(options.resultSummary ==
-            "Cache 64 LFU, prefill 128, FP16 KV, RDADVISE off, full SHA-256")
+            "Cache 96 LFU, OS-managed / hot 16, prefill 128, FP16 KV, RDADVISE off, full SHA-256")
     }
 
     @Test func everyPublicChoiceMapsToRuntime() throws {
@@ -60,6 +62,9 @@ import TurboFieldfare
         #expect(throws: AppInferenceError.self) {
             try AppRuntimeOptions(prefillChunkTokens: 96).validate()
         }
+        #expect(throws: AppInferenceError.self) {
+            try AppRuntimeOptions(expertCacheSlots: 8, expertCacheHotSlots: 16).validate()
+        }
     }
 
     @Test func loadedRuntimeKeyTracksOnlyLoadTimeChoices() {
@@ -74,6 +79,8 @@ import TurboFieldfare
         value = base; value.expertCachePolicy = .lru; variants.append(value)
         value = base; value.rdadvisePolicy = .bounded; variants.append(value)
         value = base; value.modelVerification = .trustedInstall; variants.append(value)
+        value = base; value.expertCacheResidency = .fixed; variants.append(value)
+        value = base; value.expertCacheHotSlots = 8; variants.append(value)
 
         for variant in variants {
             #expect(AppLoadedRuntimeKey(
diff --git a/Tests/TurboFieldfareApp/Core/Configuration/MacAppSettingsTests.swift b/Tests/TurboFieldfareApp/Core/Configuration/MacAppSettingsTests.swift
index 1bc66aa..06b6fee 100644
--- a/Tests/TurboFieldfareApp/Core/Configuration/MacAppSettingsTests.swift
+++ b/Tests/TurboFieldfareApp/Core/Configuration/MacAppSettingsTests.swift
@@ -71,7 +71,10 @@ import Testing
             topK: 32,
             topPEnabled: false,
             topP: 0.8,
-            prefillEnabled: false)
+            thinkingEnabled: true,
+            prefillEnabled: false,
+            expertCacheResidency: .fixed,
+            expertCacheHotSlots: 0)
         try MacAppSettingsFileStore.save(initial, forModelDirectory: modelDirectory)
 
         let model = AppModel(
@@ -84,11 +87,17 @@ import Testing
         #expect(model.topK == 32)
         #expect(!model.topPEnabled)
         #expect(model.topP == 0.8)
+        #expect(model.thinkingEnabled)
         #expect(!model.runtimeOptions.prefillEnabled)
+        #expect(model.runtimeOptions.expertCacheResidency == .fixed)
+        #expect(model.runtimeOptions.expertCacheHotSlots == 0)
 
         model.temperature = 0.6
         model.runtimeOptions.expertCacheSlots = 32
         model.runtimeOptions.prefillEnabled = true
+        model.runtimeOptions.expertCacheResidency = .osManaged
+        model.runtimeOptions.expertCacheHotSlots = 8
+        model.thinkingEnabled = false
         let beforeGenerate = MacAppSettingsFileStore.loadOrCreate(
             forModelDirectory: modelDirectory)
         #expect(beforeGenerate == initial)
@@ -101,6 +110,9 @@ import Testing
         #expect(saved.temperature == 0.6)
         #expect(saved.expertCacheSlots == 32)
         #expect(saved.prefillEnabled)
+        #expect(saved.expertCacheResidency == .osManaged)
+        #expect(saved.expertCacheHotSlots == 8)
+        #expect(!saved.thinkingEnabled)
         model.cancel()
     }
 
diff --git a/Tests/TurboFieldfareApp/Core/Configuration/RuntimeDefaultsDriftTests.swift b/Tests/TurboFieldfareApp/Core/Configuration/RuntimeDefaultsDriftTests.swift
index c376211..8906963 100644
--- a/Tests/TurboFieldfareApp/Core/Configuration/RuntimeDefaultsDriftTests.swift
+++ b/Tests/TurboFieldfareApp/Core/Configuration/RuntimeDefaultsDriftTests.swift
@@ -28,21 +28,30 @@ struct RuntimeDefaultsDriftTests {
         #expect(runtime.rdadvisePolicy == RuntimeDefaults.rdadvisePolicy)
     }
 
-    @Test("The Mac app's options and its PERSISTED settings both match")
+    @Test("ElasticUMA app, persisted settings, and decode wire defaults match")
     func appMatches() throws {
         let options = AppRuntimeOptions()
-        #expect(options.expertCacheSlots == RuntimeDefaults.expertCacheSlots)
+        #expect(options.expertCacheSlots == 96)
+        #expect(options.expertCacheResidency == .osManaged)
+        #expect(options.expertCacheHotSlots == 16)
         #expect(options.prefillChunkTokens == RuntimeDefaults.prefillChunkTokens)
         #expect(options.prefillEnabled == RuntimeDefaults.prefillEnabled)
         #expect(try options.resolvedRuntimeConfiguration(forceLogitsHead: false)
-                == .production)
+                .expertCacheSlots == 96)
 
         // The settings FILE is the one that actually reaches a user: a stored
         // value wins over the option default, which is how the Mac app kept
         // running 16 after the option was changed to 64.
         let stored = MacAppSettings()
-        #expect(stored.expertCacheSlots == RuntimeDefaults.expertCacheSlots)
+        #expect(stored.expertCacheSlots == options.expertCacheSlots)
+        #expect(stored.expertCacheResidency == options.expertCacheResidency)
+        #expect(stored.expertCacheHotSlots == options.expertCacheHotSlots)
         #expect(stored.prefillEnabled == RuntimeDefaults.prefillEnabled)
+
+        let wire = DecodeRuntimeOptions()
+        #expect(wire.expertCacheSlots == options.expertCacheSlots)
+        #expect(wire.expertCacheResidency == options.expertCacheResidency.rawValue)
+        #expect(wire.expertCacheHotSlots == options.expertCacheHotSlots)
     }
 
     /// The wire format cannot reference RuntimeDefaults — it deliberately has
@@ -50,9 +59,11 @@ struct RuntimeDefaultsDriftTests {
     @Test("The decode-service wire format's literals still match")
     func decodeProtocolMatches() {
         let options = DecodeRuntimeOptions()
-        #expect(options.expertCacheSlots == RuntimeDefaults.expertCacheSlots)
+        #expect(options.expertCacheSlots == 96)
         #expect(options.prefillChunkTokens == RuntimeDefaults.prefillChunkTokens)
         #expect(options.prefillEnabled == RuntimeDefaults.prefillEnabled)
+        #expect(options.expertCacheResidency == "os-managed")
+        #expect(options.expertCacheHotSlots == 16)
     }
 
     /// Bumping the stored-settings version is what migrates existing installs
@@ -60,7 +71,7 @@ struct RuntimeDefaultsDriftTests {
     @Test("Stored settings from an older version are rejected and re-defaulted")
     func staleSettingsAreInvalid() {
         var old = MacAppSettings()
-        old.version = 1
+        old.version = 2
         #expect(!old.isValid())
         #expect(MacAppSettings().version == MacAppSettings.currentVersion)
     }
diff --git a/Tests/TurboFieldfareApp/Core/Inference/DecodeProtocolTests.swift b/Tests/TurboFieldfareApp/Core/Inference/DecodeProtocolTests.swift
index f8a1cfc..2bca91a 100644
--- a/Tests/TurboFieldfareApp/Core/Inference/DecodeProtocolTests.swift
+++ b/Tests/TurboFieldfareApp/Core/Inference/DecodeProtocolTests.swift
@@ -10,7 +10,9 @@ import TurboFieldfareDecodeProtocol
             prefillEnabled: false,
             prefillChunkTokens: 64,
             rdadvisePolicy: "adaptive",
-            modelVerification: "trusted-install")
+            modelVerification: "trusted-install",
+            expertCacheResidency: "fixed",
+            expertCacheHotSlots: 0)
         let request = DecodeLoadRequest(
             modelPath: "/tmp/model.gturbo",
             maxContextTokens: 8192,
@@ -41,7 +43,11 @@ import TurboFieldfareDecodeProtocol
             rdadviseCallsPerToken: 0,
             rdadviseMegabytesPerToken: 0,
             rdadviseSkippedPerToken: 0,
-            rdadviseFailures: 0)
+            rdadviseFailures: 0,
+            expertHotSlots: 16,
+            expertVolatileTransitions: 900,
+            expertEmptyRecoveries: 21,
+            expertReclaimedBytes: 400_000_000)
         let event = DecodeServiceEvent(
             kind: .finished,
             generationID: UUID(),
@@ -69,6 +75,25 @@ import TurboFieldfareDecodeProtocol
         #expect(decoded.runner == runner)
     }
 
+    @Test func generationRequestRoundTripPreservesThinkingPreference() throws {
+        let request = DecodeGenerationRequest(
+            prompt: "reason carefully",
+            maxNewTokens: 64,
+            maxContextTokens: 4_096,
+            temperature: 0.2,
+            thinkingEnabled: true)
+        let pipe = Pipe()
+        try pipe.fileHandleForWriting.write(
+            contentsOf: DecodeFrameCodec.encode(request))
+        try pipe.fileHandleForWriting.close()
+        let decoded = try DecodeFrameCodec.read(
+            DecodeGenerationRequest.self,
+            from: pipe.fileHandleForReading)
+
+        #expect(decoded.thinkingEnabled)
+        #expect(decoded.prompt == request.prompt)
+    }
+
     @Test func prefillEventRoundTripPreservesProgress() throws {
         let event = DecodeServiceEvent(
             kind: .prefill,
@@ -97,6 +122,7 @@ import TurboFieldfareDecodeProtocol
             generationID: UUID(),
             sequence: 1,
             textDelta: "caf\u{00E9}",
+            tokenChannel: "reasoning",
             tokenCount: 1)
         let frame = try DecodeFrameCodec.encode(event)
         let pipe = Pipe()
@@ -111,6 +137,7 @@ import TurboFieldfareDecodeProtocol
 
         #expect(decoded.sequence == 1)
         #expect(decoded.textDelta == "caf\u{00E9}")
+        #expect(decoded.tokenChannel == "reasoning")
     }
 
     @Test func oversizedPayloadIsRejectedBeforeEncoding() {
diff --git a/Tests/TurboFieldfareApp/Core/Installation/AppModelCatalogTests.swift b/Tests/TurboFieldfareApp/Core/Installation/AppModelCatalogTests.swift
new file mode 100644
index 0000000..acc8d33
--- /dev/null
+++ b/Tests/TurboFieldfareApp/Core/Installation/AppModelCatalogTests.swift
@@ -0,0 +1,206 @@
+import Foundation
+import Testing
+@testable import TurboFieldfareAppCore
+
+@Suite(.serialized)
+struct AppModelCatalogTests {
+    @Test func exactPinnedHubResultIsInstallable() async throws {
+        CatalogURLProtocol.handler = { request in
+            if request.url?.path == "/api/models" {
+                return (200, Data(Self.qwenSearchResponse.utf8))
+            }
+            return (404, Data())
+        }
+        let snapshot = await AppModelCatalogClient(session: Self.session()).refresh()
+        let qwen = try #require(snapshot.available.first {
+            $0.id == AppModelInstallDescriptor.qwen36.repoID
+        })
+        #expect(qwen.compatibility == .verified)
+        #expect(qwen.canInstall)
+        #expect(qwen.installDescriptor?.revision == AppModelInstallDescriptor.qwen36.revision)
+        #expect(qwen.downloads == 42)
+        #expect(snapshot.hubWarning == nil)
+    }
+
+    @Test func hubFailureKeepsPinnedOfflineCatalog() async {
+        CatalogURLProtocol.handler = { _ in (503, Data()) }
+        let snapshot = await AppModelCatalogClient(session: Self.session()).refresh()
+        #expect(snapshot.available.count == AppModelInstallDescriptor.supported.count)
+        #expect(snapshot.available.allSatisfy { $0.compatibility == .verified })
+        #expect(snapshot.hubWarning != nil)
+    }
+
+    @Test func liveSearchFetchesRawConfigBeforeClassifyingAMirror() async throws {
+        CatalogURLProtocol.handler = { request in
+            switch request.url?.path {
+            case "/api/models":
+                return (200, Data(Self.incompleteMirrorSearchResponse.utf8))
+            case "/community/Qwen3.6-35B-A3B-4bit/resolve/aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa/config.json":
+                return (200, Data(Self.qwenRawConfig.utf8))
+            default:
+                return (404, Data())
+            }
+        }
+
+        let snapshot = await AppModelCatalogClient(session: Self.session())
+            .refresh(search: "community Qwen3.6")
+        let mirror = try #require(snapshot.available.first {
+            $0.id == "community/Qwen3.6-35B-A3B-4bit"
+        })
+        #expect(snapshot.hubQuery == "community Qwen3.6")
+        #expect(mirror.family == .qwen36)
+        #expect(mirror.compatibility == .needsValidation)
+        #expect(!mirror.canInstall)
+        #expect(snapshot.hubWarning == nil)
+    }
+
+    @Test func exactRepositoryURLIsLookedUpDirectlyAndUsesTheConfiguredToken() async throws {
+        CatalogURLProtocol.handler = { request in
+            guard request.value(forHTTPHeaderField: "Authorization") == "Bearer test-token" else {
+                return (401, Data())
+            }
+            switch request.url?.path {
+            case "/api/models/community/Qwen3.6-35B-A3B-4bit":
+                return (200, Data(Self.incompleteMirrorModelInfo.utf8))
+            case "/community/Qwen3.6-35B-A3B-4bit/resolve/aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa/config.json":
+                return (200, Data(Self.qwenRawConfig.utf8))
+            default:
+                return (404, Data())
+            }
+        }
+
+        let query = "https://huggingface.co/community/Qwen3.6-35B-A3B-4bit"
+        let snapshot = await AppModelCatalogClient(
+            session: Self.session(), token: "test-token").refresh(search: query)
+        let result = try #require(snapshot.available.first {
+            $0.id == "community/Qwen3.6-35B-A3B-4bit"
+        })
+        #expect(snapshot.hubQuery == query)
+        #expect(result.family == .qwen36)
+        #expect(result.compatibility == .needsValidation)
+        #expect(!result.canInstall)
+    }
+
+    private static func session() -> URLSession {
+        let configuration = URLSessionConfiguration.ephemeral
+        configuration.protocolClasses = [CatalogURLProtocol.self]
+        return URLSession(configuration: configuration)
+    }
+
+    private static let qwenSearchResponse = """
+    [{
+      "id": "mlx-community/Qwen3.6-35B-A3B-4bit",
+      "sha": "38740b847e4cb78f352aba30aa41c76e08e6eb46",
+      "downloads": 42,
+      "lastModified": "2026-08-25T00:00:00.000Z",
+      "config": {
+        "model_type": "qwen3_5_moe",
+        "quantization": {"group_size": 64, "bits": 4, "mode": "affine"},
+        "text_config": {
+          "model_type": "qwen3_5_moe_text",
+          "hidden_size": 2048,
+          "num_hidden_layers": 40,
+          "num_attention_heads": 16,
+          "num_key_value_heads": 2,
+          "num_experts": 256,
+          "num_experts_per_tok": 8,
+          "moe_intermediate_size": 512,
+          "vocab_size": 248320
+        }
+      }
+    }]
+    """
+
+    private static let incompleteMirrorSearchResponse = """
+    [{
+      "id": "community/Qwen3.6-35B-A3B-4bit",
+      "sha": "aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa",
+      "downloads": 7,
+      "lastModified": "2026-08-25T00:00:00.000Z",
+      "config": {
+        "model_type": "qwen3_5_moe",
+        "quantization_config": {"bits": 4}
+      }
+    }]
+    """
+
+    private static let incompleteMirrorModelInfo = """
+    {
+      "id": "community/Qwen3.6-35B-A3B-4bit",
+      "sha": "aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa",
+      "downloads": 7,
+      "lastModified": "2026-08-25T00:00:00.000Z",
+      "config": {
+        "model_type": "qwen3_5_moe",
+        "quantization_config": {"bits": 4}
+      }
+    }
+    """
+
+    private static let qwenRawConfig = """
+    {
+      "model_type": "qwen3_5_moe",
+      "quantization": {"group_size": 64, "bits": 4, "mode": "affine"},
+      "text_config": {
+        "model_type": "qwen3_5_moe_text",
+        "hidden_size": 2048,
+        "shared_expert_intermediate_size": 512,
+        "num_hidden_layers": 40,
+        "num_attention_heads": 16,
+        "num_key_value_heads": 2,
+        "head_dim": 256,
+        "num_experts": 256,
+        "num_experts_per_tok": 8,
+        "moe_intermediate_size": 512,
+        "vocab_size": 248320,
+        "tie_word_embeddings": false,
+        "attn_output_gate": true,
+        "hidden_act": "silu",
+        "linear_num_key_heads": 16,
+        "linear_num_value_heads": 32,
+        "linear_key_head_dim": 128,
+        "linear_value_head_dim": 128,
+        "linear_conv_kernel_dim": 4,
+        "rope_parameters": {
+          "rope_theta": 10000000,
+          "partial_rotary_factor": 0.25
+        },
+        "layer_types": [
+          "linear_attention", "linear_attention", "linear_attention", "full_attention",
+          "linear_attention", "linear_attention", "linear_attention", "full_attention",
+          "linear_attention", "linear_attention", "linear_attention", "full_attention",
+          "linear_attention", "linear_attention", "linear_attention", "full_attention",
+          "linear_attention", "linear_attention", "linear_attention", "full_attention",
+          "linear_attention", "linear_attention", "linear_attention", "full_attention",
+          "linear_attention", "linear_attention", "linear_attention", "full_attention",
+          "linear_attention", "linear_attention", "linear_attention", "full_attention",
+          "linear_attention", "linear_attention", "linear_attention", "full_attention",
+          "linear_attention", "linear_attention", "linear_attention", "full_attention"
+        ]
+      }
+    }
+    """
+}
+
+private final class CatalogURLProtocol: URLProtocol, @unchecked Sendable {
+    nonisolated(unsafe) static var handler: @Sendable (URLRequest) -> (Int, Data) = {
+        _ in (500, Data())
+    }
+
+    override class func canInit(with request: URLRequest) -> Bool { true }
+    override class func canonicalRequest(for request: URLRequest) -> URLRequest { request }
+
+    override func startLoading() {
+        let (status, data) = Self.handler(request)
+        let response = HTTPURLResponse(
+            url: request.url!,
+            statusCode: status,
+            httpVersion: "HTTP/1.1",
+            headerFields: ["Content-Type": "application/json"])!
+        client?.urlProtocol(self, didReceive: response, cacheStoragePolicy: .notAllowed)
+        if !data.isEmpty { client?.urlProtocol(self, didLoad: data) }
+        client?.urlProtocolDidFinishLoading(self)
+    }
+
+    override func stopLoading() {}
+}
diff --git a/Tests/TurboFieldfareApp/Core/Installation/AppModelInstallTests.swift b/Tests/TurboFieldfareApp/Core/Installation/AppModelInstallTests.swift
index e3889a1..8ee52c4 100644
--- a/Tests/TurboFieldfareApp/Core/Installation/AppModelInstallTests.swift
+++ b/Tests/TurboFieldfareApp/Core/Installation/AppModelInstallTests.swift
@@ -78,7 +78,7 @@ import TurboFieldfareRepackCore
   @MainActor
   @Test func defaultInstallDescriptorMatchesPinnedAudit() {
     let descriptor = AppModelInstallDescriptor.default
-    #expect(descriptor.displayName == "Gemma 4 26B-A4B IT 4-bit")
+    #expect(descriptor.displayName == "Gemma 4 26B-A4B · 4-bit")
     #expect(descriptor.repoID == "mlx-community/gemma-4-26b-a4b-it-4bit")
     #expect(descriptor.revision == "0d77464eeb233a2da68ebf9d7dc4edaac7db956d")
     #expect(descriptor.sourceIndexSHA256 == "bf198c9f5ea6462addca1966e5dd669c407537a876e82cf06db9084c5c850b13")
@@ -87,6 +87,31 @@ import TurboFieldfareRepackCore
     #expect(descriptor.requiredFreeBytes == 15_432_772_572)
   }
 
+  @Test func dynamicMirrorHasAUniqueSelectionKeyAndRemainsSelectable() {
+    let baseline = AppModelInstallDescriptor.qwen36
+    #expect(baseline.approximateDownloadBytes == 20_146_183_200)
+    let mirror = AppModelInstallDescriptor(
+      family: baseline.family,
+      displayName: "Community Qwen",
+      repoID: "community/Qwen3.6-35B-A3B-4bit",
+      revision: String(repeating: "a", count: 40),
+      sourceIndexSHA256: baseline.sourceIndexSHA256,
+      approximateDownloadBytes: baseline.approximateDownloadBytes,
+      installedBytes: baseline.installedBytes,
+      rangeStagingBytes: baseline.rangeStagingBytes,
+      reserveBytes: baseline.reserveBytes)
+
+    let choices = AppModelInstallDescriptor.selectionChoices(including: mirror)
+    #expect(mirror.id == baseline.id)
+    #expect(mirror.selectionKey != baseline.selectionKey)
+    #expect(choices.first == mirror)
+    #expect(Set(choices.map(\.selectionKey)).count == choices.count)
+    #expect(mirror.runtimeCapabilities.inputSummary == "Text only")
+    #expect(mirror.runtimeCapabilities.supports(.text))
+    #expect(!mirror.runtimeCapabilities.supports(.image))
+    #expect(!mirror.runtimeCapabilities.supports(.audio))
+  }
+
   @MainActor
   @Test func insufficientSpaceDisablesInstallAndExposesShortfall() {
     let requirement = AppModelInstallRequirement(
diff --git a/Tests/TurboFieldfareApp/Core/Installation/AppModelLocationTests.swift b/Tests/TurboFieldfareApp/Core/Installation/AppModelLocationTests.swift
index a74103d..cc6b0bb 100644
--- a/Tests/TurboFieldfareApp/Core/Installation/AppModelLocationTests.swift
+++ b/Tests/TurboFieldfareApp/Core/Installation/AppModelLocationTests.swift
@@ -6,42 +6,26 @@ import Testing
     @Test func explicitURLWins() {
         let result = AppModelLocation.resolve(
             explicitURL: URL(fileURLWithPath: "/models/explicit.gturbo"),
-            executableURL: nil,
             currentDirectoryURL: URL(fileURLWithPath: "/repo"),
-            applicationSupportURL: URL(fileURLWithPath: "/support"),
-            fileExists: { _ in false })
+            cacheRootURL: URL(fileURLWithPath: "/cache/elasticuma"),
+            installDirectoryName: "qwen.gturbo")
         #expect(result.path == "/models/explicit.gturbo")
     }
 
-    @Test func executableAncestorFindsPackageRootOutsideCWD() {
-        let files: Set<String> = ["/repo/Package.swift", "/repo/Sources/TurboFieldfareApp/Mac"]
+    @Test func canonicalCacheDoesNotDependOnCheckoutOrExecutable() {
         let result = AppModelLocation.resolve(
             explicitURL: nil,
-            executableURL: URL(fileURLWithPath: "/repo/.build/debug/TurboFieldfareMac"),
-            currentDirectoryURL: URL(fileURLWithPath: "/elsewhere"),
-            applicationSupportURL: URL(fileURLWithPath: "/support"),
-            fileExists: files.contains)
-        #expect(result.path == "/repo/scratch/gemma4.gturbo")
-    }
-
-    @Test func currentDirectoryCanBePackageRoot() {
-        let files: Set<String> = ["/repo/Package.swift", "/repo/Sources/TurboFieldfareApp/Mac"]
-        let result = AppModelLocation.resolve(
-            explicitURL: nil,
-            executableURL: nil,
             currentDirectoryURL: URL(fileURLWithPath: "/repo"),
-            applicationSupportURL: URL(fileURLWithPath: "/support"),
-            fileExists: files.contains)
-        #expect(result.path == "/repo/scratch/gemma4.gturbo")
+            cacheRootURL: URL(fileURLWithPath: "/cache/elasticuma"),
+            installDirectoryName: "qwen.gturbo")
+        #expect(result.path == "/cache/elasticuma/packed/qwen.gturbo")
     }
 
-    @Test func standaloneAppFallsBackToApplicationSupport() {
-        let result = AppModelLocation.resolve(
-            explicitURL: nil,
-            executableURL: URL(fileURLWithPath: "/Applications/TurboFieldfareMac"),
-            currentDirectoryURL: URL(fileURLWithPath: "/"),
-            applicationSupportURL: URL(fileURLWithPath: "/support"),
-            fileExists: { _ in false })
-        #expect(result.path == "/support/TurboFieldfare/gemma4.gturbo")
+    @Test func supportedModelsHavePinnedDistinctCanonicalPaths() {
+        let qwen = AppModelInstallDescriptor.qwen36
+        let gemma = AppModelInstallDescriptor.default
+        #expect(qwen.installDirectoryName.contains(qwen.revision))
+        #expect(gemma.installDirectoryName.contains(gemma.revision))
+        #expect(qwen.installDirectoryName != gemma.installDirectoryName)
     }
 }
diff --git a/Tests/TurboFieldfareApp/Core/State/AppModelTests.swift b/Tests/TurboFieldfareApp/Core/State/AppModelTests.swift
index a2c7723..7c04954 100644
--- a/Tests/TurboFieldfareApp/Core/State/AppModelTests.swift
+++ b/Tests/TurboFieldfareApp/Core/State/AppModelTests.swift
@@ -17,13 +17,12 @@ import TurboFieldfare
         #expect(request.maxNewTokens == 4_096)
         #expect(request.repetitionPenalty == 1)
         #expect(!request.isPureGreedy)
-        // Reference the constant, not a literal: a literal here is an eighth
-        // copy of the default and would drift the same way the other seven did.
-        #expect(request.runtimeOptions.expertCacheSlots
-                == RuntimeDefaults.expertCacheSlots)
+        #expect(request.runtimeOptions.expertCacheSlots == 96)
         #expect(request.runtimeOptions.expertCachePolicy == .lfu)
         #expect(request.runtimeOptions.rdadvisePolicy == .off)
         #expect(request.runtimeOptions.prefillEnabled)
+        #expect(request.runtimeOptions.expertCacheResidency == .osManaged)
+        #expect(request.runtimeOptions.expertCacheHotSlots == 16)
     }
 
     @MainActor
@@ -153,6 +152,7 @@ import TurboFieldfare
         model.run()
 
         #expect(model.outputPromptText == "original prompt")
+        #expect(model.promptText.isEmpty)
         #expect(model.hasOutputTranscript)
         #expect(model.outputResponsePlainText.isEmpty)
         #expect(model.outputConversationPlainText == "You:\noriginal prompt")
@@ -167,6 +167,40 @@ import TurboFieldfare
         #expect(!model.outputConversationPlainText.contains("edited prompt"))
     }
 
+    @MainActor
+    @Test func interleavedThinkingStaysSeparateFromAnswerText() {
+        let model = readyModel(client: MockInferenceClient())
+        model.apply(.token(AppTokenEvent(
+            index: 0, textDelta: "", elapsedDecodeSeconds: 0.1,
+            channel: .reasoningStarted)))
+        model.apply(.token(AppTokenEvent(
+            index: 1, textDelta: "first thought", elapsedDecodeSeconds: 0.2,
+            channel: .reasoning)))
+        model.apply(.token(AppTokenEvent(
+            index: 2, textDelta: "", elapsedDecodeSeconds: 0.3,
+            channel: .reasoningEnded)))
+        model.apply(.token(AppTokenEvent(
+            index: 3, textDelta: "answer one. ", elapsedDecodeSeconds: 0.4)))
+        model.apply(.token(AppTokenEvent(
+            index: 4, textDelta: "second thought", elapsedDecodeSeconds: 0.5,
+            channel: .reasoning)))
+        model.apply(.token(AppTokenEvent(
+            index: 5, textDelta: "answer two.", elapsedDecodeSeconds: 0.6)))
+
+        #expect(model.outputText == "answer one. answer two.")
+        #expect(model.thinkingBlocks.map(\.text) == ["first thought", "second thought"])
+        #expect(model.thinkingBlocks.allSatisfy { $0.isComplete })
+        #expect(!model.isThinking)
+    }
+
+    @MainActor
+    @Test func thinkingPreferenceReachesGenerationRequest() throws {
+        let model = readyModel(client: MockInferenceClient())
+        model.promptText = "think"
+        model.thinkingEnabled = true
+        #expect(try model.makeRequest().thinkingEnabled)
+    }
+
     @MainActor
     @Test func staleReadySessionDisablesGenerationUntilReload() throws {
         let client = MockLifecycleInferenceClient()
@@ -222,7 +256,7 @@ import TurboFieldfare
     @MainActor
     @Test func cancelDuringPrefillKeepsPromptSnapshotUntilClear() async throws {
         let client = MockInferenceClient(response: "unused", tokenDelayNanos: 1_000_000)
-        client.prefillSteps = 20
+        client.prefillSteps = 200
         let model = readyModel(client: client)
         model.promptText = "prefill prompt"
         model.run()
diff --git a/Tests/TurboFieldfareApp/MacPresentation/ResponseMarkdownRendererTests.swift b/Tests/TurboFieldfareApp/MacPresentation/ResponseMarkdownRendererTests.swift
index 4cebe1c..2807a85 100644
--- a/Tests/TurboFieldfareApp/MacPresentation/ResponseMarkdownRendererTests.swift
+++ b/Tests/TurboFieldfareApp/MacPresentation/ResponseMarkdownRendererTests.swift
@@ -108,9 +108,9 @@ import Testing
         let color = TurboFieldfareMacTheme.accentNSColor
             .usingColorSpace(.sRGB)
         #expect(color != nil)
-        #expect(abs((color?.redComponent ?? 0) - 106.0 / 255.0) < 0.000_001)
-        #expect(abs((color?.greenComponent ?? 0) - 186.0 / 255.0) < 0.000_001)
-        #expect(abs((color?.blueComponent ?? 0) - 113.0 / 255.0) < 0.000_001)
+        #expect(abs((color?.redComponent ?? 0) - 45.0 / 255.0) < 0.000_001)
+        #expect(abs((color?.greenComponent ?? 0) - 139.0 / 255.0) < 0.000_001)
+        #expect(abs((color?.blueComponent ?? 0) - 63.0 / 255.0) < 0.000_001)
     }
 
     @Test func rebuildsThenAppendsOnlyNewResponseSuffix() {
diff --git a/Tests/TurboFieldfareRepack/Core/Command/RepackCLITests.swift b/Tests/TurboFieldfareRepack/Core/Command/RepackCLITests.swift
index 244ef06..eeb72bc 100644
--- a/Tests/TurboFieldfareRepack/Core/Command/RepackCLITests.swift
+++ b/Tests/TurboFieldfareRepack/Core/Command/RepackCLITests.swift
@@ -71,7 +71,7 @@ struct RepackCLITests {
     private func run(_ arguments: [String]) throws
         -> (status: Int32, stdout: String, stderr: String) {
         let executable = URL(fileURLWithPath: FileManager.default.currentDirectoryPath)
-            .appendingPathComponent(".build/debug/TurboFieldfareRepack")
+            .appendingPathComponent(".build/debug/slipstream-repack")
         let process = Process()
         let stdout = Pipe()
         let stderr = Pipe()
diff --git a/Tests/TurboFieldfareServer/OpenAIValidationTests.swift b/Tests/TurboFieldfareServer/OpenAIValidationTests.swift
index 1e3e6e0..7f0315e 100644
--- a/Tests/TurboFieldfareServer/OpenAIValidationTests.swift
+++ b/Tests/TurboFieldfareServer/OpenAIValidationTests.swift
@@ -267,7 +267,9 @@ struct GemmaToolCallTests {
         #expect(try decoder.consume(tokenID: tokenizer.channelStartID, delta: "").isEmpty)
         #expect(try decoder.consume(tokenID: tokenizer.bosID, delta: "thought").isEmpty)
         #expect(try decoder.consume(tokenID: tokenizer.bosID, delta: "\n").isEmpty)
-        #expect(try decoder.consume(tokenID: tokenizer.bosID, delta: "private").isEmpty)
+        #expect(try decoder.consume(tokenID: tokenizer.bosID, delta: "private") == [
+            .reasoning("private"),
+        ])
         #expect(try decoder.consume(tokenID: tokenizer.channelEndID, delta: "").isEmpty)
         #expect(try decoder.consume(tokenID: tokenizer.bosID, delta: "visible") == [
             .content("visible"),
